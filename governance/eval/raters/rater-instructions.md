你是檢索相關性評審。任務:對 governance/eval/retrieval-labeling-sheet.md 的每個案例、每個候選節點,標 0/1/2。

判準:
- 搜尋案例(S01-S30):想像使用者在知識架構圖搜這個詞——這個候選是不是他真正想找的?
  2=必看(這查詢的正主/核心文件) 1=有用(相關但非核心) 0=噪音(只是字面碰到)
- 編輯案例(E01-E20):想像開發者正要改這支 code 檔——這篇筆記該不該被主動推到他眼前?
  2=必看(不看會出事:合約/事故/直接規格) 1=有用(背景參考) 0=噪音

可讀 vault(docs/lumos-toolchain-knowledge/)內任何節點判斷,唯讀。
輸出:**純 JSON、無其他文字**,格式 {"S01": {"節點路徑或候選字串": 0|1|2, ...}, ..., "E20": {...}}。
每個候選都要明列(含 0),鍵一字不差照抄 sheet 的候選字串。
