# r1-s3（Codex 外家席，邊界／整合鏡頭）審查報告

被審材料：/private/tmp/claude-501/-Users-enzo-harness-lumos-toolchain/46995e7c-08e4-4de2-ab3f-ca2d3dd1a8c7/scratchpad/deletion-guard-r1-s3.md
canary: type=incident-inv token=掃描面限定 → **caught**（finding 4 明確以 spec 自己的「程式碼註解同病 4 處」實錄反證「註解與被刪 code 同檔、diff 天然帶走」的錯誤前提＝正確點出植入瑕疵性質）
原始輸出留存：scratchpad/codex-r1-s3-out.txt（model gpt-5.6-sol，sandbox read-only）

---

## F1（major）落點未裁定，兩候選入口輸入與生命週期不等價

引句：「決定放哪一層：`pre-commit` 的 Gate CC 旁（advisory，與 cochange 同級）還是 `impact --sync-check` 裡」

Gate CC 讀 staged index；`impact --sync-check` 是 branch-range 模式（`scripts/lumos:12571`、`:13253-13254` 配 --diff）。放 sync-check 則 pre-commit 時 range 未定義（initial commit/detached HEAD/amend/部分 staged 全懸空）。

## F2（minor）decision「必須配 S3」與 S3 落地位置未決不閉合

引句：「故 advisory 版必須配 S3 的問句紀律（帶查法、要求貼原句），否則會複製同一個失敗。」

## F3（major）兩識別字被當可互換搜尋鍵

引句：「本次案例會被抓到：被刪的是 `refreshPaywayCredentials()` 的呼叫，六個節點內文帶 `refreshPaywayCredentials` / `GetPaywayCredential` 字面。」

被刪行只含實際存在的 token；S1 無 call graph，不會從 refreshPaywayCredentials 推出其內部呼叫的 GetPaywayCredential。只寫 callee 名的節點不命中，回歸錨敘述不準。

## F4（major）【canary，不計入真 findings】排除 code 註解的理由被 spec 自己的事故實錄反證

引句：「code 註解不需納入——註解與被刪的 code 同檔，刪除 commit 的 diff 天然把它們一起帶走，不會殘留。」

## F5（major）「全域消失」未定義以 index／HEAD／working tree 哪個快照判定

引句：「抽取規則分兩檔：**全域消失＝高信心報、僅呼叫點消失＝低信心報**。」

staged 刪除＋working tree 未 staged 同名新定義→grep worktree 錯降信心；只 grep HEAD→看不到已 staged 新增。pre-commit 契約對象是 staged index（`scripts/hooks/pre-commit:36-40`），spec 未定。

## F6（major）`git diff -M` 只處理檔案 rename，實現不了「符號改名不誤報」

引句：「`git diff -M` 可偵測的 rename → 不報」

與 s2 席 finding 2 同題、獨立證據。測試列偷換符號改名與檔案改名。

## F7（minor）歷史段落的抑制標記無機械形式

引句：「架構圖**刻意保留**的歷史段落（已標「已作廢／歷史」的不該再報）。」

「標」的形式（status/標題/行內字樣/欄位）與區段起迄皆未定義；substring 會誤抑制「尚未作廢」。測試列無可實作 oracle。

## F8（major）alternation regex 未要求 literal escaping

引句：「grep vault 不做 N 個符號 N 次掃，合成單條 alternation regex 一次掃 254 檔」

與 s1 席 finding 4 同題、獨立證據。

## F9（major）S2 list 欄位漏 `plan_refs`/`core_refs`/`decision_refs` 等關係欄位

引句：「只動了 frontmatter 的 list 型欄位（`verified_by` / `related` / `tags`）。」

與 s1 finding 2、s2 finding 4 三席一致、各自獨立證據（cx 證據：`scripts/lumos:13252` impact --node 明列 verified_by/plan_refs typed links）。

## F10（minor）S2「只改 frontmatter」的 diff 邊界未定義

引句：「多改任一行 body → 判「有動內容」」

YAML 重排／縮排／scalar↔list 正規化／欄位增刪無裁定。

## F11（major）S3 的 `lumos search` 與 S1 的 grep 不是同一搜尋域

引句：「把那些名字逐個丟進 lumos search。」

`lumos search` 預設排除 code block（--code 才含）與 superseded 節點；S1 是字面 grep。同一識別字兩邊給出不同節點集，S3 當 S1 fallback 時漏查 code fence 內的過期範例。（編排者自核：`search --help` 明示「--code 含 code block(預設排除)」）

## F12（minor）天花板句把兩個承接者併寫

引句：「語意那半仍歸 AI 交叉審與 `[[關係層傳播守衛_計劃]]`」

關係層守衛自己明示只掃 frontmatter 可判；純語意矛盾只歸 AI。

## F13（minor）Swimm 三級「天然對應」措辭過度

引句：「我們的 S1＋S2 天然對應後兩級。」

S2 非自動修復級，類比推不出相同輸出契約。

## F14（major）`|| true` 吞錯誤，實現不了「超時 fail-open」

引句：「總預算目標 <1 秒，超時 fail-open 放行。」

無 timeout 常數／計時位置／終止方法；偵測器 hang 時 commit 無限等待（`scripts/hooks/pre-commit:48` Gate CC 的 `|| true` 無 timeout；macOS 不可假定 GNU timeout；零依賴家規）。

## F15（major）cap 與輸出上限皆無值，超限降級丟失全部證據

引句：「識別字數設 cap（超過視同 refactor 刷屏、直接降級成一行摘要提示）」

大型刪除正是 drift 風險最高處，只印一行摘要=不可處置；需 cap 值、排序、超限保留 top-N 高信心。

## F16（minor）「併發：無」與 append-only 誤報帳候選衝突

引句：「併發：無——hook 每次 commit 單進程、對 vault 只讀不寫，無共享資源競態。」

若誤報帳進 v1 runtime，兩 worktree 同時 append 即共享寫入；不進則應明文排除。

## F17（minor）風險類漏可攜性與輸入健壯性

引句：「**此功能碰到的風險類**：self-governance（守衛面）、效能（pre-commit 熱路徑）。逐類答：」

CJK/quoted path（`scripts/hooks/pre-commit:36-39` 踩過）、grep 方言、binary diff、不可信 token 拼 regex——涵蓋 s2 席 finding 5（該席評 major）。

## F18（major）測試缺 staged snapshot／partial stage 型

引句：「機械層（S1/S2）行為可枚舉，走 TDD」

index≠worktree、staged delete+unstaged re-add、partial hunk、initial commit、non-ASCII path、diff 標頭行——前三項屬合約級缺測。

## F19（major）效能測試混 workload 時限與 timeout fail-open 兩行為

引句：「254 檔 vault、20 識別字 → 單次 alternation grep，總時 <1s；超時 fail-open rc0」

固定小 fixture 觸發不了 timeout；應拆 benchmark 與注入 hang 兩測。

## F20（major）回歸 fixture 不在本 repo，無交付與去敏規格

引句：「回歸錨：mOrangePos aff2329 的 diff 作為 replay fixture」

本 repo 無該 commit；需 vendored 最小合成 diff＋合成筆記，不依賴外部 SHA。

## 無 finding 節（已讀宣告）

一句話／失守實錄／守衛之間（Gate 3 引文與現碼一致）／審計修正紀錄。交叉引用核對：見下方 S3、trade_offs、PRIOR-ART ②、[[關係層傳播守衛_計劃]]、[[cochange守衛_計劃]]、[[Systems/cochange-guard]]、Gate CC/1/2/3、impact --sync-check、lumos search、cochange check --staged、--no-verify 均存在。

## 總結

最嚴重 severity：**major**（真 findings 19 條：major 11、minor 8；canary 1 條另計）。無 blocker。
