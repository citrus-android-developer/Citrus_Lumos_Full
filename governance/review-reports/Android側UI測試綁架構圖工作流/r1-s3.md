# r1-s3(opus 整合與跨系統合約)
canary: type=c token=ui_flow_refs → **caught**(F2 全庫 0 命中 + cmd_contracts 不讀 frontmatter + 指出 lint 不擋未知鍵會長靜默假資料)
## F1(major)★platforms 是全域模式開關★—補了會讓 mOrangePos 既有 19 條 kotlin 綁定全懸空 引句:「專案 `.lumos/config.json` 補 `platforms` 宣告（`{maestro: {profile: "maestro", root: "."}}` 之類）」
load_platforms(:1875-1907)一看到該鍵即進 multiplatform、legacy test_profile 失效;resolve_test_refs 把無冒號 ref 歸 default_platform(此例=maestro)。實查 mOrangePos config 現為 kotlin-junit、架構圖有 19 條裸名綁定 → doctor 直接紅。spec 引用了 test-profile-multiplatform 卻沒帶到「平台前綴≠profile 名」與「default_platform 缺省即報錯」兩條。
## F2(major)【canary,不計】引句:「節點 frontmatter 的 `ui_flow_refs` 同步列出，可雙向對帳」
## F3(major)★綁 [test:] = E2E 變本機硬閘,與 test-layers 裁定正面衝突★ 引句:「這不是新紀律，是既有「**真跑 > 機械查 > LLM 判官 > 自報**」信任階梯在 UI 層的實例」
code-loop S1:diff 命中綁 [test:] 的星標節點→pass 前必跑該測試且須綠、「解析不了所以沒跑」不構成放行;而 test-layers軟提醒_計劃(done,spec 自己 related)明文「E2E 慢+flaky,本機硬擋逼出 --no-verify 文化反噬其他硬閘;真硬閘留 CI 合併點」。spec 未裁。且天花板的「取不到裝置→明記未驗」接不上 S1 的退路階梯(那是為指令解析失敗設計的)。
## F4(major)真裝置重放結帳 flow 產生真實單據,全份無環境/資料隔離規定 引句:「複製設定時**務必改裝置名**（否則兩台產生重複單號，直接踩既有事故）」
實查參考實作 smoke-01 檔頭註解記「驗過:2026-08-11 通過,單號 M092026081100001」=真的開了一張單。pitfalls 的 payment/prod-irreversible 兩類一類沒答。
## F5(major)[kill:] 第三階對 maestro 走不通,spec 宣稱「不另立機制」不成立 引句:「這是既有合約鏈三階（存在性 → `[audit:]` 夠格 → `[kill:]` 咬得住）的第三階在 UI 層的樣子，**不另立機制**」
guard kill 要 platforms.<plat>.run_cmd(缺即 ERROR);宣告 platforms 後 legacy test.run_cmd 失效;kill 用 worktree 改副本原始碼再跑,但裝置跑的是已安裝 APK,改 .kt 不影響被測物(除非 run_cmd 含 build+install,而換版本要移除重裝=設定全清)。驗收第四列是一次性人工動作,沒有 kill-add 配方=無回歸守衛。
## F6(major)第一缺口「時機」交付後仍沒關上 引句:「| **時機** | code-loop **終審**時 | **功能完成當下** |」
待辦 1-4 全不動觸發時機,第 5 條仍未決;軟提醒既有實作只掛 pre-push(非功能完成當下)。
## F7(major)示範 KEY 行兩處照抄就壞(a 續行隱形 b 測試名非識別字;與 s1 F2/s2 F2 同題)
## F8(major)[audit:] 第二階全份缺席,驗收判準寫窄 引句:「Check T 對綁了 `[test:maestro:*]` 的節點**不再報**裸合約／懸空 test_ref」
Check T 五判定含 unaudited;補綁定後只是從「裸合約」換成「未審」,doctor 照樣不綠。
## F9(major)缺實務隱患節;逐類代答:併發(裝置獨佔+S1必跑+kill 兩趟)/效能(root="." 讓 doctor os.walk 整個 Android repo)/資源(裝置狀態污染、換版本不可逆清設定)/認證(挑戰碼人工 vs 無人 loop)/PII(影片 log 進版控含帳號單據)/金流不可逆(見F4);可判不碰=快取/限流/遷移。
## F10(minor)「掃 .maestro/ 下」與現況不符—實際是整個 platform root os.walk,dirs 只被 scaffold 用;common-login.yaml 加 name 就會被當合法證據
## F11(minor)name: 行帶註解 NO MATCH(與 s2 F1 同題)
## F12(minor)「既有 flow 的 name: 改識別字」講錯—實查三支 flow 一個 name: 都沒有,是「加」不是「改」,且要加在 config 區塊
## F13(minor)name: 唯一性無約束(與 s2 F3 同題)
## F14(minor)證據路徑少 governance/ 前綴 引句:「證據仍存 `review-reports/<loop>/ui-evidence/`」
## F15(minor)delguard 歸因錯(與 s1 F3/s2 F5 同題,三席一致)
## F16(minor)缺口表 row3「只缺慣例層」與待辦 1(機制設定層)自相矛盾 引句:「⚠ 但**只缺慣例層**：`[test:`/Check T 機制層的 maestro 通道 2026-07-02 已交付」
max severity: major
