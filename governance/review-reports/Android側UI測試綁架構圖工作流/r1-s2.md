# r1-s2(sonnet 邊界與可執行性)
canary: type=d token=device-ready.json → **missed**(未觸及;送分題型漏抓=強警報。惟真 findings 品質高且與 s3 多處獨立重合,依 d4 觀測制不作廢)
## F1(major)示範 name: 行帶行尾註解會 NO MATCH 引句:「name: smoke_manual_discount_over_100   # ← [test:] 綁的是這個,必須是識別字」
MAESTRO_NAME_RE 行尾 `\s*$` 硬錨;maestro profile 未覆寫 comment_strip(預設 c-style 不剝 YAML #)。實測 findall → []。照抄示範=懸空,與驗收表第一列矛盾。
## F2(major)示範 KEY 續行寫法照抄會隱形 引句:「[test:maestro:smoke_manual_discount_over_100]」(第二行)
INVARIANT_RE 要求行首 KEY:★INVARIANT★;續行對 Check T/contracts/lint 全隱形。且 guard bind 實際會逗號併進同一行同一括號。
## F3(major)name: 重複無偵測 引句:「綁 flow 檔內的 **`name:` 欄位**；Check T 驗它存不存在」
discover_test_methods 回扁平 set 不記路徑,兩支 flow 同名照樣判「存在」。
## F4(major)name→file path 無解析路徑 引句:「以**檔案形式**跑 maestro test .maestro/<flow>.yaml」
maestro 只吃檔案路徑;Check T 不記路徑;S1 真跑紀律在 maestro 上無從組指令(dotnet --filter/pytest -k 有原生名字選取,maestro 沒有)。
## F5(minor)delguard 歸因錯(與 s1 F3 同題)
## F6(major)缺實務隱患節;併發(裝置被人機搶用→綠燈但畫面狀態已亂)/CI 無實機使 tier=high UI 條款恆退化成「未驗+原因」/真實單號序列被反覆重放污染。
max severity: major
