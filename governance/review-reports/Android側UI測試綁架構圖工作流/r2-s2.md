# r2-s2(opus 折入一致性與整合,delta 輪)
canary: type=c token=scan_budget_ms → **caught**(F4 全 repo grep 零命中 + 指出它同時違反本 spec「不造新機制」自我約束)
## F1(major)示範測試名中文(與 s1 F1 同題,獨立證據:另指出真實測試是反引號含空白中文名,KOTLIN_TEST_RE 抽的是原名→就算手改也懸空)
## F2(major)命名慣例是 r2 新立硬性規則,但同檔三處示範全違反它 引句:「v1 的解：**命名慣例硬性**——`name:` 必須等於檔名去副檔名並轉底線」
檔名轉底線應為 smoke_05_...,示範寫 smoke_manual_...(少 05);慣例存在目的就是機械反推,示範反推找不到檔。
## F3(major)JSON root 與 KEY/隱患節/待辦相反(與 s1 F2 同題)
## F4(major)【canary,不計】引句:「並在 config 加 `scan_budget_ms` 讓 doctor 超時自動降級」
## F5(major)待辦1「先綁非星標節點」機制上做不到 引句:「★此步先綁**非星標節點**示範接上★」
cmd_guard_bind 只在 INVARIANT_RE 行上找目標(:4495-4503),Check T 也只掃 ★INVARIANT★(:705-717);連帶驗收表兩列與 guard audit 都預設綁星標。
## F6(major)★d2 立論前提與實況不符★ 引句:「把 maestro flow 綁進 ★INVARIANT★，等於**每次動到那個合約就必須當場有一台備妥的裝置才能 push**」
S1 條文自標「紀律層規則非機械閘」(SKILL.md:227);pre-push 擋的是 tier=high 無 code-loop 留痕(hooks/pre-push:99-118),錯誤訊息直接給 `code-loop skip --note` 出路。d2 為解不存在的死鎖換來跨 skill 改動。
## F7(major)d2 要驗的「上次實跑留痕」全份未定義 引句:「終審改驗「flow 存在＋上次實跑留痕」」
非 pass --note、非 Check T 五判定之一、非既有 ui-evidence;唯一近似物是 flow 檔頭人寫註解=信任階梯最底層自報。
## F8(major)待辦4 與待辦3 全局約束/認證條打架 引句:「maestro MCP 的 `list_devices` → `inspect_screen` → `run`，與既有 Playwright 通道並列」
待辦4 沒帶前置,照字面做會在測試門店約定落地前於真裝置真後端開真單。
## F9(minor)19 條數字對不上(實跑 guard list:合約 5 條 — 真綁 5)
## F10(minor)multiplatform 鍵不存在(與 s1 F4 同題)
## F11(minor)governance 前綴只折本文,frontmatter 留舊版;且被引節點 pitfalls-code-loop:17 自身才是該修的
## F12(minor)「步驟區加不算」方向反了—Check T 不看 --- 分界,放步驟區照樣綠但 flow 壞
## F13(minor)d1 範圍宣告與 d2 動作不相容(d2 要動治理條文,d1/PRIOR-ART 說不動治理層)
max severity: major
