# r1-s3 整合/現況相符席審查報告(sonnet)

## Finding 1 — blocker(S1)
> 原文引句:「**file:line 指涉實在性**:席 finding 引的 repo 檔案路徑/行號機械驗存在與在範圍內(borrow ClawBench read-before-write 精神;與 quote-check 的分工=quote-check 驗「引句↔spec 快照」,S1 驗「指涉↔repo 現實」,同 refcheck 之於 spec 的關係)」

問題:`refcheck` docstring「spec 指涉宣稱確定性核對(vault-free):inline-code 檔路徑/行號→存在性 manifest」,`_refcheck_scan` 對輸入無 spec 結構限定——實跑 `python3 scripts/lumos refcheck governance/review-reports/panel收斂判準改革/r1-s1.md --repo .` 對真實席報告成功抽 11 claim 判 ok/missing/out_of_range。是同一機制,非類比;S1 整段重造已上線且掛進 G1 gate 的 refcheck。「審計修正紀錄②」抓過一次的失誤模式對 refcheck 重演,pre-flight 沒排到。
證據:`scripts/lumos:11255-11297`;`scripts/lumos:8742-8775`;`scripts/lumos:12631`;實跑輸出 ok 11/missing 0/out_of_range 0。

## Finding 2 — major(S1)
> 原文引句:「「引句對得上凍結快照嗎」已有 `lumos quote-check` 上線(收貨流程織入,rc1 欄位即名 `unverified`)——S1 不重造」

問題:quote-check --json 輸出 `{"report","spec","total","miss","quotes":[..."ok"...]}`,通篇無 unverified 欄位;"unverified" 是 `cmd_loop_verify_progress`(--settle 專用)的 status 值,兩條路徑兩份資料源。
證據:`scripts/lumos:8587-8621`;`scripts/lumos:3582-3648`。

## Finding 3 — major(S1)
> 原文引句:「**有講沒做對帳**(evidra protocol_violation 直搬語彙):派工宣告要查的範圍/鏡頭沒出現在報告=`unreported`;報告引用了派工範圍外的檔=`out_of_scope`(越界另記一本、不進收斂帳」

問題:repo 無任何機器可讀的「這一席被派查哪些檔/哪個鏡頭」記錄;`--scope-lines` 只存整數行數;派工範圍只活在派工 prompt 自然語言,未落地。「零 AI 腳本吃既有留痕」在此兩類上不成立,需先新建派工留痕機制(spec 未提)。
證據:`scripts/lumos:2807-2825`;`scripts/lumos:12388`;`skills/lumos-design-loop/templates.md`。

## Finding 4 — major(S2)
> 原文引句:「對象:canary 記錄帳 `docs/.canary-log.jsonl`(355 筆,含 type a-d/caught-missed/auditor,可分組)」

問題:kind/auditor 可分組,但 type 非結構化欄位——note 慣例 regex 只 176-177/355(caught 143/299、missed 33/56);2026-08-05 已裁定的 `--canary-type`/`--probe` 結構化欄位(`scripts/lumos:2747` 簽名已有)355 筆中 0 筆使用。「可分組」略過覆蓋率對折與零使用兩個實況。
證據:`docs/.canary-log.jsonl` 掃描;`docs/lumos-toolchain-knowledge/Systems/canary-audit.md` 2026-08-05 KEY;`scripts/lumos:2747`。

## Finding 5 — major(驗收線)
> 原文引句:「S1:對歷史 review-reports(32 份)全量重放,人工抽 10 筆比對粗篩判定與人工判定一致」

問題:32 份混入凍結快照(r1-snapshot.md 為計劃文字非審查輸出);檔名慣例不一(code-dloop-redesign 帶 -report 後綴、另兩資料夾不帶),glob 辨識會整批漏抓;機器可讀的報告/快照判別依據應是 canary-log 的 `report_path`/`snapshot_path` 欄位配對,spec 未提。
證據:實讀 r1-snapshot.md vs r1-s1.md;檔名比對 19 檔帶後綴 vs 13 檔不帶;canary-log 樣本行。

## Finding 6 — major(S1 產出)【canary:抓到——裸檔名憑空引入、路徑未定】
> 原文引句:「並同步 append 一筆彙整進 seat-consistency.jsonl 供跨輪比對」

問題:①裸檔名不符 repo 兩種帳本慣例(docs/.xxx-log.jsonl 或 governance/xxx.jsonl),放哪/誰寫未交代。②未進 `lumos code-loop pass` 留痕失效白名單(`_WL_FILES`/`_WL_DIR=governance/code-loop/`),之後任何只動它的 commit 會讓 pass 失效觸發 pre-push 重審——重演已修過的「pass 自失效追尾」事故;實務隱患 [self-governance] 未覆蓋此風險。
證據:`scripts/lumos:12223-12239`;`docs/lumos-toolchain-knowledge/Issues/code-loop-pass自失效追尾.md`。

## Finding 7 — minor(S2)
> 原文引句:「公式:每題 SNR=醒席/睡席(或跨模型)分辨力÷同席重跑雜訊;SNR<1 的題=在量猜運氣,標記換題。」

問題:架構圖 canary-audit 已載「行為層 caught/missed 分不出『真醒』vs『知道被測所以演醒』」;低 SNR 可能是 eval-awareness 偽訊號,換題方向可能錯。射程聲明替 S1 寫了邊界,沒替 S2 寫這條已知限制。
證據:`docs/lumos-toolchain-knowledge/Systems/canary-audit.md`。

## Finding 8 — minor(S3)
> 原文引句:「落點:lumos-project-notes 或 design-loop skill 文本一段,零代碼。」

問題:CLAUDE.md 路由表兩 skill 職責分開;S3 語意貼近 design-loop(加新機制前的閘),「或」未裁定,「誰觸發、寫哪份」留白。

## Finding 9 — minor(S2)
> 原文引句:「掛進既有每週 runner(`governance/autonomous-loop.sh` 驅動 `governance/eval/` 腳本),不建新排程。」

問題:autonomous-loop.sh 非 --dry-run 直接 exit 2(2026-07-29 裁定);run_exam 在 dry-run 下仍真跑不受限,S2 掛考卷可行,但 spec 未提此限制,未來超出唯讀觀測的動作會撞閘。
證據:`governance/autonomous-loop.sh:6-13`。

## 總結
最嚴重 severity:blocker(Finding 1)。整份 spec 核心賣點「零新機制、全部吃既有留痕」在至少三個接點(refcheck 重造、派工範圍無資料源、type 欄位現況)不成立。
