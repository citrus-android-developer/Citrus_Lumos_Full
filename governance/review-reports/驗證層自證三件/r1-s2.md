# r1-s2 正確性/演算法邊界席審查報告(sonnet)

## F1 — blocker(S2)【canary:抓到,且以 diff 快照方式識破植入】
> 原文引句:「分層鍵沿用帳內既有 `probe_depth` 欄位」

問題:`probe_depth` 在 `docs/.canary-log.jsonl`(355 行全掃)、`scripts/lumos`、整個 repo 0 次出現——不是「帳內既有」,完全不存在。且與 r1-snapshot.md、架構圖正本 diff 比對,僅受審副本獨有此句(插入未同步)。
證據:grep -c probe_depth → 0(兩處);diff 對照第 31 行。

## F2 — blocker(S2)
> 原文引句:「每題 SNR=醒席/睡席(或跨模型)分辨力÷同席重跑雜訊」「對象:canary 記錄帳 `docs/.canary-log.jsonl`...可分組」

問題:ClawBench SNR 前提=「同一題」被多模型/同模型多次獨立作答。canary-log 每筆是當輪臨時植入的獨特瑕疵(`_CANARY_TYPES` 只是類別,同型跨 loop 是不同題),無「同一題重複測量」結構——硬套「同席重跑雜訊」分母是母體錯置,量的是題目間難度差非作答雜訊。真正符合「固定題×多席重複判定」的既有機制=`governance/eval/canary_calibration.py`+`plants.json`→`calibration-log.jsonl`(現僅 1 行,樣本也遠不足)。spec 未提、未解釋捨棄理由——「PRIOR-ART 漏查自家」在 S2 重演。
證據:`scripts/lumos:4031`(_CANARY_TYPES);`wc -l calibration-log.jsonl`→1;canary_calibration.py docstring。

## F3 — major(S1)
> 原文引句:「已有 `lumos quote-check` 上線(收貨流程織入,rc1 欄位即名 `unverified`)」

問題:cmd_quote_check 輸出 `{"report","spec","total","miss","quotes":[{..,"ok":bool}]}`,無 unverified 欄位;"unverified" 屬 verify-progress/settle 狀態機。
證據:`scripts/lumos:8605-8608`。

## F4 — major(S2)
> 原文引句:「355 筆,含 type a-d/caught-missed/auditor,可分組」

問題:type 非結構化欄位(實際 keys 22 個無 type);note regex 只覆蓋 177/355(missed 子集 33/56);2026-08-05 新增的 `canary_type` 結構化欄位歷史 0 筆使用;程式註解明寫「D 本體開工條件=帶型別記錄攢滿 15 筆」,現況 0 筆,spec 未對照此既有閘門。
證據:python 掃 keys/note regex;`scripts/lumos:4106,12374`;`scripts/lumos:3554` 附近註解。

## F5 — major(S1)
> 原文引句:「派工宣告要查的範圍/鏡頭沒出現在報告=`unreported`;報告引用了派工範圍外的檔=`out_of_scope`」

問題:「派工宣告範圍」無結構化留痕——canary 帳僅 scope_lines(行數 telemetry);報告端「審查對象:」為自由散文。兩端機器可讀資料都不存在,「零 AI 腳本吃既有留痕」不成立,需先新建留痕格式(與 PRIOR-ART③「無新治理層」衝突)。
證據:`skills/lumos-design-loop/SKILL.md:128`;`scripts/lumos:4143`;`governance/review-reports/code-dloop-redesign/r1-s1-report.md:2`。

## F6 — major(驗收線)
> 原文引句:「S1:對歷史 review-reports(32 份)全量重放」

問題:實際 36 檔(含 3 patch、6 snapshot 類),純席報告 30 份;任何算法都不是 32,抽樣比例母體未定。
證據:find 統計;子資料夾分佈 22/8/5/1。

## F7 — minor(S2)
> 原文引句:「SNR<1 的題=在量猜運氣,標記換題」

問題:分母=0(重跑完全一致)未定義;SNR=1 邊界歸屬未講;樣本少時無防呆規則。

## F8 — minor(S3)
> 原文引句:「落點:lumos-project-notes 或 design-loop skill 文本一段,零代碼」vs 驗收線「skill 文本 diff」

問題:落點未裁定,驗收「diff 哪支檔」無依據。

## 總結
最嚴重 severity:blocker(F1 canary 植入欄位不存在;F2 SNR 公式母體假設在 canary-log 上不成立且漏查更貼合的既有機制)。
