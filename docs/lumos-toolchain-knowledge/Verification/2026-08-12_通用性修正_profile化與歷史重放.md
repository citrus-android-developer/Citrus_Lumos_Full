---
type: verification
status: pass
date: 2026-08-12
valid_under:
  - "symbol_profile 目前有 csharp/kotlin/python 三個;其他語言棧需增補"
  - "neg_lexicon 目前有 zh/en 兩個"
revalidate_when:
  - "新增語言棧 profile"
  - "有專案回報 drift-history 曲線與實際感受不符"
tags:
  - type/verification
  - status/pass
  - scope/cli-read
summary: |-
  TEST:Check Y profile 化 10 條 + drift-history 3 條;全套綠
  VERIFY:★歷史重放證明 drift 是穩態不是快照★——兩條幽靈符號橫跨全部取樣點、跨 21→30 篇架構圖成長期都沒被修掉
  KEY:★本次最重要的產出不是程式碼,是「別相信我在別份架構圖上的結論,自己量」這個做法★
---

# 驗證：通用性修正（profile 化 + 歷史重放）

## 起因：一個正確的質疑

> 「我擔心的是你只對這份架構圖適用，那新的架構圖如果用別的闡述方式，會不會就抓不到語義識別？」

**質疑成立。** 誠實分級：

| Check | 靠什麼 | 通用性 |
|---|---|---|
| N | 使用者自己宣告的語法約定 | ✅ 完全通用 |
| Y | 命名慣例 ＋ 否定詞表 | ⚠ **本次已 profile 化** |
| U | 三張中文詞表 | ❌ **本次降級定位，不宣稱通用** |

## 做了三件事

1. **Check Y 兩層 profile 化**——`SYMBOL_PROFILES`（csharp/kotlin/python）＋ `NEG_LEXICONS`（zh/en），
   沿用既有 `TEST_PROFILES` 的形狀（未知名稱出聲警告並回預設，★不靜默★）。
   測試釘住：python profile 下 snake_case 生效／`neg_extra` 可增補／未知 profile 出聲。
2. **新增 `lumos drift-history`**——沿 git 歷史重放，讓每份架構圖自己量規律。
3. **Check U 降級定位**——明說它是「中文寫作風格的啟發式」，要移植先跑 `drift-history` 量。

## 歷史重放：規律成立

`GetOrdersForRedeemAsync` / `ListAvailableAsync` **橫跨全部取樣點**，
2026-05-26 → 2026-07-15 期間架構圖從 23 篇長到 27 篇、候選符號 108 → 149，**一次都沒被修掉**。

> ★這證明它是穩定可重現的規律，不是對一份快照過擬合。★

## ★寫這個工具時，我自己犯了兩次它要防的錯★

### ① 表面形狀的假設，錯得無聲無息

首版重放腳本用 `.endswith(".md")` 過濾 `git ls-tree`——git 對非 ASCII 檔名**加引號跳脫**，
25/30 個中文檔名節點被濾掉，**結論從「2%、持續三個月」變成「0%、完全沒問題」**。

### ② 取樣點與要量的問題同形狀

首版只取「動過架構圖的 commit」。但 drift 的誕生機制正是「**只改 code、沒改架構圖**」——
在架構圖 commit 取樣就系統性錯過出生那刻。**是測試翻紅才發現的**。

> 這兩次都不是粗心，是**同一類認知盲區**：把「我看得到的」當成「全部」。
> 而這正是架構圖 drift 的心理機制——**寫的人以為自己寫的是現況**。

## 相關

- [[Systems/drift-history]]
- [[Systems/check-y-symbol-existence]]
- [[Systems/check-u-overgeneralization]]
