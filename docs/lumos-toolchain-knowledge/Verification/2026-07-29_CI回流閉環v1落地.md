---
type: verification
status: pass
date: 2026-07-29
feature: ci-feedback-loop-v1
valid_under:
  - "GitHub Actions＋gh CLI（用原生 --commit 過濾，非 client-side sha 比對）"
  - "v1 範圍＝direct flow；PR/ship/auto-merge 未實作（移 v2）"
revalidate_when:
  - "gh CLI 的 run list/view JSON 欄位或旗標變更 → fixture 與判定重驗"
  - "開啟 PR 流程 → v2 計劃另審（本驗證不涵蓋）"
plan_refs:
  - "[[Projects/CI回流閉環_計劃]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:t_ci_wait/t_ci_status_and_gov/t_ci_hooks 共 28 檢查全綠;全套 1662 passed 0 failed;doctor 0 issues
  VERIFY:[[Projects/CI回流閉環_計劃]] v1(direct only)落地——ci-wait/ci-status/ci-log 帳/gov 第7源/SessionStart hook/repo 根 gitignore
  KEY:總開關=未宣告 .lumos/config.json 的 ci 區塊即功能全關(不等/不提醒/gov 不 load/config 壞損也退全關)——零侵入的唯一定義,五處消費路徑一致
  KEY:假綠三防——①用 gh 原生 --commit 過濾(client-side 比對會被預設 limit 20 擠掉)②空集合不算「全部完成」+no-run 快速路徑(本 repo 在 feature 分支 push 零 run,原設計會白等 600s)③判綠前 grace 期重查(晚註冊 run 緩解,明文非保證)
  KEY:紅燈證據——失敗步驟取自 run view --json jobs(非 log 文字解析,多個以 ; 串接)+log 尾 40 行再截 4000 字;任一紅即 rc1 早退(不等其餘跑完)
  KEY:hook 生命週期對稱——新 hook 同時進 HOOK_ENTRIES(SessionStart)與 _GLOBAL_CLAUDE_HOOKS 白名單(缺後者=註冊指向不存在的檔、下次 init 被 _prune_dangling 剪掉;沿 [[Issues/hook卸載殘留註冊]] 通則)
---
# Verification: CI 回流閉環 v1 落地（2026-07-29）

驗證對象：[[Projects/CI回流閉環_計劃]] 的 v1 範圍——「push 後同輪知道 CI 紅了並當輪修」的機械面。

## 落地內容

| 條款 | 實作 | 測試 |
|---|---|---|
| [S1] `ci-wait` | 輪詢 `gh run list --commit <sha>`；綠 rc0／紅 rc1（含失敗步驟＋log 尾段）／no-run·逾時·工具缺席 rc0／帳寫失敗·參數錯 rc2；聚合任一紅早退 | `t_ci_wait` 18 檢查 |
| [S1b] 總開關 | `.lumos/config.json` 的 `ci` 區塊；未宣告＝全關；`workflow` 可指定單支或陣列（逐支查後合併） | 同上（含 disabled 路徑） |
| [S3] 帳＋`ci-status` | `docs/.ci-log.jsonl`（去重鍵 `run_id:attempt:conclusion`，含 attempt 才不吞重跑）；`ci-status` 唯讀不打網路＋>24h 過期註記 | `t_ci_status_and_gov` 6 檢查 |
| [S2b] 後備網 | SessionStart hook（新檔＋雙處註冊）；`lumos gov` 第 7 源（含總開關） | `t_ci_hooks` 8 檢查 |
| [S5] 文件 | README 指令參考＋§8 七帳、三處命令數 49→51、repo 根 `.gitignore`、cochange exclude | `t_docs_command_count` |

## 過程中被機械守衛咬住兩次（正面案例）

1. **命令數漂移守衛**：新增兩個頂層命令後，`t_docs_command_count` 立刻在三份文件上翻紅——這正是它昨天被加進來的目的，且 spec [S5] 已預先寫進連動。
2. **假 gh fixture 的跳脫壞掉**：測試自身的 stub 產生了語法錯的 Python，導致所有 `ci-wait` 案例都走 fail-open；靠「verdict 應為 green 卻是 unavailable」的斷言抓出來——若當初只斷言 rc0，這個壞 fixture 會靜默通過（rc0 正是 fail-open 的回值）。**測試要斷言語意不是只斷言 rc**。

## 順手硬化

`ci-wait` 的 git 呼叫改 fail-open（PATH 缺 git 時原本會 traceback）——由「gh 缺席」測試逼出來。

## 文件同步與新守衛（2026-07-29，使用者指出「為什麼這些都還要我講」）

當天有守衛的（指令數）一改就被咬住並當場修好；沒守衛的三處全漏、由使用者提醒才補：
kill verdict 值域（reference.md 仍寫「六態/timed_out 算接住」）、ARCHITECTURE 強制力管線（缺回流段）、兩支 skill 的 push 後紀律。

**根因不是疏忽是結構**：同一事實散在多處、只有一處有機械守衛；文件越長漂移面越大（呼應 [[Projects/上下文瘦身_計劃]]）。
**修法＝把守衛面推廣**：新增 `t_docs_enumeration_drift`，把「禁止手寫數字／列舉」從指令數擴到三類機械可推導事實——
①治理帳數（文件宣稱 vs `cmd_gov` 實際 load 數）②kill verdict 值域（碼寫得出的 verdict 是否都列在 reference.md）
③hook 生命週期對稱（`HOOK_ENTRIES` 註冊 ⟺ `_GLOBAL_CLAUDE_HOOKS` 複製白名單 ⟺ 檔案存在）。
守衛已做 mutation 驗證（拿掉一個 verdict → 翻紅；還原 → 綠），非稻草人。

**通則入帳**：凡文件宣稱一個「可從碼機械推導的清單或數字」，就該有一條這樣的測試——否則它遲早漂移，而且是靠人發現。

## 誠實天花板

- **晚註冊 run 的 grace 期是緩解非保證**：無法證明沒有更晚註冊的 workflow。
- **修復重試上限／flaky 判別／Issue 產出屬 skill 紀律層**，工具端只出 rc1 訊號與失敗證據，不機械強制。
- **v1 不含 PR 流程**：紅燈仍會短暫存在於 main（靠當輪修補回），要「紅燈進不了 main」需 v2。

## 外審 round3 補正（2026-07-29 晚，第二輪 Codex 實查後）

**上文兩處宣稱需就地更正**（原文保留，供對照）：
1. 表格 [S1] 寫「紅 rc1（含失敗步驟＋log 尾段）」——**當時只在 `--json` 成立**，預設文字輸出不印。已修（見下）。
2. 「假綠三防」只防到三種紅色 conclusion；**非紅非綠的 `cancelled`／`action_required`／`stale` 仍被判綠**，
   與本 feature 計劃節點 [S1] 自列的三分類矩陣不符。已修。

**本輪新增測項**：`t_ci_wait` +13（四種非成功 conclusion × rc/verdict、一綠一 cancelled 混合、
紅燈文字輸出三條、綠燈無噪音一條）、`t_ci_status_and_gov` +3（同 sha 多筆取最壞／全綠仍綠／cancelled 不報綠）。
兩處修法都做過 mutation 驗證：退回舊行為即分別翻紅 5 條與 1 條，非稻草人。

**守衛面同時擴大**（`t_docs_command_count` / `t_docs_enumeration_drift`）：
- 命令數真值改由 `lumos --help` 的 choices 取得，**不再對原始碼做 regex 近似**——舊 regex 漏掉迴圈註冊的
  `links`/`backlinks`，把真數 53 算成 51，於是三份文件寫「51」反而全綠：**守衛的尺自己在漂**。
- 掃描對象由手維護的三檔清單改為**全部活文件**（排除外審歸檔與 `governance/golden/` 這兩類不得回改的歷史）。
  首跑即抓出 `AGENTS.md`(44)、`skills/lumos-project-notes/reference.md`(49) 兩處長期漂移。
- kill verdict 值域的抽取改為「取 `verdict = <右式>` 整行的所有字面量」——舊 regex 遇三元式
  `verdict = "killed" if ok else "killed_unattributed"` 只抓得到前者，**`killed_unattributed` 整個從值域裡蒸發**。
- 值域守衛範圍加上 `Systems/guard-kill.md`（架構圖節點本身），態數宣稱也改為從碼推導；同時清掉 code 裡
  `icon` map 殘留的死鍵 `timed_out`（六態時代遺物，且掛 ✓ 會讓人誤以為逾時算接住）。
- hook 生命週期由單向包含改為**雙向等價**：舊斷言只驗「註冊的都有複製」，「複製了卻沒註冊」的幽靈 hook 照樣過。

**通則（第二次入帳，比上次更強）**：上次寫的是「文件宣稱的機械事實要有守衛」。這次要補一句——
**守衛用來當真值的那把尺，本身也要能被證明沒漂**。優先順序：直接問程式（`--help`、實際執行）＞
對原始碼做 regex 近似；後者是「看起來像機械驗證」的假機械。
