---
type: verification
status: stale
feature: guard kill 殺傷力驗證(kill-add/kill 六態+worktree 隔離+run_cmd command-runner)
commit: (見本檔所在 commit)
date: 2026-07-10
valid_under:
  - "python3 stdlib;git worktree 支援(git 2.x);run_cmd 由專案 config 宣告"
  - "六態判定+rc 語意如 Systems/guard-kill KEY 行"
  - "worktree 於系統 temp、HEAD 基準、timeout=baseline×5 下限 20s(env 可覆寫)"
revalidate_when:
  - "六態/rc 語意變更或 hydration(v2)落地"
  - "run_cmd schema 變更(platforms.X.run_cmd/test.run_cmd)"
  - "Landmark C# 端到端驗收完成時(補真機段)"
plan_refs:
  - "[[Projects/guard殺傷力驗證_計劃]]"
---
# 驗證：guard kill 殺傷力驗證

## 測試項目（t_guard_kill，21 斷言全綠；全套 923 綠）

| 項目 | 結果 |
|------|------|
| kill-add 寫後自驗+KEY 標記+重複拒絕+naked rc2 | ✅ |
| killed happy path(壞法讓測試翻紅)+JSON manifest | ✅ |
| survived(稻草人測試全綠)→ rc1 | ✅ |
| drifted(old 漂移)/abort(baseline 紅)/error(圍欄擋逃逸)→ rc2 | ✅ |
| timed_out(壞法致掛,floor 覆寫)→ 歸 killed 類 rc0 | ✅ |
| 缺 run_cmd rc2;路徑圍欄擋 ../;worktree 零殘留;dirty 警告 | ✅ |
| 留痕 docs/.kill-log.jsonl + lumos gov 第 5 支 load 撈得到 | ✅ |
| 文件同步:SKILL/graph-discipline/CLAUDE 注入區塊+t_marker_doc_sync 加 [kill: | ✅ |

## 真機驗收（Landmark，跨 repo/跨語言 C# xunit）

2026-07-10：對 Landmark（獨立 C# repo、真 DB）的 `Systems/POS-API` 一條 opus-audited ★INVARIANT★「HMAC 驗章只認相同 canonical 元件+正確 secret」跑 kill：
- 業務壞法配方：`ValidateSignature` 短路成 `return true`（驗章形同虛設，任何竄改放行）。
- worktree 隔離、冷 `dotnet test --filter FullyQualifiedName~HmacSignatureRejectsTampering` baseline 綠 → 套壞法 → **verdict=killed**（測試真的翻紅）。證明該合約的綁定測試不是稻草人。
- 另驗 drifted 態：早期配方 old 在 canonical builder 命中 2 次（DRY 味）→ 正確報 drifted（漂移）。
- `lumos gov` 撈得到兩筆 kill 留痕（第 5 支 load 跨 repo 生效）。
- 驗收產物未留進 Landmark 分支（config/配方待 lumos 本體 push 後走正式 update 再宣告）。

## 2026-07-29 標記 stale（本檔自己的 revalidate_when 命中）

`revalidate_when` 第一條寫「六態/rc 語意變更 → 重驗」，而 oracle 品質包已把 verdict 由六態改為**七態**、
rc 優先序也改（`timed_out_weak` 不再算 killed、全弱證據 rc1）。因此 `valid_under` 的
「六態判定+rc 語意如 Systems/guard-kill KEY 行」**已不成立**，本驗證降 `status: stale`。

外審實錘（2026-07-29）：這一篇連同 [[Systems/guard-kill]] 一起留著舊六態說法，是「架構圖錯、code 對」的活例；
新語意的驗證見 [[Verification/2026-07-29_oracle品質包落地]]，本檔僅存為歷史。

## 相關模組

- [[Systems/guard-kill]]
