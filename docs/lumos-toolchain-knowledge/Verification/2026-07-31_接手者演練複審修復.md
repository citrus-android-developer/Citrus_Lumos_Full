---
type: verification
status: pass
date: 2026-07-31
feature: 接手者演練複審發現 reference.md:18 警告句仍矛盾(前次全域取代連警告句本身都被改到)+ doctor/lint 揭露段「這三支未交付」漏第 4 支 signoff——修文案、補測試白名單,並在可拋棄 Landmark clone 上實跑驗證揭露段屬實
valid_under:
  - "以 2026-07-31 當下 slim/README.md、slim/skills/lumos-project-notes/reference.md、scripts/test_lumos.py 的內容為準;三檔文案若再被其他全域取代動到,結論需重驗"
  - "Landmark 演練的『vendored 完整版 50 支』是 /Users/enzo/backend/LandmarkMember 當下 vendored 的 scripts/lumos(commit cb9f5640)快照量測值,與本 repo 現版 53 支不同(舊快照少 3 支新指令);Landmark 更新 vendored 版本或本 repo 再增減指令都會改變這兩個數字,不影響『vendored 完整版遠超精簡版 24 支』這個結論本身"
revalidate_when:
  - "reference.md/README.md 的『未交付指令』揭露段再增減項目 → 需同步重跑 t_slim_readme_assertions 白名單(reviewed_exception 集合)"
  - "scripts/lumos 的 check_regen_provenance() 若改走其他升級指令名(不再是 signoff) → 揭露段與白名單需同步改"
  - "本 repo scripts/lumos 或 Landmark vendored 版本的頂層指令數再變動 → 重跑 ④ 危險路徑那步,確認『遠超 24 支且含未交付指令』的結論仍成立"
tags:
  - type/verification
  - status/pass
plan_refs:
  - "[[Projects/公開精簡版_實作計畫]]"
summary: |-
  TEST:`python3 scripts/test_lumos.py -k slim` 78 checks 全綠(無新增測試函式,對既有 `t_slim_readme_assertions` 的白名單斷言內容做修正)
  VERIFY:[[Projects/公開精簡版_實作計畫]] 交付通過終審修復([[Verification/2026-07-31_公開精簡版終審修復]])後,複審再挑出兩處殘留問題並在可拋棄 Landmark clone(`git clone /Users/enzo/backend/LandmarkMember /tmp/lm-rehearsal`,演練完 `rm -rf`,原 repo 全程未寫入)上實測驗證修復後的揭露段屬實
  KEY:①reference.md:18 前次「37 處 `python3 scripts/lumos` 全域取代成 `lumos`」把警告句本身也改壞,變成「下文一律用全域 `lumos` 前綴。★不要用 `lumos`★」自相矛盾——改回指名真正危險的前綴 `python3 scripts/lumos`,語意對齊 vendored 完整版情境;全檔 grep 確認其餘 36 處與 slim/README.md 皆已正確轉全域 `lumos`,無其他誤傷
  KEY:②doctor/lint 揭露段原寫「`lumos init`/`lumos update`/`lumos self-audit`——這三支未交付」,複審在 `scripts/lumos:1999-2000`(`check_regen_provenance()`,lint 與 doctor 共用)找到第 4 支:regen 節點缺 `[src:]`/`[git:]` 證據時建議「走 signoff 升級」,`signoff` 同樣未交付。README 與 reference.md 兩處揭露段皆從「這三支」改成不寫死數量、判準改成「打了得到未知指令錯誤」;連帶把 `t_slim_readme_assertions` 的懸空引用白名單從 3 項擴成 4 項(`signoff` 會被 `slim-scan.py --python` 掃成新候選,已實測驗證加入前後 candidates 數從 3 變 4)
  KEY:④ Landmark 演練逐項證實揭露段屬實——(1) 正常路徑:`lumos search/context/contracts/doctor` 對 `docs/landmark-knowledge/` 真實架構圖跑得動、輸出合理(POS-API 節點正常列出 4 條硬合約);(2) 危險路徑:同目錄 `python3 scripts/lumos --help` 真的叫出 vendored 完整版(50 支,含 init/update/self-audit/signoff/bootstrap/canary 等一票精簡版沒有的指令),不是精簡版 24 支;(3) C1 實境:刪 CLAUDE.md sentinel END 行後 `lumos doctor` 真的印「跑 lumos init/update 修復」,實測 `lumos init`/`lumos update` 皆 rc=2 argparse invalid choice——「打不動」是真的,揭露段沒有唬讀者;(4)(5) 反誤傷:全程只有故意弄壞的 CLAUDE.md 有變動,還原後 clone 與原 repo 的 `git status --porcelain` 皆與演練前一致
---
# 2026-07-31_接手者演練複審修復

驗證對象:[[Projects/公開精簡版_實作計畫]] 交付通過終審修復([[Verification/2026-07-31_公開精簡版終審修復]])後的複審——兩處文案缺陷修復 + 在可拋棄 Landmark clone 上做接手者實境演練。完整演練逐項輸出見 `.superpowers/sdd/公開精簡版_實作計畫/landmark-rehearsal.md`(SDD 產出,gitignored,依計畫慣例落地於此)。

## 修復摘要

| 項目 | 問題 | 修復 |
|---|---|---|
| ① reference.md:18 | 前次全域取代把警告句本身也改壞,「不要用 `lumos`」自相矛盾、危險前綴無指涉對象 | 改回指名 `python3 scripts/lumos`,全檔複查無其他誤傷 |
| ② 「這三支」過度宣稱 | README/reference.md 揭露段漏列 `signoff`(第 4 支未交付、`check_regen_provenance()` 會建議) | 改成不寫死數量的講法,列出已知四支;連帶修 `t_slim_readme_assertions` 白名單 3→4 項 |
| ④ Landmark 演練 | 驗證①②揭露段是否屬實 | 可拋棄 clone 上實跑五項,全部與揭露段描述吻合;原 repo 與 clone 均無誤傷殘留 |

跑法:`python3 scripts/test_lumos.py -k slim`(78 checks,全綠;★不要跑完整套件★)。

## 相關

- 前一篇(本次複審針對的基線):[[Verification/2026-07-31_公開精簡版終審修復]]
- 系統筆記:[[Systems/slim-readme]]、[[Systems/slim-skill-修剪]](本次改動的兩份文件所屬節點)
