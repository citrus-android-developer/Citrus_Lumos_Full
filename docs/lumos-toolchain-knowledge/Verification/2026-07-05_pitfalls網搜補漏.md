---
type: verification
status: pass
created: 2026-07-05
updated: 2026-07-05
plan_refs:
  - "[[pitfalls網搜補漏_計劃]]"
related:
  - "[[pitfalls網搜補漏_計劃]]"
  - "[[linter-gap實務隱患]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:lumos-pitfalls-gapfill skill 落地 + dogfood 真 stack(lumos python)走通全流程——讀節點→WebSearch→反證預篩→駁倒→進〈已評估駁回〉去重;證 workflow 可跑、refute-prescreen 濾掉已處理坑
  VERIFY:block ③ 網搜補漏落地(skill + 純架構圖、無 lumos 新碼);跳 design-loop(純散文 skill 空轉)、驗收走 dogfood
  KEY:dogfood 唯一候選(isinstance bool 穿 int)被反證預篩駁倒——反證者 grep 證 lumos 唯二 config int 守衛皆已加 not isinstance bool、無未修實例;〈已採納〉本輪空(無 survivor)、人放行→已採納 路徑本次未觸發(honest)
---
# 2026-07-05 pitfalls 網搜補漏(block ③)驗證

`lumos-pitfalls-gapfill` skill 落地並 dogfood 真 stack 走通。落地 [[pitfalls網搜補漏_計劃]](解 [[pitfalls-lint-integration_計劃]] ③)。

## 交付
- **skill**:`skills/lumos-pitfalls-gapfill/SKILL.md`(on-demand,symlink 進 ~/.claude/skills)——WebSearch 找 linter 未收錄新坑→反證預篩(駁倒即丟)→駁不倒候選→人輕量放行→進架構圖。**無 lumos 新碼**。
- **架構圖落點**:[[linter-gap實務隱患]](每專案一個,兩段〈已採納〉〈已評估駁回〉,節點自去重)。

## dogfood(真 stack = lumos-toolchain / python)走通全流程
1. **建/讀節點**:`lumos new issue linter-gap實務隱患`(兩段結構)。✓
2. **WebSearch**:搜 python linter 未收錄 gotcha → 濾掉 linter 已抓的(mutable default=pylint 抓、subprocess check=W1510 抓)→ 留候選「`isinstance(x,int)` 誤收 bool」(config 驗證 trap,ruff/pylint 一般不 flag)。✓
3. **反證預篩**:派乾淨 refuter(default-refute+要 file:line)→ **駁倒**:grep 證 lumos 唯二 config int 守衛(`_impact_load_config:5573-5576`、`impact-hook.py:335`)皆已加 `not isinstance(v,bool)`、無未修實例。✓ **駁倒即丟 → 進〈已評估駁回〉**(供下次去重)。
4. **去重**:記進〈已評估駁回〉,下次 skill 進場讀到即跳過。✓

## 誠實邊界
- **人放行→〈已採納〉路徑本輪未觸發**:dogfood 唯一候選被反證預篩擋下(已處理),無 survivor 到人放行。該路徑(survivor→人放行→〈已採納〉)結構對稱、與〈已評估駁回〉同節點寫入,但**本次 dogfood 未實走**。honest。
- **無機械 oracle**(skill §4):「駁倒」是「反證者 grep 到已全修」,不是「此坑普世假」——此坑是真通則隱患,只是 lumos 當下無未修實例。人閘/反證者能力上限的天花板仍在。
- refute-prescreen 的價值本次坐實:**擋掉已處理的坑、不記進 active 隱患**(避免噪音)——正是設計意圖。
