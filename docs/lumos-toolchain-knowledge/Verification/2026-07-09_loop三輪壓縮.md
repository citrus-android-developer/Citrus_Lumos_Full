---
type: verification
status: pass
created: 2026-07-09
updated: 2026-07-09
plan_refs:
  - "[[loop三輪壓縮_計劃]]"
related:
  - "[[loop三輪壓縮_計劃]]"
  - "[[loop三輪壓縮_實作計畫]]"
  - "[[design-loop]]"
  - "[[convergence-evidence-gate]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:loop 三輪壓縮機械核心實作完成,847 passed 0 failed(branch feat/loop-panel-compress,TDD T1-T5 + opus 機械核心終審 Ready-after-fix);將 canary-護對抗審計 loop 從 6 輪同族循序壓成平行多樣 panel、收斂信號改建在結構上
  VERIFY:T1 capture-recapture 殘餘估計(Chao1 偏差修正純函式)/ T2 canary record --round 留痕欄 / T3 loop status --panel 收斂謂詞(輪有效≥2caught∧存活max≤minor∧capture-recapture殘餘<門檻+混用守衛+capture_counts)/ T4 difficulty panel_width(tier→寬度)/ T5 prose 接線(design-loop SKILL+templates §7+Systems KEY)
  KEY:收斂判準從 count/序列(G2)改結構信號——framing 汙染 count 不汙染結構:capture-recapture 讀重疊、ODC 讀 class、AC 讀 coverage;legacy(無 --panel)K-streak∧G1∧G2 逐位元不變(向後相容)
  KEY:opus 機械核心終審抓 2 blocker 已修——C1 無 capture_counts 改 fail-closed(不繞過殘餘)、I1 混用守衛改全帶 round 比對(殺 partial-mix None phantom 輪);M1 maxsev 只算 caught
  KEY:誠實天花板=文獻是 benchmark 準確度非 canary/辯方構件;capture-recapture 小樣本出極端值當一個信號非 oracle;orchestrator panel 化延後(自主 loop 暫停);code-loop SKILL 已補 panel 段(機械核心共用,差別=diff/bug-canary/無G1)
---
# 2026-07-09 loop 三輪壓縮驗證(機械核心)

把 canary-護對抗審計 loop(design-loop/code-loop)從「6 輪同族循序」壓成「平行多樣 panel」,收斂信號改建在 framing 汙染不到的結構上。緣起:使用者指出 6 輪太耗;PRIOR-ART 三線文獻 + 2 輪平行 panel dogfood(設計節點)證 6 輪跑在有用邊界外。

## 測試結果
- **`scripts/test_lumos.py` 847 passed, 0 failed**(基線 828 → +19:caprecap 5 / canary_round 3 / panel_gate 8[含 C1 fail-closed + I1 partial-mix]/ difficulty_panel_width 3)。`test_autonomous_loop.py` 53 OK(test_params_mapping 更新)。
- TDD T1-T5 逐 task 紅→綠;opus 機械核心終審 Ready-to-merge-after-fix(2 blocker C1/I1 已修)。本 repo `lumos doctor` 0 issues。

## 交付
1. **T1 `_estimate_remaining_defects`**:capture-recapture Chao1 偏差修正 `f1*(f1-1)/(2*(f2+1))`(f2=0 不 div0);高重疊→殘餘≈0 收斂、低重疊→續跑。平行 panel 的收斂結構信號。
2. **T2 `canary record --round`**:panel 一輪 W 筆共享 round-id;向後相容(不帶則無欄)。
3. **T3 `loop status --panel`**:按 round-id 分組判最新輪四條合取(輪有效≥2caught ∧ 存活 max≤minor[只算 caught]∧ capture-recapture 殘餘<1.0[**無 counts=fail-closed**])+ 混用守衛(全帶 round 否則 rc2,殺 None phantom 輪);legacy 路徑完全不動。`--capture-counts` 留痕欄。
4. **T4 `difficulty.params` panel_width**:high=5/std=3;既有 need/maxr 不變。
5. **T5 prose**:design-loop SKILL 平行 panel 段、templates.md §7 派工模板、Systems/design-loop + convergence-evidence-gate KEY。

## opus 終審實證(2 blocker 已修)
- **C1 capture-recapture 繞過**:原無 capture_counts 靜默跳過殘餘 → 改 **fail-closed**(不寫 counts 即判未枯竭擋,否則退回舊「2 caught+無 major」弱信號)。`t_loop_panel_gate` 補案。
- **I1 混用守衛不全**:`any(round)` 讓 partial-mix log 過關、round-less 記錄形成 None phantom 組可偽過 → 改「全帶 round 否則 rc2」。補 partial-mix 測案。
- **M1**:maxsev 只算 caught 審計員(missed 已剔除,對齊設計)。

## 誠實天花板
- 文獻(2-3 輪報酬遞減 / 9 judge 2 票 / capture-recapture / CGES / falsification>voting)是 benchmark 準確度,非 canary/辯方構件;移植的是「輪數 vs 廣度」「收斂點結構信號」,非 1:1。
- capture-recapture 小樣本會出極端值——當一個收斂信號、非 oracle。
- **orchestrator panel 化延後**:自主 loop 暫停中(2026-07-07),panel 化其 orchestrator-prompt 低優先。**code-loop SKILL 已補 panel 段**(2026-07-09;機械核心 loop-agnostic 共用,差別=diff 工作副本/bug-canary 四型/無 G1)。design-loop + code-loop 手動路徑均上。
- dogfood 元教訓:壓縮 spec 自己也撞 glue 天花板(2 輪 panel 未形式收斂),證「架構收斂轉 TDD、別磨散文到 cap」——正是它要治的病。

## 追加:code-loop 異質 finder 接線(2026-07-09,同日 ship)
緣起:使用者質疑「code-loop 該全盤沿用 design-loop 慣例嗎?code review 不該不一樣?」→ 交叉查文獻(AutoSafeCoder / Multi-Agent Code Verification via Info Theory arxiv 2511.16708 submodularity / Greptile TREX / CodeRabbit sandbox / PBR defect-type mapping)。結論:程式碼可執行+可靜態分析,最佳解是**異質 ensemble** 非純 LLM panel——確定性驗證器(SARIF linter/測試/type/mutation)錯誤剖面與 LLM 正交=真獨立票,直擊「9 judge 2 票」相關性天花板。
- **機械交付**:`_capture_counts_from_finders(finders)`(跨 finder 正規化 casefold+strip、finder 內去重、數重疊、降序回傳)+ `lumos loop capture-counts --finder ...`(吐 capture_counts + 殘餘估計 + 可貼的 `canary record --capture-counts` 串)。把「異質 finder → capture-recapture 輸入」的重疊計數機械化(人手數易錯)。
- **測試**:`t_capture_counts_from_finders`(5 check:三家中同洞→含 3、獨發→多個 1、正規化去重、空、串接殘餘)+ `t_loop_capture_counts_cli`(7 check:CLI distinct/counts/建議串/續跑側/空)。**859 passed, 0 failed**。
- **踩雷 + 已硬化**:`cmd_loop_capture_counts` 原用 `for f in (finders or [])`,撞 `t_hook_copy_list_completeness` 那條 fragile regex(grep 源碼**第一個** `for f in (`)→ copy_list 抓空誤紅。先改 loop 變數 `f`→`fs` 消 shadow;**再硬化測試根因**(2026-07-09 同日,使用者指示):側 B 先錨定 `def _install_hooks_py` 再在其範圍內找複製清單 `for f in (...)`,不再抓全檔第一個——別處無關 `for f in (...)`(sarif 轉換 / capture-counts)不再搶走。加一條「源碼有 _install_hooks_py」錨點斷言防錨失效。865 passed。
- **prose**:code-loop SKILL panel 段補「異質 finder → capture_counts 機械算法」+ linter 命中經 `pitfalls --diff` 取。anchor baseline 重核(test_lumos.py)。

### 再追加:`--from-pitfalls` 一鍵收割(2026-07-09,同日)
緣起:使用者追問「linter 命中→餵進去仍手動串」那句何意 → 釐清「便利原語(現在能做)vs 端到端無人跑(卡自主 loop 暫停)」兩者被我混淆,便利原語其實不卡。遂補:
- `loop capture-counts --from-pitfalls <range> [--repo]`:自動跑 `pitfalls --diff`、按 `source` 分組(每 linter driver / pitfalls 內建各一個確定性 finder)、命中 `file:line` 併進手動 --finder 一起算重疊。關掉「手動把 linter 命中貼成 --finder」那半破口(手動路徑即一鍵,不等自主 loop 恢復)。
- 重構:抽 `_pitfall_diff_collect`(純計算不印,回 dict|None)供 `_pitfall_diff_mode`(印)與收割共用;pitfalls diff/lint JSON 逐鍵不變(t_pitfalls_diff 11 + t_pitfalls_lint_integration 15 綠)。
- capture-counts 提前到 vault-free 段(純機械不碰架構圖)。t_loop_capture_counts_from_pitfalls(5 check:收割/重疊 count/建議串/壞 range rc2)。**864 passed 0 failed**。
