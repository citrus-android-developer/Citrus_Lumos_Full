---
type: verification
status: pass
date: 2026-07-24
valid_under:
  - "teardown 三步順序=全域hook清理→deinit(keep_graph)→uninstall;全域步先(趁 vendored merge 未刪)"
  - "merge --prune-only 只剪懸空不 re-add;_teardown_global_claude 壞 JSON 跳過不刪 .py"
  - "core.hooksPath 只在指向本 repo scripts/hooks 時 unset;uninstall 只移 symlink 不刪一般檔"
revalidate_when:
  - "動 cmd_teardown/_teardown_global_claude/_deinit_unbar_gate/cmd_uninstall/merge-claude-settings.py 的 prune-only"
plan_refs:
  - "[[Projects/teardown一鍵拆機_計劃]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:t_teardown_global_claude(4:清我方hook+剪懸空+保留使用者hook+壞JSON跳過不刪)/t_merge_prune_only(2:prune-only不re-add+對照預設會add)/t_deinit_unbar_gate_user_value(使用者core.hooksPath保留)/t_uninstall_preserves_regular_bin(一般檔不刪)/t_teardown_refuse_zero_mutation(2:非互動無--yes rc2+零mutation)+全套1365綠零迴歸
  KEY:lumos teardown 落地——一鍵拆當前 repo 專案層+機器全域(CLI/skills/hooks),★永遠保留架構圖文件★;範圍非全機所有 repo(deinit 只吃當前 toplevel)
  KEY:三步順序修正(std F5)=全域hook清理先(_teardown_global_claude 要用 merge-claude-settings.py,deinit 會刪它故必須先跑)→deinit(keep_graph=True)→uninstall
  KEY:關鍵設計修正(讀碼查出,Codex 未點)=merge 預設模式 prune 完會 re-add 我方 hook、抵銷 teardown→新增 merge --prune-only「只剪不加」;_teardown_global_claude 壞 settings JSON 先驗跳過、不刪 .py(不製造懸空,std F6)
  KEY:兩便宜守衛改共用函式(deinit/uninstall 直呼也變安全)=core.hooksPath 只在指向本 repo scripts/hooks 才 unset(std F8 不誤殺使用者 githooks)+uninstall 只移 symlink 不刪同名一般檔(std F10)
  VERIFY:設計 std 單席 Codex 跨家族審(blocker+多 major:順序F5/失敗F6/守衛F8F10/範圍F11/繼承殘留F4F9F12)+使用者「照建議」裁定;先寫失敗測試 TDD
  KEY:繼承殘留未修(記 Issues/hook卸載殘留註冊 已補移除半邊;F4 CLAUDE.md 正規化/F9 整夾rmtree/F12 移全部skills=deinit老bug另票)
---
# 2026-07-24_teardown一鍵拆機

`lumos teardown` 落地。spec：[[Projects/teardown一鍵拆機_計劃]]。緣起：使用者要一個指令拆機器層＋專案層但保留架構圖，並補全域 hook 殘留坑（[[Issues/hook卸載殘留註冊]] 的移除半邊）。

## 做了什麼
新增 `lumos teardown`：一鍵三步（順序：全域先）——①`_teardown_global_claude()` 清全域 `~/.claude` 我方 hook＋剪懸空註冊 ②`cmd_deinit(keep_graph=True)` 拆專案層（架構圖強制保留）③`cmd_uninstall()` 拆機器 CLI/skills。

## Codex 跨家族審＋讀碼查出的關鍵修正
- **順序（std F5）**：deinit 會刪掉 `merge-claude-settings.py`，故全域清理（要用它）必須先跑。
- **merge re-add（讀碼查出，Codex 未點）**：merge 預設 prune 完會把 hook **加回去**、抵銷 teardown → 新增 `merge --prune-only`「只剪不加」。
- **失敗語意（std F6）**：`_teardown_global_claude` 先驗 settings.json 可解析，壞則跳過＋warn、**不刪 .py**（不製造懸空）。
- **兩守衛（std F8/F10，改共用函式）**：`core.hooksPath` 只在指向本 repo `scripts/hooks` 時 unset（不誤殺使用者 githooks）；`uninstall` 只移 symlink、不刪同名一般檔。
- **範圍誠實（std F11）**：teardown ＝當前 repo＋機器全域，非全機所有 repo。

## 未修的繼承殘留（另開 deinit 票）
F4 剝 CLAUDE.md 正規化 sentinel 外空白/CRLF、F9 `scripts/hooks` 與 `scripts/templates` 整夾 rmtree（刪使用者自有檔）、F12 uninstall 移除全部 skills。teardown help 明列這些破壞性。

## 測試
5 個測試函式 10 檢查（全域清理/prune-only/兩守衛/拒絕零 mutation）＋全套 1365 綠。假 HOME 隔離，嚴禁碰真 `~/.claude`。
