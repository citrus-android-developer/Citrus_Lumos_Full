---
type: verification
status: pass
feature: cochange 共改漏改守衛(rules/check CLI + pre-commit Gate CC)
commit: (見本檔所在 commit)
date: 2026-07-10
valid_under:
  - "python3 stdlib(3.9+),零依賴;git 2.x(-c core.quotePath=off 支援)"
  - "scripts/hooks/pre-commit Gate CC 結構(STAGED 後、Gate 1 前)"
  - "預設門檻 conf≥0.8/support≥3/max_changeset 20;預設排除清單 11 pattern"
  - "本 repo ~480 非 merge commits、Landmark ~1480(挖掘延遲 <0.5s 的量級)"
revalidate_when:
  - "pre-commit hook 結構重排(Gate 順序/早退點變動)"
  - "cochange 門檻預設值或排除清單變更"
  - "repo 歷史成長逾一個量級(>1 萬 commits——效能上限未驗,cache 是 v2)"
  - "git 改變 --name-only/quotePath 行為"
plan_refs:
  - "[[Projects/cochange守衛_計劃]]"
---
# 驗證：cochange 共改漏改守衛

## 變更範圍

- `scripts/lumos`：`_COCHANGE_DEFAULTS`/`_COCHANGE_DEFAULT_EXCLUDE` + `_cochange_repo_root`/`_cochange_load_config`/`_cochange_excluded`/`_cochange_transactions`/`_cochange_mine` + `cmd_cochange_rules`/`cmd_cochange_check` + parser/dispatch（頂層命令 41→42）。
- `scripts/hooks/pre-commit`：Gate CC（advisory，STAGED 後、Gate 1 前，`|| true` 隔離）。
- `scripts/test_lumos.py`：`t_cochange`（28 斷言）+ `_mk_cochange_repo` 合成歷史。
- 文件計數同步：SKILL.md（42+分類「完整性/影響 3」+vault-free 表列）、README.md、README.en.md、ARCHITECTURE.md×3。

## 測試項目

| 項目 | 預期 | 結果 |
|------|------|------|
| 合成歷史 conf/support（A⇒B 0.8、B⇒A 1.0） | 精確 | ✅ |
| merge/大 commit(>20 檔)排除、根層 lockfile 雙試 glob 排除 | 不產規則 | ✅ |
| 中文檔名 key 非 octal 逃逸 | 字面中文 | ✅ |
| rules 預設套門檻/--all 超集/support=1 連 --all 不見 | 如 spec | ✅ |
| check --staged 漏改警告(stdout)/夥伴同 staged 不警告/右側已刪不警告 | 如 spec | ✅ |
| --diff 挖掘母體=range 左端(不自我豁免)/皆缺 rc2/同給 diff 優先 | 如 spec | ✅ |
| config 覆寫/exclude 合併/min_support=1 視為 2/壞 JSON fail-open rc0+stdout 提示 | 如 spec | ✅ |
| zero-commit rules+check rc0、非 git rc2 | 如 spec | ✅ |
| 全套迴歸 | 893/893 | ✅ |

## 真機驗收

- 本 repo：`rules` 含 README.en.md⇒README.md(1.0)、test_lumos⇒lumos(0.82)、**ARCHITECTURE.md⇒README.md(1.0)**——r3 審計員手動抓的散落對，守衛自動命中；方向性雙向驗收通過（改 en 漏主→警告；反向→無警告）。
- Landmark（1481 commits consumer）：209 條規則 / 0.43s / 生成檔噪音 0 / 架構圖節點共改對浮現（`Issues/抽獎券流水號設計⇒Systems/票券系統` conf 1.0）。

## 測試方式

`python3 scripts/test_lumos.py -k cochange`；真機驗收指令見 [[Projects/cochange守衛_計劃]] 驗收節。

## 相關模組

- [[Systems/cochange-guard]]
