---
type: verification
status: pass
date: 2026-08-20
valid_under: "本 repo 為獨立交付來源(非從上游單向覆蓋同步);GitHub 帳號與 repo 名未變更"
revalidate_when: "從上游同步任何 commit 後;改 get.sh/get.ps1/_bootstrap_url/README 安裝段時;repo 更名或轉移時"
tags:
  - type/verification
  - status/pass
plan_refs:
  - "[[Projects/bootstrap一鍵對稱_計劃]]"
---
# 2026-08-20_鏡像安裝入口去上游耦合

> 白話:這個 clone(`citrus-android-developer/Citrus_Lumos_Full`)原本的一鍵安裝網址、PowerShell 入口、README 指令、以及 bootstrap 的最後一段預設值,**全都寫死指向上游 `EnzoHsieh-Android/Lumos`**。本次把這些活路徑一律改指向本 repo 自己,讓從這裡裝機、從這裡更新的人不會被拉回上游。

## 為什麼要做

2026-08-19 的鏡像自足只解到一半:`_self_repo_origin` 讓「**已經有 clone**」的機器自足,但**一鍵安裝那條路根本還沒有 clone**——`get.sh` 自己的 `LUMOS_URL` 是寫死的,所以 curl 本 repo 的 `get.sh` 仍然會 clone 上游。實測 2026-08-20:本 repo 與上游的 `get.sh` 在該行**一字不差**。

⚠ **當時沒有壞掉**:兩個 raw 網址皆回 200、兩邊 HEAD 同為 `2072755`,所以裝到的內容一樣。★這是「哪天分岔就出事」的潛伏耦合,不是當下故障★——記錄在此以免日後誤判成「本來就好好的」。

## 改了哪些(活路徑,10 檔)

| 檔 | 改什麼 |
|---|---|
| `get.sh` / `get.ps1` | 用法註解的 curl/irm 網址、`LUMOS_URL`/`$url` 預設值 |
| `README.md` / `README.en.md` / `ONBOARDING.md` | 一鍵安裝指令、手動 clone 指令、Windows 入口 |
| `ARCHITECTURE.md` | 架構圖「唯一源」標籤 |
| `scripts/lumos` | `_bootstrap_url` 解析鏈**第四段**(硬編碼 fallback) |
| `scripts/test_lumos.py` | 上述第四段的行為斷言 |
| `skills/lumos-project-notes/reference.md` | 生命週期段「唯一源」描述 |
| `Systems/slim-get-一行安裝` | 描述完整版 get.sh clone 誰的那句 |

**刻意不改**:`docs/design/`、`docs/superpowers/plans/`、`governance/` 下的命中——那些是當時的歷史紀錄與審計證據,改了等於竄改帳。

## 驗證

- `bash -n get.sh` 通過;`scripts/lumos` 編譯通過。
- 解析鏈實跑:無訊號時第四段回 `https://github.com/citrus-android-developer/Citrus_Lumos_Full`;本 clone(有 origin)回 `…/Citrus_Lumos_Full.git`。
- 全量測試 **2858 passed, 0 failed**。
- 活檔 `EnzoHsieh-Android/Lumos` 命中數歸零(歷史檔除外)。

## ⚠ 殘留風險(未解,交人)

★**上游同步會把這些改動洗掉**★:上游那份 clone 有一個名為 `Full` 的 remote 直接指向本 repo,歷史上是「上游 → 本鏡像」單向推。下次照舊推一次,本次 10 檔全部被還原成指向上游。

本次**沒有**加任何機械守衛擋這件事——要擋得在上游端做(例如同步前先跑一條檢查:本 repo 的活檔不得出現上游 URL),那是上游 repo 的改動,不在本次範圍。
