---
type: verification
status: pass
date: 2026-07-31
feature: slim-scan
valid_under:
  - "精簡版指令集固定為現行 24 支保留指令(KEEP 白名單)；removed 集合仍即時取 `lumos --help`,故指令新增/改名自動反映"
  - "掃描對象是純文字 Markdown(README/SKILL.md/reference.md),非結構化 frontmatter/程式碼"
revalidate_when:
  - "精簡版保留指令清單(KEEP)變動 → 連動改腳本內白名單並重跑三個已知案例"
  - "DROP_SKILLS(不交付 skill 清單)增減 → 重跑真實檔案掃描確認候選數與已知案例仍命中"
  - "`lumos --help` 的 choices 輸出格式改變(目前靠 regex `\\{([a-z0-9,\\-]+)\\}` 解析) → 重驗 all_commands()"
plan_refs:
  - "[[Projects/公開精簡版_實作計畫]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:t_slim_scan 8 checks 全綠(`python3 scripts/test_lumos.py -k slim_scan`)
  VERIFY:[[Projects/公開精簡版_實作計畫]] Task 1 落地;spec 放行時列的三條裸項之一(第五種形態=裸散文)由本掃描器接住
  KEY:對真實交付檔 skills/lumos-project-notes/SKILL.md 與 skills/lumos-project-notes/reference.md 跑一次,candidates=129(>50 門檻),三個已知案例(spec 三輪對抗審計各自抓到的形態)全命中——reference.md:85(子命令全覽,bare-token 多命中)/reference.md:730(`loop status --gate`,span-with-args)/SKILL.md:156(裸散文 canary,prose)
  KEY:已知缺口——形態⑤裸散文比對 `len(cmd)<4` 短路跳過短指令名(如 `gov`),刻意取捨非疏漏,見 [[Systems/slim-scan-掃描器]]
---
# Verification: slim-scan 交付文字掃描器落地（2026-07-31）

驗證對象：[[Projects/公開精簡版_實作計畫]] Task 1 —— 交付文字掃描器（`scripts/slim-scan.py`）。

## 落地內容

| 項目 | 內容 |
|---|---|
| 新檔 | `scripts/slim-scan.py`（stdlib only，rc 0/1/2） |
| 測試 | `scripts/test_lumos.py::t_slim_scan`（8 checks：rc1 有候選、五種形態各一 check、不誤報保留指令、乾淨檔 rc0） |
| 真實檔案驗證 | `skills/lumos-project-notes/SKILL.md` + `skills/lumos-project-notes/reference.md`，candidates=129 |

## 測試結果

```
$ python3 scripts/test_lumos.py -k slim_scan
lumos 測試(1 案例)
  ✓ slim-scan 有命中回 rc1
  ✓ slim-scan 命中形態 prefixed
  ✓ slim-scan 命中形態 bare-token
  ✓ slim-scan 命中形態 skill-name
  ✓ slim-scan 命中形態 span-with-args
  ✓ slim-scan 命中形態 prose
  ✓ slim-scan 不誤報保留指令 search(第6行)
  ✓ slim-scan 乾淨檔 rc0

────────────────────────────────────────
8 passed, 0 failed
```

## 已知缺口

形態⑤（裸散文型）對短於 4 字元的指令名（如 `gov`）不做比對——刻意取捨（誤報率過高），交形態①②接住。若日後有短指令名只以裸散文提及且未被移除，掃描器抓不到，需人工複查或另立規則。

## 相關

- 設計/規格：`.superpowers/sdd/公開精簡版_實作計畫/task-1-brief.md`（非架構圖路徑，SDD 產出落地於此）
- 系統筆記：[[Systems/slim-scan-掃描器]]
