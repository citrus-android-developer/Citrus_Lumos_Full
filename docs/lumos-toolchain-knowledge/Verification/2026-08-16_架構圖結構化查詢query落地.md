---
type: verification
status: pass
date: 2026-08-16
valid_under:
  - "scripts/lumos cmd_query 現行實作(TDD branch feat/query)"
  - "標籤家族 2026-08-05 收編定版(type/status/priority/scope/flag/risk)"
  - "Landmark 架構圖 2026-08-16 快照(314 節點)為驗收語料"
revalidate_when:
  - "cmd_query 篩選語意變更(旗標增刪/AND 語意/收案態集合)"
  - "extract_contracts 合約認定格式變更"
  - "標籤家族值域改版(影響 --tag/--active 語意)"
  - "superseded 真遺忘策略變更(search/query 應同步)"
plan_refs:
  - "[[Projects/架構圖結構化查詢_計劃]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:t_query_* 7 案 32 斷言全綠(標籤 AND/no-tag/active/contract 真解析器/linked 鄰域/superseded 排除+逃生/bare rc2/json);全量 2636 passed 0 failed(基線 2629+7:含 5 檔指令數宣稱 60→61、slim KEEP 收編 query 25→26 支、slim 範本三檔支數同步——全是漂移守衛當場抓的散落列舉)
  VERIFY:Landmark 真庫驗收三情境重放——①risk/金流+--contract=7 節點(比 2026-08-16 土法模擬少 1:計劃節點三處合約符號全是「應標」待辦散文,真解析器正確排除,誤算在土法)②risk/金流+--active=4 筆與模擬完全一致(1 todo 計劃+3 open 併發 Issue)③--linked 票券系統+type/issue+status/open=6 筆,與 backlinks 82 行人工核對之 open Issue 集一致
---
# 2026-08-16_架構圖結構化查詢query落地

`lumos query` 結構化查詢原語落地([[Projects/架構圖結構化查詢_計劃]];borrow-design 自 zk list 旗標語意)。

## 驗證內容

1. **TDD 回歸**:`scripts/test_lumos.py` 新增 `t_query_*` 7 案(先紅後綠);全量 2636 passed / 0 failed。
2. **Landmark 真庫驗收**(314 節點,乾淨無 fixture 污染):
   - `query --tag risk/金流 --contract` → 7 節點(票券/點數/滿額贈/發票補登/活動報名/點數商城/POS-API)。與土法模擬差 1 筆:`滿額贈達標明細軟刪與PosOrderNo退場_計劃` 檔內 ★INVARIANT★ 三處全是「應標成合約」的待辦散文(:175/:203/:259),extract_contracts 只認 KEY 行標準格式,排除正確——真解析器修正了土法 grep 的誤算。
   - `query --tag risk/金流 --active` → 4 筆,與模擬完全一致。
   - `query --linked 票券系統 --tag type/issue --tag status/open` → 6 筆 open Issue,與 backlinks 全量 82 行人工核對一致。
3. **交付面同步**(漂移守衛逼出,非自發記得):AGENTS/ARCHITECTURE/README/README.en/skill reference 指令數 60→61;slim KEEP 收編 query(25→26 支)+ 範本三檔宣稱同步——query 屬讀原語家族,消費端(如 Landmark)正是主要受益者,故交付而非白名單繞過。
