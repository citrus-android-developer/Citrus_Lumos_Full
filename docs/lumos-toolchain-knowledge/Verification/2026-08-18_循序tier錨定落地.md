---
type: verification
status: pass
date: 2026-08-18
valid_under: cmd_loop_next 現行守衛結構、_roster_kind 三值判、_TIER_PARAMS standard=(3,3)
revalidate_when: tier↔格式守衛再改動、_TIER_PARAMS 值變更、skill 循序 cap 政策改動
summary: |
  VERIFY:TDD 六組(anchor/panel_ok/回歸格含 light/roster 表/cap 行為/tier_hint 警告)先紅後綠+全量 2810 綠零倒退
  KEY:守衛放寬僅 code∧standard∧無round 一格(seq 布林含 not panel_fmt);零收緊(panel 照舊合法);cap 6→3=第 3 輪攤人;tier_hint 警告式不邀請補標
tags:
  - type/verification
  - status/pass
plan_refs:
  - "[[Projects/循序codeloop-tier錨定_計劃]]"
---

# 2026-08-18_循序tier錨定落地

> 白話:循序單 reviewer 的 code loop 終於能標 standard 檔位——守衛認得 loop 種類後只放行這一格,其餘象限回歸測試逐格釘住零收緊;cap 從被迫的 6 輪變成設計的 3 輪攤人。

## 驗證內容

- TDD 六組測試先紅後綠(紅=守衛 rc2/表無列/警告缺):`t_seq_code_std_anchor`(含前置斷言 design 象限仍擋)/`t_seq_code_std_panel_ok`(零收緊釘)/`t_seq_guard_regression_grid`(high×2、indeterminate、light×2 五格不動)/`t_seq_cap_and_roster`(3 筆 cap-reached+編制吐單 reviewer+否決)/`t_seq_tier_hint_warning`(code 含「整批計入」警告、design 不含,雙前置斷言防 vacuous)/`t_tier_roster_table` 更新(五組合+sequential)。
- 全量 `scripts/test_lumos.py` 2810 passed / 0 failed(基線 2790+20)。

## 交付面

- `scripts/lumos`:守衛加 `not seq` 豁免(seq=`code ∧ standard ∧ not panel_fmt`);width 覆寫 1(cap 沿 _TIER_PARAMS);canary_type/rmode/cluster_hint 三分支點接 seq(disposal_cmd 隨 rmode 自動跟動);tier_hint 對 code kind 換警告式措辭;`_TIER_ROSTER` 補 ("code","standard") sequential+上方註解修真。
- skill:`lumos-code-loop/SKILL.md` 護欄 cap 行改三檔(legacy 6/錨定循序 3/panel 3 輪)+頂部 standard 條補「可錨定」;架構圖:[[Projects/派工編制資料化_計劃]] 範圍刀 supersede 註記、[[Issues/loop-next吐不可宣告的tier]] 病根結案橫幅。
