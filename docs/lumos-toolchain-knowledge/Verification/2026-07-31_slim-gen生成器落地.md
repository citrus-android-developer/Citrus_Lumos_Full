---
type: verification
status: pass
date: 2026-07-31
feature: slim-gen
valid_under:
  - "精簡版保留指令清單固定為現行 24 支(DEFAULT_KEEP);root 集合演算法覆蓋 dispatch if 分支 + module-level 語句/class body + 迴圈註冊三種形態"
  - "輸入是單檔 `scripts/lumos`(module-level main() dispatch 慣例),非任意多檔案專案"
plan_refs:
  - "[[Projects/公開精簡版_實作計畫]]"
revalidate_when:
  - "DEFAULT_KEEP(24 支保留指令)變動 → 重跑 t_slim_gen 確認 --help 集合仍等於新白名單"
  - "scripts/lumos 的 subparser 註冊改用第四種形態(非 Assign/Expr/迴圈)→ collect_edits 需補對應 visit,否則移除清單漏砍"
  - "scripts/lumos 註解密度大幅變動(新增/刪除大量註解區塊)→ 重新量測 t_slim_gen_keeps_comments 的 90% 密度門檻是否仍合理"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:t_slim_gen 全套 12 checks 全綠(`python3 scripts/test_lumos.py -k slim_gen`);全套回歸 1713 passed, 0 failed(`python3 scripts/test_lumos.py`)
  VERIFY:[[Projects/公開精簡版_實作計畫]] Task 2 落地;前一位實作者寫完 slim-gen.py(336 行)+ 三條測試但未收尾(無報告/無 commit),本次為接手收尾
  KEY:收尾時對抗實測後二次修正 t_slim_gen_keeps_comments 的斷言,細節見 [[Systems/slim-gen-生成器]] decisions
---
# Verification: slim-gen AST 生成器落地(2026-07-31)

驗證對象:[[Projects/公開精簡版_實作計畫]] Task 2 —— AST 生成器(`scripts/slim-gen.py`)。前一位實作者已完成實作與三條測試(12 checks 全綠),中斷於未寫報告、未 commit。本次任務是接手收尾:修正一條測試斷言(裁定已下)、跑全套回歸、寫報告並 commit。

## 落地內容

| 項目 | 內容 |
|---|---|
| 新檔 | `scripts/slim-gen.py`(stdlib only,336 行) |
| 測試 | `scripts/test_lumos.py`::`t_slim_gen`/`t_slim_gen_loop_registration`/`t_slim_gen_keeps_comments`(共 12 checks) |
| 手法 | 行級手術(算 root → 可達性閉包 → 收集刪除行號範圍 → 套用),非 `ast.unparse`(語法樹無註解,會把 686 行事故脈絡註解全剝光) |

## 測試結果(slim_gen)

```
$ python3 scripts/test_lumos.py -k slim_gen
lumos 測試(3 案例)
  ✓ slim-gen 真檔 rc0
  ✓ 產物 --help == 保留 24 支
  ✓ 產物 py_compile 0 SyntaxWarning
  ✓ 產物 dangling handler = 0
  ✓ slim-gen rc0
  ✓ ★產物註解密度未下降★(原 686/11999=5.7% → 產物 379/6203=6.1%)
  ✓ 事故脈絡註解仍在(實測 test_lumos.py 260→94 那條)
  ✓ slim-gen 合成 fixture rc0
  ✓ 迴圈註冊的 dropme 被砍出註冊清單
  ✓ 迴圈註冊的 keepme 仍在
  ✓ cmd_dropme 函式被移除
  ✓ 產物 --help 不含 dropme

────────────────────────────────────────
12 passed, 0 failed
```

## 全套回歸

```
$ python3 scripts/test_lumos.py
────────────────────────────────────────
1713 passed, 0 failed
```

## 斷言修正始末(t_slim_gen_keeps_comments)

Brief 原稿指定「產物保住 ≥60% 註解」+ 哨兵查 `W4` 這條註解。查證後兩處都改:

1. **哨兵 W4 不成立**:`W4` 位於 `_link_or_copy`(`scripts/lumos:7238`),只被 `_install_skills` 呼叫,後者只從 `cmd_install`/`cmd_uninstall` 可達——兩支都在移除清單,`_link_or_copy` 被正當砍掉,註解跟著消失完全正確,是 brief 挑錯哨兵。換成 `test_lumos.py 260→94` 那條事故註解(出現兩處:module-level `TEST_PROFILES` dict 字面值裡、以及保留指令閉包內的 `discover_test_methods()` 裡,兩處都保證留在產物中)。
2. **百分比門檻無意義**:任意保留率門檻(60%/50%)砍多砍少都會動它,不是行級手術這個設計本身要鎖的性質。改鎖「產物註解密度不得低於原檔的 90%」——這才是行級手術(相對 `ast.unparse`)的實質保證,照樣抓得住 `ast.unparse` 迴歸(密度會歸零)。實測:原檔 11999 行/686 行註解=5.7%;產物 6203 行/379 行註解=6.1%,未下降。

## 意外

- 前一位實作者中途已經對 brief 原稿的斷言做過一次校正(改到 ≥50% + `O_NOFOLLOW` 哨兵,見 git 未 commit 的工作副本),但未留紀錄說明理由。本次是二次校正,採密度守衛取代任意百分比。
- `scripts/test_lumos.py` 在此 repo(lumos-toolchain 源 repo 本身,有 `skills/lumos-project-notes`)不在 pre-commit 架構圖閘的豁免白名單內(白名單排除條件是「非源 repo」),因此本次連同 `scripts/slim-gen.py` 一起需要架構圖節點才能過閘,依 CLAUDE.md 紀律建立 [[Systems/slim-gen-生成器]] + 本篇 Verification。

## 相關

- 設計/規格:`.superpowers/sdd/公開精簡版_實作計畫/task-2-brief.md`(SDD 產出,非架構圖路徑,依計畫落地於此)
- 系統筆記:[[Systems/slim-gen-生成器]]
