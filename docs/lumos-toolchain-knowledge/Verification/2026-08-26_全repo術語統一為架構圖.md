---
type: verification
status: pass
date: 2026-08-26
updated: 2026-08-27
valid_under:
  - "commit 222135b..0f0de99 的取代結果(全 repo「圖譜」→「架構圖」,349 檔 2456 處內文 + 9 個檔名/目錄名)"
  - "英文術語刻意未同步——README.en.md 仍用 knowledge graph / graph-as-contract(2026-08-26 使用者明示維持現狀)"
  - "識別碼層刻意未改名——check-graph-sync.py / graph-rename.sh / install-graph-toolchain.sh / find_vault / graph_root 仍為 graph"
  - "delguard 預設 deadline 2.0s(本次完整掃描以 LUMOS_DELGUARD_DEADLINE=600 補跑取得)"
revalidate_when:
  - "英文文件術語跟進改名(README.en.md 的 knowledge graph / graph-as-contract)"
  - "識別碼層改名(hook 檔名或 CLI 函式名的 graph 字樣)"
  - "delguard 預設 deadline 調整,或 pitfalls [併發] 規則改版(本次假陽性判讀的前提)"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:全量 2866 passed / 0 failed(改名前後同基線);lumos doctor 架構圖健康 0 issues(301 篇);301 篇 wikilink 無因改名而斷(40 條既有斷連全為模板佔位符);UTF-8 / py / json / jsonl 全數有效
  VERIFY:機械 oracle=逐檔取舊 blob 套用同一取代後與新 blob 比對,350 檔全部 byte-equal——證明 refactor(naming) 的 diff 純為用詞變更、零語意改動(2107 insertions / 2107 deletions 對稱)
  KEY:過閘紀錄——anchor verify 擋下 push(5 錨點:runner×2 + hooks×3),查證純術語後走 approve 留痕而非 --no-verify;code-loop tier=high 命中 scripts/lumos:9216 經查為 print 字串,以 skip --note 留痕放行;delguard 完整跑 degraded=false,2609 筆命中全為 conf=low 改名假陽性、0 筆 high
  KEY:本節點當時登記的 DEBT「delguard 預設 deadline 僅 2.0s,大 commit 必定超時 fail-open」**已於 2026-08-27 解除**——不是靠調 deadline(預設仍是 2.0s),是靠把掃描成本降下來(逐 token grep,見 [[Verification/2026-08-27_delguard逐token-grep]]);DEBT 標記已撤。★本節點〈過閘紀錄〉引述的 delguard 輸出與成本敘述皆為 2026-08-26 當時值,非現況★
related:
  - "[[Verification/2026-08-26_delguard超時分類與降級訊息修復]]"
  - "[[Verification/2026-08-27_delguard逐token-grep]]"
---
# 2026-08-26_全repo術語統一為架構圖

全 repo 術語「圖譜」統一改為「架構圖」。commit `222135b`(取代+改名)、`0f0de99`(治理帳收尾),已 push 至 `origin/main`。

## 改動範圍

| 層 | 量 | 說明 |
|---|---|---|
| 內文 | 349 檔 2456 處 | `docs/` 文件與架構圖筆記、`scripts/` 工具與 hook、`governance/` 治理帳 |
| 「知識圖譜」 | 70 處 | 一併改為「知識架構圖」(使用者裁定,不做例外) |
| 檔名/目錄名 | 9 個(展開 27 檔) | `git mv`,git 全數辨識為 rename |

改名清單:`docs/methodology/圖譜即合約{,-對外論述,-全景圖}.md`、`Projects/圖譜結構化查詢_計劃.md`、`Verification/2026-08-16_圖譜結構化查詢query落地.md`、`Projects/Android側UI測試綁圖譜工作流_{實作計畫,計劃}.md`、`governance/{golden,review-reports}/Android側UI測試綁圖譜工作流/`。

★順序陷阱★:內文取代先跑,把引用一併改成新名,當下那些引用是**斷的**;隨後的 `git mv` 才把它們接回。若只做取代不改名,或只改名不取代,都會留下斷鏈。

## 驗證方法

1. **機械 oracle**:逐檔取舊 blob → 套用同一「圖譜→架構圖」取代 → 與新 blob 比對。350 檔全部 byte-equal,證明 diff 純為用詞變更。`refactor(naming)` 統計 2107 insertions / 2107 deletions 對稱,佐證同一結論。
2. **全量測試**:2866 passed / 0 failed。
3. **架構圖健康**:`lumos doctor` 0 issues(301 篇);自查 301 篇 wikilink,40 條斷連全為 `<日期>_`、`A`、`..` 等模板佔位符,**與本次改名相關的斷連為 0**。
4. **編碼與格式**:349 檔 UTF-8 全數有效;6 個 `.py` 語法通過,12 個 `.json`、5 個 `.jsonl`(含 `governance/eval/retrieval-goldset.json` 檢索評測金標)全數解析成功。

★踩過的坑★:第一次用 `perl -CSD -i -pe` 跑完 rc=0 但零改動——`-CSD` 讓輸入解碼為字元、腳本內的字面量仍是位元組,兩邊對不上。改用位元組層取代才生效。**rc=0 不等於有做事**,必須另外驗改動量。

## 刻意保留的不一致

- **英文術語未同步**:`README.en.md` 仍用 `knowledge graph` / `graph-as-contract`(該檔 `graph` 40 處,全 repo 英文術語 8 處)。中文已是「架構圖」,語意上已對不上。pre-commit 的 cochange 提醒曾點到這件事(「改了 AGENTS.md 但未動 README.en.md,歷史 80% 同改」),當時被判為改名假陽性放過——**該判定是錯的**,它是真的。2026-08-26 使用者明示維持現狀。
- **識別碼層未改名**:`check-graph-sync.py`、`graph-rename.sh`、`install-graph-toolchain.sh`、`templates/graph-discipline.md`,以及 `find_vault` / `graph_root` 等函式名。改名需連 `settings.json` 一併調整,本次不動。
- **「歷史不得回改」區一併取代**:`governance/{external-reviews,golden,review-reports}` 共 94 檔 561 處。`test_lumos.py:15072` 明訂外審歸檔與 golden 凍結語料「歷史不得回改」;本次為全 repo 用詞一致而取代,經使用者確認保留。無 checksum 守衛盯 golden,故不會機械性壞掉,但逐字紀錄的忠實性已有折損。

## 過閘紀錄

| 閘 | 結果 |
|---|---|
| pre-commit 架構圖同步 | 擋下 `eddbb4b`(純 mode 100644→755)。依 hook 提示第 2 條以 `--no-verify` 通過,post-commit 已寫 `docs/.bypass-log.jsonl` |
| anchor verify | 擋下第一次 push,5 個錨點(`post-commit`/`pre-commit`/`pre-push`/`test_autonomous_loop.py`/`test_lumos.py`)sha256 不符。逐檔查證為純術語取代後走 `anchor approve --note`,**未用 `--no-verify` 繞過** |
| 全量測試閘 | 2866 passed / 0 failed |
| doctor | 架構圖健康 0 issues |
| code-loop | tier=high 命中 `scripts/lumos:9216`。該行是 `print("  ⚠ 來源不可用,略過 hooks…")`,進 diff 的唯一原因是術語取代;該檔 192 行變動逐行比對確認全為用詞變更。以 `code-loop skip --note` 留痕放行 |
| delguard | 兩筆 commit 的 pre-commit 皆超時 fail-open(★本表引述的輸出為當時版本,訊息已於同日修正,見〈待辦〉★)。事後以 `LUMOS_DELGUARD_DEADLINE=600` 補跑,`degraded: false`、tokens=7、2609 筆命中全為 `conf=low`、0 筆 high。7 個「被刪符號」(`Android`/`Projects`/`docs`/`knowledge`/`lumos`/`toolchain`)全是 rename 舊路徑的片段;唯一 `fake_sync` 命中亦為單行 wikilink 改名 |

## 待辦

- ~~**delguard 降級輸出易被誤讀為正常通過**~~ → 已修,見 [[Verification/2026-08-26_delguard超時分類與降級訊息修復]]。本節點上方〈過閘紀錄〉引述的 `delguard: 內部錯誤(TimeoutExpired),放行` 是**當時**的輸出,現已改為「超時降級…★本輪未實際守衛★」。
  ~~★但只修了分類與訊息,沒修成本★~~ → **成本已於 2026-08-27 修掉**(逐 token grep,10 個常見 token 39.00s→0.25s,端到端 0.23s 未降級,見 [[Verification/2026-08-27_delguard逐token-grep]])。
  當時的敘述保留供脈絡:「完整掃描成本隨『被刪 token 數 × vault 規模』升,2 檔/63 刪除行的 commit 逾 5 分鐘未完成;閘仍然經常沒跑,差別只在現在看得出來」——★這段是 2026-08-26 的現況,今天已不成立★。
  ⚠ 本節點 `revalidate_when` 只寫了「deadline 調整」與「pitfalls 併發規則改版」,**沒預見「根因解法本身被換掉」**,所以這條 DEBT 的結清不是被重驗機制叫醒的,是靠退場自問掃出來的——重驗觸發條件的已知盲點。
- 英文術語與識別碼層是否跟進,見上方〈刻意保留的不一致〉。
