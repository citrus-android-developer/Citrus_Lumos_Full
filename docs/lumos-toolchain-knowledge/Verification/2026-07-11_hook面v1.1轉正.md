---
type: verification
status: pass
date: 2026-07-11
valid_under: goldset labels 2026-07-11 定稿;凍結參數 direct_base=0.30/free_quota=10/dyn_coef=0.65(v1.2 追記由 0.55 調升;本欄原記 0.55 已過時,2026-07-20 對帳更正);Landmark 實驗場 12 真實案例
revalidate_when: 排序參數再調整、goldset 重建、或 pitfall_when trigger 語意變更時重跑 §6
self_audit: sonnet/2026-07-20
plan_refs:
  - "[[Projects/檢索優化_計劃]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:§6 gate 七盞全綠 PASS——hook P@8 .509→.707、過閾中位 17→3、p95 50→9(名額制)、延遲 max .48s;search 面不動
  KEY:三病因三藥——①動態閾 0.25→0.55(主藥,精度 +20pp)②direct 基底 0.5→0.3(單體檔 p95 19→10)③content trigger 改比對「改動內容」(delta-scoped,3/9 固定席誤傷歸零)
  KEY:誠實取捨——0.65 係數應試最優(P@8 同 0.706)但必看視野 19/30,棄;0.55+名額制=精度過線且必看視野 24/30 最高;必看視野 28/30→24/30 是精度的代價,記帳不隱藏
  KEY:hook v1.1 已接線——窗外 ranked 降噪(固定席+top-8),TTL 窗內 --incidents-only 快速路;真機注入從 40+ 行縮到 4 行且頭名全中
  KEY:[2026-07-20 更正註]v1.2 追記的「必看 24/30」不可重現——同配置重跑 P@8 0.7298/噪音 1 皆重現,必看實為 19/30(=v1.1 已記載的 0.65 係數代價,數字被沿用進 v1.2 句子);真實取捨=精度+3pp 換掉 5 條必看視野,當時未當 trade-off 記;歸類折入漂移,未動參數
  VERIFY:Landmark=實驗場(12 真實改檔案例量條數/延遲;考卷量精度)——雙場同好才凍結
---
# 2026-07-11_hook面v1.1轉正

推播面（impact ranked）從 goldset FAIL（P@8=.509）修到 §6 七盞全綠 PASS 並接上 PreToolUse hook。

## 病因→修法→實測

| 病因（r1 code-loop Codex 診斷） | 修法 | 效果 |
|---|---|---|
| direct 基底 0.5 恆過動態閾（max≤0.25）——根因 | 動態閾係數 0.25→**0.55**；direct 基底 0.5→**0.30**（`0.30+0.70L`，弱字面直接關聯可被修剪） | P@8 .509→.707；單體檔（Landmark 核銷服務,44 篇引用）過閾 p95 19→10 |
| pitfall trigger 整檔比對誤傷（3/9 固定席噪音） | content trigger 改比對「**這次改動的內容**」（payload.query）；整檔比對僅限無 delta 的 legacy 呼叫 | E02/E03 型誤觸發歸零；新增危險內容仍必觸發（delta 含之） |
| 單體檔洪水（p95 24） | **名額制**：自由席過閾後按排名最多留 10（=top_k+2,gate 對齊）;剪 11 名以後、不動前 8 視野 | p95 24→9,精度/必看召回不受影響 |

## 調參紀律（雙場網格）

- **考卷段**（本 vault goldset train）量精度；**Landmark 實驗場**（12 個真實 git 改檔案例,含 `RedeemActivityService.cs` 單體怪物）量條數/延遲——**兩場同好才凍結**。
- 18 組網格 → 0.55 係數 P@8 高原起點;直接席基底只影響單體檔 p95;hop≥2 字面底線無作用（閾值已殺完,不加參數）。
- **應試 vs 誠實**：0.65 係數同樣 P@8≈.71 且 p95=9,但「必看(標2)在實際推播視野(固定席+前8)」19/30——砍到需要看的東西;0.55 為 24/30 最高 → 取 0.55+名額制。**必看視野從舊配置 28/30 降至 24/30 是精度的代價**,如實記帳;改善路徑=更好的排序（A3 in-degree/事件 frecency）而非更兇的閾值。

## hook v1.1 接線（同批轉正）

- 窗外：`impact --ranked --stdin-payload`,query=delta（Edit 舊+新/MultiEdit 串接/Write 全文,512 distinct token cap）;注入=「必看(固定席)+相關(top-8 帶分)+截斷注記」——真機從 40+ 行縮到 4 行且頭名全中。
- TTL 窗內：`--incidents-only` 快速路（事故安全面每次看,重型 BFS 降頻）——取代原「窗內全壓」。
- **兩個規格偏離（supersede r2 條款,記入計劃 decisions）**：①content trigger 比對對象從「套 delta 後整檔」改「delta 本身」——整檔比對對單體檔恆誤觸發（實測 3/9 噪音源）,且新增危險內容必在 delta 內,語意不損;②hook 不建 prospective 整檔（①之後無消費者）——省每次編輯的全檔讀改寫。
- 測試:t_impact_hook_v11 6 斷言+兩處 mock 改 ranked schema;全套 1018 綠。

## 更正註(push 前終審 r1-r2 折入,2026-07-11 深夜)

標準級終審(1 canaried sonnet+Codex 否決席)×2 輪,抓出四條真貨全折:
1. **[major×2 同源] `--incidents-only` 並非快速路**——反查/BFS 照跑只是不顯示,「降頻」宣稱落空(sonnet r1 與 Codex 獨立抓到;本篇原文即受害者)。修=incidents_only 真跳過反查+BFS(種子空集)。
2. **[major] delta-scoped trigger 的函式內部盲區**——編輯片段不含函式名時 tripwire 漏抓。修=探測範圍改「delta+±600 字元鄰域窗」(goldset 驗證噪音不回來)。
3. **[major] hook 端截斷假陰性兩形**——超長 old 吃掉 new(Codex 實測反例)→每段配額;地板配額×多段再尾切又吃尾段 edit(r2 抓到)→保底 300/總量不尾切。兩反例皆入測。
4. **[major,方法論] 評測可重現性**——活語料漂移使 P@8 隨「寫文件」±1pp 擺動。修=評測預設釘 goldset snapshot worktree(edit 面走 --repo——實測 impact 不吃全域 --vault);史帳記 eval_head+旋鈕。
**釘定快照後的誠實數字:P@8=0.6998(差門檻 0.0002,在測量粒度之下)**——不為湊小數點扭參數,判定交人裁(見計劃節點)。
r1 canary(升級工藝:真 context+真 blob)sonnet 首度漏抓→該輪判決不採信、majors 由編排者機械查證後折入;r2 換型加碼後 caught。

## v1.2 追記(同日,雙庫考卷調參)

Landmark 考卷暴露合約密集圖病灶後開 v1.2:**合約固定席收窄 hop≤1**(supersede r3「任意 hop」條款;hop2 合約降自由席,L≈0 被閾殺)+**動態閾 0.55→0.65**(名額制+快照後舊的必看視野代價消失:兩庫 24/30、7/7)。凍結後:工具鏈 P@8 0.6998→**0.7298(PASS 有餘裕,離開刀口區)**、固定席噪音 6→1;Landmark P@8 0.298→0.363、噪音 51→18、必看 7/7。誠實殘餘:LM 絕對值仍低於 0.70——主因前端檔無架構圖覆蓋(知識缺口,排序不可修)+合約樞紐 hop1 仍 18 席(dense hub 特性);v1.3 若做=補前端架構圖覆蓋,非再扭參數。

## ⚠ 更正註(2026-07-20 重跑實測):v1.2 追記的「必看 24/30」不可重現,實為 19/30

重跑釘定快照(`retrieval_eval.py --goldset ...`,即凍結配置 dyn_coef=0.65/direct_base=0.30/free_quota=10):

| 上段記載 | 重跑實測 | 判定 |
|---|---|---|
| 工具鏈 P@8 **0.7298** | **0.7298** | ✅ 重現 |
| 固定席噪音 **1** | **1** | ✅ 重現 |
| 兩庫必看視野 **24/30** | **19/30** | ❌ **不重現** |

同一句話裡三個數字、兩個精準重現,故非環境差異——是**帳記錯了**。

**錯法可追**:`24/30` 是 **v1.1(dyn_coef=0.55)** 的數字。本篇 summary 白紙黑字記著「0.65 係數⋯必看視野 **19/30**,棄」——v1.2 正是把係數調成 0.65,結果自然回到 19/30。v1.2 追記卻寫「舊的必看視野代價消失:24/30」,把 v1.1 的數字**沿用進了 v1.2 的句子**。

**實質影響(比數字本身重要)**:v1.1 當初**明確拒絕** 0.65,理由就是「砍到需要看的東西」(24/30→19/30)。v1.2 採用了 0.65,等於**推翻了那個拒絕**,卻在帳上記成「代價消失」。真實取捨是:精度 0.6998→0.7298(+3pp)**換掉 5 條必看視野**(24/30→19/30,亦即 28/30 起算共讓掉 9 條)。這個 trade-off 成不成立可以討論,但它**當時沒有被當成 trade-off 記下來**。

**歸類**:典型「逐輪折入漂移」——同 [[Issues/design-loop折入漂移_機械守衛]] 的 finding class(增量改寫時舊數字被沿用進新結論)。v1.2 是**追記段**、未經完整 loop,正是漂移最易發生處。

**未動參數**:本次只重跑對帳、未改任何凍結值。若判定 19/30 不可接受,那是 v1.3 的題目。

### 併同更正:本篇〈調參紀律〉的「改善路徑=更好的排序」已被證偽

上方〈調參紀律〉末句寫「改善路徑=**更好的排序(A3 in-degree/事件 frecency)** 而非更兇的閾值」。**這條建議已不成立,勿再依循**:

- **A3(權威度 in-degree)**——同日即被消融實測**殺除**(search train nDCG 0.875→0.847、edit P@8 0.700→0.653,雙面負增益),code 已刪留墓碑註。見 [[Projects/節點靜態先驗_調研]]。
- **A1.5(狀態降權)**——本庫零效果,旋鈕預設關。
- **A2(事件 frecency)**——只種了事件帳(`docs/.usage-log.jsonl`),語料不夠、未進分數。
- **整體重排**——2026-07-20 釘定快照實測:自由席**完美排序 +0.0pp**(已是最佳序)。見 [[Projects/主動影響幅度偵測_計劃]]。

**修正後的出路盤點**(三選一,尚未裁定):①重排自由席=✗(+0.0pp) ②補架構圖覆蓋=對「候選池空」有效,但**對本篇這 11 條無效**(它們已在池內) ③**重新權衡 `dyn_coef`/`free_quota` 本身=唯一對症**,代價即上述 +3pp 精度 ↔ 5 條必看視野。

亦即本篇原句「而非更兇的閾值」把唯一對症的路排除了,而它推薦的替代路後來全數落空。**此為典型折入漂移在「建議」層而非「數字」層的變形——數字錯了會被重跑抓到,建議錯了不會。**

## 相關模組

- [[Systems/retrieval-ranking]]
- [[Projects/檢索優化_計劃]]
- [[Verification/2026-07-11_檢索goldset評測]]
