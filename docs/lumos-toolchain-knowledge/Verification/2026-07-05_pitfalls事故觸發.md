---
type: verification
status: pass
created: 2026-07-05
updated: 2026-07-05
plan_refs:
  - "[[pitfalls事故觸發_計劃]]"
related:
  - "[[pitfalls事故觸發_計劃]]"
  - "[[主動影響幅度偵測_計劃]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:pitfalls ④ 事故觸發實作完成,671 passed 0 failed(branch feat/pitfall-incidents,TDD 4 task + opus 終審 Ready to merge 無 Critical/Important);真機 impact scripts/lumos incidents=0 不誤傷
  VERIFY:事故 pattern-trigger 落地——pitfall_when(glob/content-regex)→ _match_incident_triggers → cmd_impact incidents 段+去重(節點既結構又 trigger 命中只列 incidents)+讀被碰檔內容 → 復用 impact hook 注入;補 impact「跨檔按主題觸發」缺口
  KEY:去重承重牆經對照組正面驗證(X 無 trigger→在 indirect、有 trigger→只 incidents);健壯(壞 glob/regex 不 crash)、回歸(無 pitfall_when 架構圖 incidents 空);2 doc caveat 折入設計§2(content regex 避嵌套量詞 ReDoS、glob **/x 不match 根層)
---
# 2026-07-05 pitfalls 事故觸發(block ④)驗證

事故語料 pattern-trigger 實作完成並通過 opus whole-branch 終審(Ready to merge、無 Critical/Important)。落地 [[pitfalls事故觸發_計劃]](解 [[pitfalls-lint-integration_計劃]] ④)。

## 測試結果
- **`scripts/test_lumos.py` 671 passed, 0 failed**(基線 634[impact merge 後]→ +37:trigger 比對/incidents 段/去重對照組/hook 注入/回歸/smoke/main e2e)。
- TDD 4 task,每 task fresh subagent + sonnet review + fix;opus whole-branch 終審。

## 交付
1. **`pitfall_when` 慣例**:事故節點 frontmatter list,`glob:<path>`(比被碰檔路徑)/`content:<regex>`(grep 被碰檔內容)。
2. **`_match_incident_triggers`**(`scripts/lumos`):掃全圖 pitfall_when 節點比 glob/content,新建檔只 glob;壞 glob/regex 靜默跳過不 crash。
3. **`cmd_impact` incidents 段**:`--json` 頂層 `incidents:[{node,matched_by,contract,combo}]`+人讀「相關事故」節;**去重**——節點既 impact 結構命中(direct/indirect)又 trigger 命中→只列 incidents;讀被碰檔內容(OSError→空)。
4. **impact hook 注入 incidents**:三段(direct/indirect/incidents),空守衛三皆空才不注入。

## 與 impact 分工(釐清)
impact 撈「引用該檔的結構節點」(file-specific);④ 補「跨檔按主題 pattern-trigger」(如「凡碰 raw SQL 都看 N+1 事故」)。去重確保節點不在兩處重複。

## opus 終審實測驗過(無 Critical/Important)
- **去重承重牆非空洞**:`t_impact_incidents_indirect_dedup` 對照組(X 無 pitfall_when → BFS 撈成 indirect)vs 實驗組(X 有 trigger → 從 indirect 移除、只 incidents)——正面證去重生效。
- **健壯**:壞 glob(`[unterminated`)/壞 regex(`re.error`)/scalar pitfall_when/junk 前綴 皆守護不 crash(實測)。
- **回歸**:無 pitfall_when 真架構圖 → incidents 空、doctor 淨(pitfall_when 為容忍未知欄位)、direct/indirect/rc/fail-open/TTL 不變。
- **真機**:`impact --file scripts/lumos --repo . --json` incidents=[](本圖無 pitfall_when)。

## 誠實天花板 / 已知 Minor(折入設計 §2)
- trigger 人寫 GIGO;content-regex 假陽假陰。
- **ReDoS**:`content:` 嵌套量詞(`(a+)+$`)對大檔卡住(python re 無 timeout)→ 慣例警示用簡單 pattern。
- **`glob:**/x` 不 match 根層檔** → 用 `glob:*x*` 或兩條。
- 無機械 oracle「此刻真相關嗎」——trigger 命中=可能相關,Claude 動手前判。
