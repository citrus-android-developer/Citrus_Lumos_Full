---
type: verification
status: pass
date: 2026-07-31
feature: 全分支終審(對抗代碼審)提出 4 條 finding 的修復——C1 交付 CLI 自己叫人跑不存在指令/C2 等價驗證假守衛/C3 doctor 實跑收假紅/C4 reference.md 教危險前綴
valid_under:
  - "以本次修復當下的 scripts/lumos(24 支保留指令白名單)與 governance/eval/retrieval-goldset.json(30 條 search 查詢)為準;白名單或 goldset 改動需重跑 t_slim_gate 全批"
  - "C2 的等價驗證改跑在 docs/lumos-toolchain-knowledge(本 repo 自身 vault)的當下內容上,非固定快照——vault 內容變動不影響「兩版一致」這個等價性斷言本身(兩版讀同一份即時內容），但會影響「30 條全部非空」那條防退化守衛的具體通過與否"
  - "C1 的 --python 掃描候選數(21 條)是本次修復當下 scripts/lumos 原始碼的量測值,原始碼字串常數增減會改變這個數字,不影響 --python 模式本身的正確性"
revalidate_when:
  - "scripts/lumos 新增/移除頂層指令,或新增會印出已移除指令名稱的 warn()/print() 字串 → 重跑 t_slim_scan_python 確認 --python 模式仍抓得到"
  - "slim-gen.py 的排序/截斷邏輯改動 → 重跑 t_slim_gate_search_equivalence_counterfactual 確認反事實仍會翻紅"
  - "run_doctor 的例外處理方式改動(如加了 try/except 吞掉例外) → 重跑 t_slim_gate_doctor_nameerror_counterfactual 確認 NameError 仍逃不掉這道閘"
plan_refs:
  - "[[Projects/公開精簡版_實作計畫]]"
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:t_slim_* 全批(`python3 scripts/test_lumos.py -k slim`)78 checks 全綠,含本輪新增 7 個測試函式(t_slim_scan_python / t_slim_gate_search_equivalence_counterfactual / t_slim_gate_doctor_nameerror_counterfactual 三支全新 + t_slim_gate/t_slim_readme_assertions 兩支既有測試內加斷言)
  VERIFY:[[Projects/公開精簡版_實作計畫]] Task 5 交付通過全分支終審(對抗代碼審)後,審查員提出 4 條 finding(C1 blocker 等級×1、C2 blocker 等級×1、C3/C4 major 等級),本篇驗證其修復
  KEY:C1(交付 CLI 自己叫人跑不存在指令)——slim-scan.py 原本只掃 markdown,從未掃過產物 CLI 自己的字串常數;加 `--python` 旗標走 ast 模式只認 `ast.Constant` 的 str,先寫紅測試(拿 dist/scripts/lumos 當測資斷言掃到 init/update/self-audit)再實作;README 與 reference.md 各加一段揭露「doctor 有些建議指向未交付指令,看到請忽略,CLAUDE.md 相關檢查在本版無修復路徑是刻意的」
  KEY:C2(等價驗證假守衛)——原本跑在 mkvault() 的空 vault 上,兩邊 search 都回空字串,比 `'' == ''`;改跑在本 repo 自己的 docs/lumos-toolchain-knowledge(唯讀,不寫入),先驗證完整版在此 vault 上 30 條全部非空才往下做,並把這條「30 條全非空」也做成永久防退化斷言;補 spec:268 要求的反事實測試——竄改精簡版產物副本的排序(reverse `_rank_score_candidates` 回傳)後重跑同款比對邏輯,斷言 same 不再是 10/10
  KEY:C3(doctor 實跑收假紅)——原斷言 `d.returncode in (0, 1)` 收不住未捕捉例外(Python 例外未捕就是 exit 1,跟正常「發現 issue」撞號);加 `"Traceback" not in d.stderr` 與 stdout 收尾摘要行斷言(`架構圖健康`/`個 issue` 且含 `篇)`);補反事實測試——在精簡版產物副本的 `run_doctor` 開頭注入 `_totally_undefined_helper()`,斷言新版判準邏輯確實翻紅
  KEY:C4(reference.md 教危險前綴)——reference.md:27-39 表格 13 列 `python3 scripts/lumos <cmd>` 前綴,在 vendored 完整版的專案裡會誤呼叫到完整版 53 支指令(含 C1 那三支未交付指令會真的執行、可能動到專案層 CLAUDE.md);判斷同一缺陷貫穿全檔(共 37 處,不只表格 13 列),範圍擴大到全檔一併改成 `lumos`(全域安裝路徑),並修正 :18 對「前綴等價」的錯誤宣稱、明講 vendored 情境下不等價
  KEY:★C4 範圍決策(超出 finding 字面範圍,記錄理由)★——finding 明確只點名 reference.md:27-39 這 13 列,但 walkthrough 範例段(453-789 行)有相同的 37 處中另外 24 處同型缺陷;判斷「只修表格、留著範例段教錯前綴」會製造不一致的交付文字(讀者跟著範例段的指令照打一樣會踩雷),故一併修正全部 37 處,而非嚴格照 finding 字面停在 13 列
---
# 2026-07-31_公開精簡版終審修復

驗證對象:[[Projects/公開精簡版_實作計畫]] 交付通過全分支終審(對抗代碼審)後的 4 條 finding 修復(C1 blocker / C2 blocker / C3 major / C4 major)。逐條修復報告(改了什麼、紅→綠實際輸出、C2/C3 反事實測試怎麼證明閘真的擋得住)見 `.superpowers/sdd/公開精簡版_實作計畫/final-fix-report.md`(SDD 產出,非架構圖路徑,依計畫慣例落地於此)。

## 修復摘要

| finding | 等級 | 修復 | 新測試 |
|---|---|---|---|
| C1 | blocker | slim-scan.py 加 `--python` ast 模式掃產物字串常數 + README/reference.md 加誠實揭露段 | `t_slim_scan_python` |
| C2 | blocker | 第 3 道等價驗證從空 vault 換成本 repo 真 vault + 反事實測試(竄改排序必翻紅) | `t_slim_gate_search_equivalence_counterfactual` |
| C3 | major | 第 2 道 doctor 實跑斷言加 Traceback 排除 + stdout 收尾摘要斷言 + 反事實測試(注入 NameError 必翻紅) | `t_slim_gate_doctor_nameerror_counterfactual` |
| C4 | major | reference.md 全部 `python3 scripts/lumos` 前綴(37 處)改 `lumos`,修正 :18 等價宣稱 | (無新測試;純文件修正,人工核對 `grep -c "python3 scripts/lumos"` 回 0) |

跑法:`python3 scripts/test_lumos.py -k slim`(78 checks,全綠;★不要跑完整套件★,控制端另跑)。

## 相關

- 前一篇(本次修復針對的交付基線):[[Verification/2026-07-31_公開精簡版交付]]
- 系統筆記:[[Systems/slim-scan-掃描器]](C1 直接改動的系統)
