---
type: project
status: doing
created: 2026-08-02
updated: 2026-08-02
related:
  - "[[Systems/retrieval-ranking]]"
  - "[[Verification/2026-07-11_檢索goldset評測]]"
tags:
  - type/project
  - status/doing
summary: |-
  FLOW:`lumos search` 多詞查詢=整串子字串比對→0 命中不回退(retrieval-ranking 節點既有的已知技術債) → 修法=★fallback-only★:片語 0 命中且非 regex 且多詞時,退成各詞 OR 召回再交 BM25F 排序;`--any` 旗標先行、預設關 → 補多詞評測題 → 量測後才決定預設
  KEY:★根因不是「沒人想到多詞」,是量測工具自己的盲區傳染進量測集★——`build_goldset.py` 的 `search_pool(q)` 用同一道子字串比對組池,多詞查詢池為空 → `if not pool:` 靜默寫「標註跳過」→ goldset 30 題★29 題是單詞★,要修的缺陷從頭到尾不在尺上
  KEY:★fallback-only 的零回歸是機械可證不是量出來的★:實測現有 goldset 30 題★全部回 >0 候選★,回退條件(0 命中)永不觸發 → 那 30 題行為一行不變。若改成「永遠 OR」則不成立(IDF 以候選集為語料,擴召回會擾動既有排序)
  KEY:★順序修正★:原本想「先補評測集再動 code」——★做不到★,候選池撈不出東西就沒得標。正確序=實作(藏旗標後)→組池→標註→量測→才決定預設
  KEY:★組池不得只從新系統撈★(pooling bias:讓被考的人自己出考卷)——三來源聯集:legacy 片語 + 新 `--any` top-k + ★逐詞分別 grep 的聯集★(獨立來源,偽陰性才量得到)
  KEY:★選題兩類都要★:現在回 0 的(測修好沒)~10 題 + 現在有命中的(測有沒有弄壞)~5 題。只收前者=又建一個只看得見自己想看的東西的評測集,與現況同錯
  KEY:★快照要處理★:goldset 釘 snapshot_commit=285d429(2026-07-10),但 build_goldset 對現況 vault 組池;今天直接跑會與舊 30 題不同基準。裁定=開 285d429 的 worktree 加建(新舊可比),不走 v2 重標
  KEY:★尺本身先復驗★:架構圖自記 +106.8% 查無來源、重跑 +99.6%;24/30 實為 19/30。★拿它判斷新改動之前先確認它現在跑出什麼★
  DEP:scripts/lumos cmd_search(_cand_hits/_cand_detail/matches)｜governance/eval/build_goldset.py search_pool｜governance/eval/retrieval_eval.py
  PRIOR-ART:①最小解在既有層——不動 BM25F(它本來就吃多詞)、不動候選以外任何東西,只在「0 命中」這一條死路上加回退;②世界解過=IR 的 OR/AND 回退與 pooling 是標準做法(TREC 式多系統組池避免 pooling bias),雙評+裁決本專案既有;③裁定=borrow-design,零新依賴
---
# 檢索多詞回退（計劃）

## 問題（有實例）

```
$ lumos search "BM25 檢索 搜尋 排序 欄位加權"
(候選 0)
```

不是架構圖裡沒有，是**那五個字沒有連在一起出現過**。候選階段是整串子字串比對：

```python
needle = nfc(term).lower()
matches = lambda s: needle in nfc(s).lower()
```

**BM25F 本來就會處理多詞**（`impact --diff` 用整段 hunk 當 query，運作正常）。壞掉的是**前面那道撈候選的關卡**，而它 0 命中時**不回退**（`Systems/retrieval-ranking` 已登記的技術債）。

## ★根因：量測工具的盲區傳染進了量測集★

`build_goldset.py`：

```python
pool = search_pool(q)      # legacy + ranked,兩邊同一道子字串比對
if not pool:
    sheet.append("- (無候選——查詢在 vault 無命中,標註跳過)")
```

多詞查詢池為空 → 那題靜默變空 → **goldset 30 題有 29 題是單詞**。

**要修的那個缺陷，從頭到尾不在尺上。** 這與 2026-08-02 同日的另外兩例（合約條文只枚舉一種失效形態、實驗指標跟著 payload 變）是同一個形狀。

## 修法：fallback-only

**只在片語 0 命中時**才退成各詞 OR 召回，再交給 BM25F 排序。

### 為什麼零回歸是「可證」不是「可量」

實測：**現有 goldset 30 題全部回 >0 候選** → 回退條件永不觸發 → **那 30 題行為一行不變**。

這是機械論證，不需要跑評測。若改成「永遠 OR」就不成立——`N/df/avgdl 以候選集為語料`，擴召回會擾動既有排序。

### 代價落在哪

第二趟掃描**只在第一趟 0 命中時發生**——也就是今天本來就什麼都拿不到的查詢。**順利路徑零成本。**

## 順序（★上一版想錯了★）

原本打算「先補評測集，再動 code」。**做不到**——候選池撈不出東西就沒得標。

正確順序：

1. **實作 `--any`，預設關**（不是行為變更，只是多一條可量測的路）
2. **組池**：三來源聯集
3. **標註**：沿用既有雙評（claude/codex）＋不一致深讀裁決寫 `why`
4. **量測**
5. **才決定**要不要變預設

## 待辦與未定

- [ ] M1：`--any` 實作 + 測試（本階段，trivial 級可跳 design-loop）
- [ ] M2：goldset 復驗（先確認尺現在跑出什麼）
- [ ] M3：多詞題組池 + 標註（285d429 worktree）
- [ ] M4：量測 → 裁定預設

**未定**：`--any` 要不要同時支援「AND 優先、OR 墊底」的階梯（先試全詞都中的節點，再放寬）？v1 不做，先看資料。

## ★Landmark 真庫驗證（2026-08-02，284 篇，全程唯讀）★

10 題現實查詢**先寫死再跑**（避免看了資料才挑好看的題目）。

### 結果一：缺陷不是邊角案例

**10/10 在預設下全部 0 命中。** 每一個現實會打的多詞查詢，今天都拿不到東西。

### 結果二：`--any` 的第一名，7/10 明顯正確

`POS 作廢 未同步` → `Issues/2026-07-18_POS作廢未同步_對帳真差異_北村102`（幾乎逐字命中）；
`點數 交易 死鎖` → `Issues/2026-07-27_PointComplete死鎖_CustTransfer全表掃`；
`背景排程 心跳 監控` → `Systems/背景排程總覽`；以及簡訊、LINE、稽核、升降等四題。

3 題不理想：`對帳 假差異 自癒`（相關但不是最該回的）、`發票 補登 排除`、`續會 門檻 計算`。

### ★結果三：最差那題不是排序爛，是查詢用詞錯——而工具沒告訴我★

逐詞覆蓋：`★續會:0★  門檻:50  計算:35`。

**「續會」在整個 Landmark 架構圖裡一篇都沒有**（是我猜錯他們的用語）。剩下兩個超常見詞把結果洗走。

### ★由此發現 OR 回退最大的風險（原本沒想到，是實測逼出來的）★

> **回退之後，搜尋幾乎不可能再回 0——「查無」這個訊號會消失。**

今天「0 筆」至少有時候代表「這個概念不在架構圖裡」。加了 OR 之後，
**你永遠會拿到看起來合理的東西**，而那可能只是常見詞的噪音。
★這比「排序不夠準」嚴重得多★：排序不準你看得出來，訊號消失你看不出來。

**M1.5 的內容**：回退觸發時，把逐詞覆蓋印到 stderr：

```
（多詞回退:整串片語全庫無命中,改用各詞 OR 召回）
  逐詞覆蓋: ★續會:0★  門檻:50  計算:35
```

看到 `續會:0` 就當場知道是用詞錯了，而不是架構圖沒有——**把消失的那個訊號用另一種形式還回來**。

**本輪刻意不順手做**：M1 範圍就是 `--any`，而同日已因趕工出過一次假綠（見下）。M1.5 自己帶測試。

## ★M1 過程中自己犯的假綠（第 12 次，記為教訓）★

`t_search_multiword_fallback_is_opt_in_and_only_on_zero` 的第三條斷言
（「片語真的存在時，回退絕不能觸發」）**第一版是假綠**。

現場用「丙丁」當片語——**單一 token、中間沒有空白**——於是 `len(_fb_terms) > 1`
這道閘直接判假，**OR 路徑一次都沒被執行**。把 fallback-only 守衛拿掉（改成永遠 OR）的
翻紅釘**照樣全綠**。

這正是 [[Systems/測試假綠形態]] 第④型「現場走不到被測分支」，**而且是這條測試自己犯的**。

修法：現場改成**真的是多詞**的片語「丙 丁」，並另建一個只含「丙」的節點——
沒有那個可被誤撈的節點，這條斷言就沒有鑑別力（第⑤型）。修完翻紅釘才真的紅。

## ★M1.5 落地（2026-08-02）★

回退觸發時，把逐詞覆蓋印到 **stderr**：

```
（多詞回退:整串片語全庫無命中,改用各詞 OR 召回;逐詞覆蓋 ★續會:0★  門檻:50  計算:35）
  ★有詞在全庫 0 命中(標 ★)——多半是查詢用詞與架構圖用語不一致,先換那個詞再查,
    別把結果當「架構圖沒有」★
```

- **算在預檢那圈**：它本來就讀了每個檔、也做過同樣的 code 排除，多幾次子字串測試幾乎免費，不另開一圈
- **走 stderr**：與既有「已隱藏 N 筆作廢結果」同慣例，不污染 `--json`／`--files-only` 的機器可讀輸出

### ★實作時抓到的漂移（值得記）★

預檢與主迴圈是**兩份實作**，第一版預檢**沒套用「隱藏作廢節點」的過濾** → 覆蓋數比實際搜得到的多
（Landmark 實測 `門檻 55` vs 搜尋顯示的 `50`，差在 7 筆 superseded）。

★**覆蓋數若 > 實際，使用者會以為「這個詞有東西」而其實搜不到——誤導方向與本功能要解的問題正好相反。**★

因此測試裡放了一條**強斷言**：`覆蓋數必須與逐詞搜尋完全一致`（拿掉過濾就只有這條翻紅，精準）。

**驗證**：9 條斷言含 3 條「現場成立」前置；兩個翻紅釘（拿掉輸出／拿掉過濾）各驗一個方向。

## ★M2 完成：尺是可信的，錯的是被沿用的兩個數（2026-08-03）★

以釘定語料 `snapshot=285d429`、`eval_head=8680ac1` 重跑：

| | legacy | ranked | 提升 |
|---|---|---|---|
| search 整體 (n=30) | 0.5411 | 0.8556 | **+58.1%** |
| train (n=21) | 0.5990 | 0.8748 | +46.0% |
| **held (n=9)** | 0.4062 | 0.8109 | **+99.6%**（MRR 1.0） |

edit P@8 fusion **0.7298**（勝 bm25 0.7208、graph 0.7298）；必看視野 **19/30**；**gate 7/7 PASS**。

★**與 2026-07-20 那次重跑逐項相同**★ → **尺本身穩定可重現**。

所以我先前說的「尺沒校準、要先復驗」**講重了**——真相是：**尺一直是準的**，
對不上的只有 `+106.8%` 與 `24/30` 這兩個★被沿用進句子、卻不對應任何一次實跑★的數。

順手把 `Systems/retrieval-ranking` 掛著的「**尚無人認領**」待辦結案：三處
（Systems／計劃 §6／Verification）一次性統一到本組凍結值，code 版本記入
`governance/eval/retrieval-eval-history.jsonl`。Verification 的原始數字**保留不改**
（它記錄的是當時量到什麼），只加更正註指向現況值。

**保留掛著的**：`legacy` 基線同時漂移（0.5317→0.5411）至今未查明。
ranked 側的差異可用其後併入的 A1 型別先驗解釋，legacy 側不能。

### 對 M3/M4 的意義

尺可用，**可以直接進 M3（補多詞題）**，不需要先修尺。

## ★M3/M4：多詞題標註與量測（2026-08-03）★

原始資料全部存 `governance/eval/multiword/`（題目、池、兩位評分者、裁決、最終標籤、結果），**可自行覆核**。

### 做法

- 在 goldset 快照 `285d429` 開 worktree（102 篇），**與舊 30 題同基準**
- **實驗組 10 題先寫死再跑**（sha256 存證），**對照組 5 題**才依「現在就有命中」的條件挑
- 候選池 **三來源聯集**：legacy 片語（全 0）＋`--any` top-10 ＋**逐詞出現次數 top-3（不經 BM25F，獨立來源）** → 125 筆
- **雙評**：Claude ＋ **Codex（跨家族）**，同一張標註表
- **分歧 43 筆交乾淨 opus 裁決**（要求實讀節點、不准取中間值、可判第三值）

### 評分者一致性

逐項一致 **82/125 = 65.6%**；分歧 43，其中**強衝突（差 2 分）6 筆**。
裁決結果：採 Claude 28、採 Codex 14、**判出第三值 1**。

★**分歧成群出現時，往往是題目的問題不是系統的問題**★——6 筆強衝突有 4 筆擠在 M08，
裁決者獨立指出：「引用座標」在本庫是**既有術語**＝節點 body 指向 code 的 `file:line`（refcheck／G1），
而 Codex 讀成「節點間引用結構」。裁決者還發現**同一個「座標」在本庫有三種語意**
（節點→code 的 file:line／SARIF 行號對齊 diff／節點引用結構）。

### 量測結果（k=5）

| | 無回退 | 有回退 |
|---|---|---|
| nDCG@5 | **0.0** | **0.7668** |
| MRR | 0.0 | **0.95** |
| P@5 | 0.0 | 0.86 |

**第一名品質**：標 2（必看）**7/10**；標 ≥1（至少有用）**9/10**。

### ★兩題偏低的，都不是排序的錯★

| 題 | nDCG | 原因 |
|---|---|---|
| M06 架構圖 同步 閘 | 0.311 | ★裁決者標為「查詢歧義」★——可指 pre-commit 強迫碰架構圖的閘，也可指節點間同步漂移的守衛 |
| M10 突變 測試 存活 | 0.356 | ★M1.5 的逐詞覆蓋當場顯示 `突變:0`★——本庫寫 mutation 不寫「突變」，是我用詞錯 |

★**這是很強的一致性訊號**★：兩個弱點都被**系統自己或雙評流程當場標示出來**了，
不是靜默的排序失敗。（**此拆解是事後的**，主要指標仍以全 10 題的 0.7668 為準。）

### 對照組：零回歸的實證

5 題現在就有命中的多詞查詢，`--any` 前後結果**逐檔完全相同**（`lumos canary`／`anchor approve`／
`impact hook`／`mutation score`／`guard kill`）——與「回退條件永不觸發」的機械論證一致。

### ★誠實邊界★

- **pooling bias**：候選池半數來自 `--any` 自己的 top-10，被它排前面的一定被標到；
  「更好的系統會找到、但它沒找到」的節點**可能不在池裡**，偽陰性量不到。
  第三來源（逐詞次數 top-3，不經 BM25F）是獨立來源，但**不完全乾淨**。
- 分數是**池內相對值**，不是絕對召回品質。
- n=10、單一快照、單一 vault。

### ★人裁結果：翻（2026-08-03）★

**支持翻**：現況是「每個現實多詞查詢都回 0」；回退後 nDCG 0→0.77、MRR 0.95、
第一名必看 7/10；零回歸機械可證且對照組實證；M1.5 已把「查無」訊號用逐詞覆蓋還回來。
已查無任何機器消費端依賴「回 0」語意。

**保留意見**：pooling bias、n=10。翻的話要同時加 `--no-any` 逃生口，並更新 M1 那條
「預設維持 0」的測試。

## ★落地：預設翻開（2026-08-03 人裁）★

- `lumos search` 的多詞回退 **改為預設開**；`--no-any` 是逃生口；`--any` 保留為相容旗標（no-op）
- **M1 那條測試的 ② 從「預設必須維持 0」改成「`--no-any` 必須維持 0」**
  ——★方向相反，是刻意的行為變更，不是把斷言放寬★，docstring 已寫明
- 新增翻紅釘：拿掉 `--no-any` 接線 → 逃生口那條翻紅（精準只紅一條）

### 交付版同步

`slim/scripts/lumos` 是 `slim-gen.py` 的產生物，已重生並更新交付包
（`Citrus_Lumos`，本次 diff **只有 `scripts/lumos` 一支**，其餘檔案與 `skills/` 逐檔比對皆同步）。

交付包端到端真跑：安裝 → `lumos search "對帳 自癒"` 回結果並印逐詞覆蓋 →
`--no-any` 回 0 → 卸載 rc0、無殘留。

### 還沒做的

- **M1.5 的逐詞覆蓋只在回退觸發時印**。片語命中時不印——目前認為對，但沒驗過
  「使用者會不會希望片語命中時也知道各詞覆蓋」。
- 新的 10 題**還沒併進 `retrieval-goldset.json`**（它釘 `snapshot_commit`、有 train/held 切分，
  併入要處理切分與 IDCG 基準；本次先以獨立檔＋獨立腳本量測）。併入是待辦。
- `--any` 的階梯版（AND 優先、OR 墊底）仍未做，v1 的資料還不足以判斷值不值得。
