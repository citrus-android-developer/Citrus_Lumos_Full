---
type: project
status: done
created: 2026-08-07
updated: 2026-08-07
tags:
  - type/project
  - status/done
summary: |
  KEY:hook 紅燈殘餘 6 筆收尾——pre-flight 勘誤:僅 3 筆真斷鏈(B 群,D2 家族訊號全覆蓋)、3 筆=已 direct 被閾砍且保底未觸發(A 群);關鍵推論=光補鏈救不動(補進來照樣被砍),綁定約束=保底觸發過保守 → 兩件並進:S1 候選生成+人裁補鏈(零 LLM,D1 實證標的=train E20)、S2 觸發放寬至「free 內 direct<N 即補至水位」(考卷 A/B 裁決)
  KEY:`lumos link-candidates <file>` 唯讀恆 rc0,不寫檔;人裁後補標準引用=正常架構圖編輯,反查自然吃到;考卷誠實界線=通用規則全庫生成+實質關係人裁,labels 不動,補鏈後數字變動如實記
  KEY:r1 panel(2026-08-07):3 caught/s1 missed(d 型;該席今日三 loop 三漏);13 條全折 max=blocker——N 網格 {2,3}(N=1 時新謂詞 no-op)/前案候選明文重裁不靜默改/補入數 need 公式/D1 複合詞判準/D2 四邊型單跳+剝後綴 LCS/CLI 互斥文法+schema 封閉/重釘 snapshot 協定
  KEY:刻意不做——LLM 候選(v2+反證預篩)/Drift 符號錨(另立)/自動寫入(人閘省不掉,pbt-oracle 教訓)
  FLAG:DECISION
related:
  - "[[Projects/結構訊號補鏈D3_計劃]]"
---
# 連結缺失補全_計劃

> 緣起:[[Projects/hook必看召回修復_計劃]] 收斂後殘餘——held 必看 miss 6 筆(r1 勘誤:3 筆真斷鏈+3 筆已 direct 被閾砍,見前置實驗),週閘 hook P@8 0.6944 距 0.70 差 0.0056,多救一筆即翻綠。第三帖藥:候選生成+人裁補連結。

PRIOR-ART: ① 最小解層級——新增**唯讀候選生成子命令**(觀測,不動任何排序/閘),補的連結=正常架構圖編輯(引用寫進節點 body,反查自然吃到,零新消費端);姿勢 borrow 自家 [[Projects/pitfalls網搜補漏_計劃]](候選→人放行→進架構圖,人閘省不掉)。② 世界解:TLR(LLM 連結恢復,arXiv 2509.05585/2508.12232)——LLM 在迴圈=不可重算,僅離線候選+人裁姿勢合法;v1 甚至不用 LLM,兩個決定論偵測器夠用(見實驗)。Drift 符號錨(路徑#符號@sha)=另一題(錨定格式升級),本計劃不做。③ Growth test 三問:事故=週閘紅燈+驗屍 6 筆死法③(機械留痕);非風格偏好;既有機制(quote-check/refcheck/seat-check/reverse-lookup)全是消費端無一做候選挖掘——蓋不住,新讀取面成立。④ 裁定=borrow-design。

## 前置實驗(2026-08-07;★pre-flight 勘誤後以 live impact 反查為準,原表 3 筆誤判已更正★)
殘餘 6 筆 miss 經 `lumos impact --json` direct 清單重放,實況分兩群:
| 群 | 節點 | 實況 |
|----|------|------|
| A(已 direct 被閾砍,3 筆) | Systems/retrieval-ranking、Projects/主動影響幅度偵測_實作計畫、Verification/2026-07-05_主動影響幅度偵測 | 帶反引號引用早存在(原實驗腳本只掃正向訊號、沒查「是否已 direct」——歸因錯誤由 pre-flight 以現碼重放抓回);同題另有 direct 存活 → 零 direct 觸發條件不成立,保底未出手 |
| B(真斷鏈,3 筆) | Verification/2026-07-10_檢索排序v1←`governance/eval/retrieval_eval.py`、Projects/主動影響幅度偵測_計劃←`scripts/hooks/claude/impact-hook.py`、Projects/pitfalls事故觸發_計劃←`scripts/hooks/claude/impact-hook.py` | 零機械可抓引用;皆有 D2 家族訊號(鄰居引同檔/標題共詞幹);目標檔映射明列=驗收可獨立重放(r1 折入) |

另 train 卷 E20(Systems/convergence-evidence-gate←`governance/autonomous_loop/cross_audit.py`)=純文字提及無反引號,D1 可抓。
→ **關鍵推論(改寫本計劃範圍)**:B 群補鏈後變成「direct 但 L≈0」——照現行閾值照樣被砍,且同題有 direct 存活時保底不觸發 → **光補鏈救不動 held 數字**。綁定約束=保底觸發條件過保守(hook必看召回修復 刻意保守的已知代價,當時如實記錄)。故本計劃兩件並進:S1 補鏈工具(架構圖衛生+D1/E20 型)+S2 觸發條件放寬(考卷 A/B 裁決)。

## 規格

### S2 保底觸發放寬(治 A 群+補鏈後的 B 群;考卷 A/B 裁決)
- **前案合約候選衝突,明文裁決(r1 折入,兩席 blocker)**:[[Projects/hook必看召回修復_計劃]] 合約候選清單提名過「rescue 僅零 direct 時觸發」——候選未蓋章,本案即該候選的**晉升/改寫程序**:以同款考卷 A/B 重裁;S2 轉正則前案候選清單同工作內同步改寫並互引留痕,S2 敗訴則該候選就地晉升蓋章(guard 流程)。不靜默覆蓋。
- 放寬案=「存在被閾砍/截的 direct 且 **free 集 direct 數(排除 pinned;截斷後最終集)** < 水位 N」即觸發;**補入數=need=max(0, N−free_direct_count),取 dropped[:need]**(r1 折入:原「觸發謂詞一行級」低估——謂詞+補入數兩處連動,否則補成 free_direct+N 超出水位)。rescued 標記/第三桶語意不變;零 direct 舊行為=特例向下相容。
- **N 網格明文(r1 折入,blocker:N=1 時新謂詞≡舊謂詞,B 臂跑不到新分支)**:train 網格候選 **N∈{2,3}**(A 臂=現行 N=1 零 direct 觸發;B 臂=水位謂詞+網格 N);轉正後預設寫死選定值。
- 風險如實:多 direct 過閾(≥N)不觸發;「k<N direct 存活」場景補 N−k 席可能引噪——考卷裁決(gate 同前案:held P@8 護欄 ±0.02+Σmust ≥+1;輸=觸發條件回退)。
- 測試(r1 折入,三類 direct 分開覆蓋):「1 free direct 存活+被砍 direct 存在→補至水位 N−1 席」「free direct ≥N 不觸發」「pinned direct 不計入 free 計數」「dropped<need 補盡即止」「N>2 網格」。

### S1 `lumos link-candidates [<code-file>] [--json] [--all-goldset <goldset.json>]`(唯讀,恆 rc0;輸入壞損 rc2)
- CLI 文法(r1 折入):`<code-file>` 與 `--all-goldset` **互斥且必居其一**(皆缺/皆給=rc2);goldset 路徑為**參數非寫死**(vendored 工具不得耦合本專案 eval 治具路徑——消費端專案無此檔;檔不存在/JSON 壞損=rc2)。
- `--json` schema 封閉(r1 折入):`{"file": str, "candidates": [{"node": str, "signal": "plain-text|neighbor-family|both", "evidence": [str], "via": [str]}]}`;無候選=空陣列(人讀模式印「無」);evidence=D1 證據行摘錄(可空)、via=D2 經由節點(可空)、both=兩欄皆填。
- **D1 純文字提及**:節點可見文字(=`_search_visible_lines(include_code=False)` 既有組合——同時剝 fence 與 inline-code;★勿用 `_strip_fences_text`,它明文保留 inline-code,r1 折入★)含目標檔**完整路徑或完整檔名**;或含**去副檔名詞幹且 stem ≥6 字元且含 `_` 或 `-`**(複合詞天然有辨識度:retrieval_eval/impact-hook ✓,config/system 這類單詞 ✗——r1 折入:原「非常用詞」無母體無停用表、舉例還自相矛盾,整條規則以複合詞判準取代)→ 候選,標 `signal: plain-text`+證據行摘錄(實證標的:train 卷 E20 型)。
- **D2 鄰居家族**(r1 重寫——原基底/加分邊型不一致+CJK 詞幹無演算法+泛用後綴誤報實測 11 檔):
  - 候選母體=direct 節點集 D 的**單跳雙向鄰居**,邊型統一四種:related/body-wikilink/plan_refs/verified_by(不分基底與加分、不遞迴)。
  - 篩選(OR):(a) **檔名 stem 先剝節點型後綴**(`_計劃`/`_實作計畫`/`_調研`/日期前綴)後,與某 d∈D 的同剝 stem 之**最長公共連續子字串 ≥4 CJK 字元**(演算法明文=逐對 LCS-substring;剝後綴防「_實作計畫」這類泛用尾綴系統性誤報——r1 實測 11 檔同尾) (b) 與某 d∈D 經 plan_refs/verified_by **單跳**相連。
  - → 候選,標 `signal: neighbor-family`+`via: [經由的 d]`。
- 已是 direct(有 inline-code 引用)的節點不出候選(去重);兩訊號同中標 both。
- 輸出:逐候選 `{node, signal, via/evidence}`;無候選印「無」。**不寫任何檔**——補引用=人裁後的正常架構圖編輯。
- `--all-goldset`:對 goldset edit 卷全部 file 掃一輪出彙總(驗收用)。

### 人裁協定(省不掉的閘)
- 候選清單交人逐筆判「這節點真的管這支檔嗎」;確認的由人(或人授權的編輯)把標準引用(`路徑` inline-code)寫進節點 body 適當段落。
- **考卷誠實界線**:候選由通用規則對全庫生成、逐筆按實質關係人裁——非為過卷定向補;goldset labels 不動;補完重跑考卷的數字變動=真實改善的量測,如實記(含「此輪補鏈後題集語料已變,與舊分比較需註明」)。

## 審計修正紀錄
- **pre-flight(2026-08-07,機械排乾)**:①前置實驗表 3/6 筆歸因錯——原腳本只掃正向訊號沒查「已否 direct」,live impact 反查揭穿(A 群早有反引號引用);連帶推翻「補鏈即救」假設,S2 觸發放寬入範圍 ②驗收「6 筆全出現」與去重規則自相矛盾→改 B 群 3+E20 ③「54+1」底數誤,現為 55 子命令 ④`link-candidates` 與既有 `links` 字面近,消歧:links=節點連出邊,link-candidates=code→節點補鏈候選。

## 審計修正紀錄(r1)
- **r1(2026-08-07,panel:3 sonnet(通才/謂詞邊界/整合)+Codex 外家;canary 3 caught / **s1 missed**(d 型裸檔名;本席今日三 loop 三漏,席×型別帳持續攢);去重 13 條全折 max=blocker,全機械證實免辯方)**:
  - [blocker] N=1 時水位謂詞≡舊謂詞=治 A 群宣稱 no-op、B 臂跑不到新分支(3 席)→ N 網格 {2,3} 明文+A/B 臂定義修正。
  - [blocker] 前案合約候選「rescue 僅零 direct」被靜默改寫(2 席)→ 明文晉升/改寫程序(考卷重裁+互引留痕)。
  - [blocker] feature_tag 幽靈欄位(s2 canary 抓針時連帶指出 feature/ tag 家族已凍結、母體空集)→ canary 撤除;D2 分群改 LCS-substring 明文。
  - [major] 補入數公式沒跟上水位語意(dropped[:N] 應為 dropped[:need])(3 席)→ need=max(0,N−free_direct_count) 明文+touchpoint 修正(非一行級)。
  - [major] D1「非常用詞」無母體+舉例自相矛盾(lumos/eval 已被 ≥6 擋)(2 席)→ 改複合詞判準(stem 含 _ 或 -);「可見文字」指名 _search_visible_lines(include_code=False),勿用保留 inline-code 的 _strip_fences_text。
  - [major] D2 基底/加分邊型不一致+鏈無跳數上界+「_實作計畫」泛用尾綴 11 檔實測誤報(3 席)→ 四邊型統一單跳+剝後綴 LCS。
  - [major] CLI 文法自相衝突(positional 必填 vs --all-goldset)+vendored 耦合 eval 路徑+JSON schema 不封閉(3 席)→ 互斥文法+goldset 路徑參數化+schema 封閉。
  - [major] 前置實驗表無目標檔映射=驗收不可獨立重放(2 席)→ 補映射欄。
  - [major] 補鏈後評測語料座標未定(快照釘定 vs live)(Codex)→ 重釘 snapshot_commit 協定。
  - [major] 緣起句殘留「6 筆全屬死法③」與勘誤矛盾(Codex)→ 修。
  - [major] 測試謂詞未限定 free direct(pinned 混入可讓錯謂詞過測)(Codex)→ 三類 direct 分開覆蓋。

## 落地後發現(2026-08-08,設計逃逸如實入帳)
- **重釘協定不完備(loop 收斂後才撞上)**:依 r1 折入的「補鏈後重釘 snapshot」實跑——P@8 0.713→0.447、固定席噪音 77 條:新快照拉進 7/20 後全部未標節點,依「未標=噪音」規則淹沒指標。病根=goldset labels 凍結於舊語料,**語料前進必須配套標註刷新**,r1 panel 與編排者皆未察(進逃逸帳)。裁定=快照回退 285d429 維持可比性;標註刷新另立計劃;補鏈四筆之效果以 live 反查 direct 名單機械可證(工具目的=架構圖衛生,本就不繫於考卷分數)。

## 合約候選清單(收斂提名,候選≠已標——蓋章走 guard 流程)
- link-candidates 恆唯讀不寫檔(自動寫入=maker bias 閉環,人閘不可拆)。
- goldset 路徑恆為參數,vendored 工具不得寫死本專案治具路徑。
- S2 敗訴則「rescue 僅零 direct」前案候選就地晉升蓋章。

## 刻意不做(記帳防回鍋)
- LLM 語意候選(v2 再議,須 pitfalls-gapfill 式反證預篩)。
- Drift 符號錨/`@sha` 過期偵測(另一題,錨定格式升級另立計劃)。
- 自動寫入節點(人閘省不掉——pbt-oracle 教訓:自動生成+自動採納=maker bias 閉環)。
- 排序/閾值一概不動(hook必看召回修復 剛收斂,天平不重調)。

## 實務隱患
- **併發/效能**:唯讀單進程掃 vault(~240 節點×regex),一次性;無熱路徑。
- **資源**:純檔案讀+stdout;無連線/鎖。
- **[self-governance]**:純觀測工具,無擋人面;誤報候選=人裁時多看一眼即棄,成本=人時,D1 詞幹下限+D2 家族條件即為抑噪。
- **[prod-irreversible]**:不適用(不寫檔)。

## 驗收線
- S1 測試:`t_link_candidates`(D1 命中/inline-code 已引不出候選/短詞幹不洪水/D2 家族命中/雙訊號 both/rc 合約);對 B 群 3 筆+E20 跑掃描:4 筆全在候選清單(通用規則召回已證訊號,已 direct 的 A 群依去重規則不出現——與規格一致)。
- S2 測試:`t_impact_direct_rescue` 補兩案(1 direct 存活+被砍存在→補至水位/direct ≥N 不觸發);考卷 A/B(harness 沿 hook必看召回修復:top8/dump-rows/env knob):gate=held P@8 不劣 ±0.02 ∧ Σmust ≥+1,輸=觸發條件回退。
- 端到端(r1 折入,評測語料座標明文):eval 預設把 vault 釘在 goldset `snapshot_commit`——人裁補鏈寫進現況架構圖**不會**出現在舊快照,預設重跑量不到變動。協定=補鏈 commit 後**重釘** goldset `snapshot_commit` 至該 commit(沿考卷既有「題集變更需註明」慣例,vault_note 記 corpus revision),S2 的 A/B 兩臂在**同一新快照**上跑(凍結語料可比性保住);數字如實記 history,不設「必須翻綠」為通過條件。
