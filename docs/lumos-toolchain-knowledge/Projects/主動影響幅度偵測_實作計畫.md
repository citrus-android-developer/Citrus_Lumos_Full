---
type: project
status: done
created: 2026-07-05
updated: 2026-07-07
tags:
  - type/project
  - status/done
related:
  - "[[主動影響幅度偵測_計劃]]"
plan_refs:
  - "[[主動影響幅度偵測_計劃]]"
summary: |-
  FLAG:DECISION
  KEY:[2026-07-19 擴充]hook 注入尾附棧別效能三問——lumos impact --ranked --json 輸出帶 stack_questions(目標檔命中 kt/cs/vue/sql 且非測試檔;冷卻 incidents-only 快速路不帶),hook 只格式化不持有表(單源=scripts/lumos _STACK_PERF_QUESTIONS);僅提問無節點時不接「判上列節點」收尾指令(答非所問修);見[[pitfalls棧別效能追問_計劃]]
  KEY:「主動影響幅度偵測」的 TDD 實作計畫(設計經 9 輪 design-loop 收斂,見 [[主動影響幅度偵測_計劃]]);兩塊=lumos impact 原語(scripts/lumos)+ PreToolUse hook(scripts/hooks/claude/impact-hook.py);逐 task 真單元測試釘正確性(設計層剩餘精度缺口交 TDD)
  KEY:任務序=T1 argparse+rc 協定 → T2 code→node 反查 → T3 直接+合約 → T4 BFS+seen → T5 via+direction → T6 core_refs 跨repo葉 → T7 排序+json/人讀輸出 → T8 config → T9 hook 過濾+呼叫 → T10 hook TTL → T11 hook 注入+fail-open → T12 註冊+端到端
  KEY:算法權威在設計節點 §2(反查/BFS/via/合約)、§3(hook);本計畫每 task 給可執行測試 + 實作要點 + 指令,細節指回設計 §
  DECISION:實作用 subagent-driven-development(fresh subagent/task + 逐 task review);merge 前跑全 test_lumos.py + 端到端 smoke
  DEP:[[主動影響幅度偵測_計劃]]
  TEST:未開工(計畫定稿)
---
# 主動影響幅度偵測 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.
> **設計權威**:[[主動影響幅度偵測_計劃]](§1 架構 / §2 lumos impact 原語 / §3 hook / §4 天花板 / §5 測試 / 審計修正紀錄)。本計畫每 task 的算法細節以設計 § 為準,不重述,只給可執行測試 + 實作要點 + 驗證指令。

**Goal:** 實作 `lumos impact` 原語 + PreToolUse hook,動手前主動算出將被改動 code 的 downstream 影響節點集、推到 Claude 眼前(lumos 關聯節點自動修復的偵測地基)。

**Architecture:** `lumos impact --file <code> --json --repo <root>` 純記憶體即時算「直接(body inline-code 反查)+ 間接(env.edges BFS)」影響清單、出機器可讀 `--json`;PreToolUse hook 攔 Edit/Write/MultiEdit → 過濾 code → TTL 節流 → 呼叫原語 → `additionalContext` 注入(不擋)。lumos 只算、Claude 只判、hook 只搬運節流。

**Tech Stack:** python3 標準庫(零依賴,同 scripts/lumos);Claude Code hooks(PreToolUse、hookSpecificOutput.additionalContext);既有 lumos 原語 `load_vault`/`_refcheck_scan`/`env.edges`/`link_target`/`env.resolve`/`extract_contracts`/`IRREVERSIBLE_RE`/`as_list`。

## Global Constraints

- **零第三方依賴**:純 python3 標準庫(scripts/lumos 慣例)。
- **vault-free / fail-open**:任何錯誤絕不擋 Claude;rc 協定 `0`=成功 / `3`=vault 找不到(hook 印 debug) / 其他非 0=內部錯(hook 純靜默)。
- **不改 `env.edges`/`load_vault`/`Note` 建構**(全 lumos 共用核心 infra);impact 的反查靠**呼叫時重讀盤**餵 `_refcheck_scan`,via 二次反查只在 impact BFS 內做。
- **code→node 反查只認 body inline-code**(`core_refs` 是跨 repo 節點指針非 code 路徑,不掃)。
- **路徑一律 repo-relative**(hook 傳絕對路徑 → `os.path.relpath(abs, repo_root)` 再比對)。
- **`--json` 是機器可讀契約**(下游自動修復消費):欄位型別/可空/枚舉/presence 依設計 §2 schema 定死。
- **測試進 `scripts/test_lumos.py`**(既有測試框架,現 500+ passed);每 task 綠燈才進下一 task。
- **hook 不進 anchor**(比照現有 `scripts/hooks/claude/*.py` 現況)。

---

### Task 1: `lumos impact` argparse skeleton + rc 協定

**Files:** Modify `scripts/lumos`(argparse subparsers 區 + 新 `cmd_impact`);Test `scripts/test_lumos.py`

**Interfaces:**
- Produces: `cmd_impact(file, repo=None, depth=2, as_json=False) -> int`(回 rc);argparse `impact` 子命令(`--file` 必填 / `--repo` / `--depth` 預設 2 / `--json`)。

- [ ] **Step 1: 失敗測試** — `impact` 子命令存在、`--file` 必填、非架構圖目錄回 rc 3。
```python
def t_impact_cli_skeleton():
    # 非 vault 目錄 → rc 3(vault 找不到)
    with tempfile.TemporaryDirectory() as d:
        rc = run_lumos(["impact", "--file", "x.py", "--repo", d, "--json"])
        assert rc == 3, f"非架構圖應 rc3, got {rc}"
    # 缺 --file → argparse rc 2
    assert run_lumos(["impact", "--repo", "."]) == 2
```
- [ ] **Step 2: 跑測試確認 FAIL**（`impact` 未定義 → argparse error / rc≠3）。Run: `python3 scripts/test_lumos.py -k impact_cli_skeleton`
- [ ] **Step 3: 實作** — 加 `impact` subparser + `cmd_impact` 骨架:先 `find_vault(repo or cwd)`,找不到 `return 3`;找到則(暫)`return 0`。rc 協定見設計 §3。
- [ ] **Step 4: 跑測試 PASS**。
- [ ] **Step 5: Commit** `feat(impact): lumos impact 子命令骨架 + rc 協定(rc0/3)`

---

### Task 2: code→node 反查(body inline-code,重讀盤,路徑規範化,top_dirs)

**Files:** Modify `scripts/lumos`(新 `_impact_reverse_lookup`);Test `scripts/test_lumos.py`

**Interfaces:**
- Consumes: `_refcheck_scan(text, repo_root)`(既有);`load_vault`。
- Produces: `_impact_reverse_lookup(rel_code_path, env, repo_root) -> list[node_rel]`(body inline-code 命中 `rel_code_path` 的節點 rel 清單)。

- [ ] **Step 1: 失敗測試** — fixture vault:節點 A body 含 `` `scripts/lumos` ``、節點 B 不含;反查 `scripts/lumos` 只回 A;`core_refs` 寫 code 路徑**不**被反查到;絕對路徑輸入正規化後仍命中。
```python
def t_impact_reverse_lookup():
    env, repo = make_fixture_vault({
        "Systems/A.md": "body 提到 `scripts/lumos` 的用法",
        "Systems/B.md": "body 無關",
        "Systems/C.md": "---\ncore_refs: scripts/lumos\n---\ncore 節點",  # core_refs 不算 code 反查
    })
    hits = _impact_reverse_lookup("scripts/lumos", env, repo)
    assert "Systems/A.md" in hits
    assert "Systems/B.md" not in hits
    assert "Systems/C.md" not in hits, "core_refs 不該進 code 反查(r7-F2)"
```
- [ ] **Step 2: 跑測試 FAIL**。
- [ ] **Step 3: 實作** — 見設計 §2.1:重讀每個 `.md` 的 text 餵 `_refcheck_scan(text, repo_root)`,收命中 `rel_code_path` 的節點;輸入若絕對路徑先 `os.path.relpath(abs, repo_root)`;`_refcheck_scan` 的 `top_dirs` 靜默過濾照舊(§4 天花板 5)。**不掃 core_refs**。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): code→node 反查(body inline-code,重讀盤,路徑規範化)`

---

### Task 3: 直接關聯 + 合約偵測(INVARIANT/IRREVERSIBLE 兩軸,combo,DEBT→null)

**Files:** Modify `scripts/lumos`(新 `_impact_contract(note) -> (contract:str|None, combo:bool)`);Test。

**Interfaces:**
- Consumes: `extract_contracts`(掃 INVARIANT/DEBT)、`IRREVERSIBLE_RE`(`scripts/lumos:1184`)、COMBO 偵測(`:725`)。
- Produces: `_impact_contract(note) -> (contract, combo)`;`direct[]` 項 `{node, hit:"body-inline-code", contract, combo}`。

- [ ] **Step 1: 失敗測試** — INVARIANT 節點回 `("INVARIANT", ...)`;IRREVERSIBLE 節點回 `"IRREVERSIBLE"`(即使無 INVARIANT——證走了獨立 RE);同時有兩者取 `IRREVERSIBLE`;`★INVARIANT★★COMBO★` → `combo=True`;純 `★DEBT★` → `(None, False)`。
```python
def t_impact_contract_two_axes():
    assert _impact_contract(note_with("KEY:★INVARIANT★ x [test:t]"))[0] == "INVARIANT"
    assert _impact_contract(note_with("KEY:★IRREVERSIBLE★ y [rollback:decisions]"))[0] == "IRREVERSIBLE"
    assert _impact_contract(note_with("KEY:★IRREVERSIBLE★ y\nKEY:★INVARIANT★ x [test:t]"))[0] == "IRREVERSIBLE"
    assert _impact_contract(note_with("KEY:★INVARIANT★ ★COMBO★ z [test:t]"))[1] is True
    assert _impact_contract(note_with("KEY:★DEBT★ w")) == (None, False)
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §2.4:`extract_contracts` 拿 INVARIANT(DEBT 映 `None`);另用 `IRREVERSIBLE_RE` 掃 summary 拿 IRREVERSIBLE;取最高 `IRREVERSIBLE`>`INVARIANT`>`None`;combo = INVARIANT 行含 `★COMBO★`。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): 合約偵測兩軸(INVARIANT/IRREVERSIBLE)+combo+DEBT→null`

---

### Task 4: 間接關聯 BFS(hop 1..depth,seen cycle guard,邊集)

**Files:** Modify `scripts/lumos`(新 `_impact_bfs`);Test。

**Interfaces:**
- Consumes: `env.edges`(`out_e`/`in_e`,`scripts/lumos:251-263`)。
- Produces: `_impact_bfs(direct_nodes, env, depth) -> list[(node, hop, from_node, is_backlink)]`(不含 direct 節點;seen 種入全 direct)。

- [ ] **Step 1: 失敗測試** — 直接節點 D,related 鄰居 N(hop1);depth=2 出兩層;**A↔B 環:A 是 direct → B hop1,A 不重入**;depth=1 只出 hop1。
```python
def t_impact_bfs_cycle_and_depth():
    env, _ = make_fixture_vault({
        "S/A.md": "---\nrelated:\n  - \"[[B]]\"\n---\n`scripts/x`",
        "S/B.md": "---\nrelated:\n  - \"[[A]]\"\n---\nb",
    })
    out = _impact_bfs(["S/A.md"], env, depth=2)
    nodes = [o[0] for o in out]
    assert "S/B.md" in nodes and "S/A.md" not in nodes, "A 是 direct,不得沿環重入 indirect(r8-F4)"
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §2.3:BFS 種子=全 direct 節點、`seen` 先種入全 direct;沿 `out_e`+`in_e` 雙向;hop=距最近 direct 的距離 1..depth;記每筆 `(node, hop, from_node, is_backlink)`(is_backlink = 該筆從 `in_e` 到達;雙向皆可達取 backlink,r8-F7)。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): 間接 BFS(hop 1..depth + seen cycle guard + 雙向邊)`

---

### Task 5: via 標記(二次反查,outlink/backlink 讀對端,body-wikilink fallback)+ direction

**Files:** Modify `scripts/lumos`(新 `_impact_via(frontier, dest, is_backlink, env) -> str`);Test。

**Interfaces:**
- Consumes: `link_target()`+`env.resolve()`(wikilink→rel);`n.fields`。
- Produces: `_impact_via(...) -> via:str`(∈ related/verified_by/plan_refs/body-wikilink);`direction` = backlink/outlink(由 is_backlink)。

- [ ] **Step 1: 失敗測試** — **outlink**:frontier F 的 `related:[[G]]` → via `related`;**backlink**(關鍵 r9-F1):H 的 `verified_by:[[F]]`,從 F 反查到 H(in_e)→ 須讀 **H.fields** 得 via `verified_by`(**不是讀 F**);frontier/dest 都無 frontmatter 連結但 body 有 → `body-wikilink`。
```python
def t_impact_via_both_directions():
    env, _ = make_fixture_vault({
        "S/F.md": "---\nrelated:\n  - \"[[G]]\"\n---\n`scripts/x`",
        "S/G.md": "g",
        "S/H.md": "---\nverified_by:\n  - \"[[F]]\"\n---\nh",  # H→F,對 F 是 backlink
    })
    assert _impact_via("S/F.md", "S/G.md", False, env) == "related"      # outlink 讀 frontier
    assert _impact_via("S/F.md", "S/H.md", True, env) == "verified_by"   # backlink 讀 dest(H),非 F
```
- [ ] **Step 2: FAIL**（若誤讀 frontier,backlink case 會 miss→body-wikilink,測試掛）。
- [ ] **Step 3: 實作** — 見設計 §2.3「到達路徑」:outlink 讀 `frontier.fields`、backlink 讀 `dest.fields`,找含指向對端的 wikilink 的欄位;比對用 `link_target`+`env.resolve`;找不到 → `body-wikilink`。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): via 二次反查(outlink/backlink 讀對端,r9-F1)+direction`

---

### Task 6: core_refs 跨 repo 葉(cross_repo/no_expand,不 BFS 進)

**Files:** Modify `scripts/lumos`;Test。

**Interfaces:** Produces: 直接節點的 `core_refs`(`as_list(n.fields.get("core_refs"))`)→ indirect 項 `{node, hop:1, via:"core_refs", direction:"outlink", from, cross_repo:True, no_expand:True, contract:None, combo:False}`。

- [ ] **Step 1: 失敗測試** — 直接節點有 `core_refs: core-knowledge/systems/rule` → 影響清單含該葉、標 `cross_repo/no_expand`、**不因它 KeyError**(它不在 env.notes)。
```python
def t_impact_core_refs_leaf():
    env, _ = make_fixture_vault({"S/A.md": "---\ncore_refs: core-knowledge/systems/rule\n---\n`scripts/x`"})
    res = _impact_collect("S/A.md", env, depth=2)  # 或整合在 cmd_impact
    leaf = [r for r in res["indirect"] if r.get("cross_repo")]
    assert leaf and leaf[0]["node"] == "core-knowledge/systems/rule" and leaf[0]["no_expand"] is True
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §2.3 core_refs bullet:直接節點的 core_refs 值當跨 repo 葉直接輸出,`as_list` 統一 str/list,**不 BFS 進**(避 `env.notes[dest]` miss)。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): core_refs 跨 repo 葉(cross_repo/no_expand,不展開)`

---

### Task 7: 排序 + `--json` schema 輸出 + 人讀輸出

**Files:** Modify `scripts/lumos`(組裝 `cmd_impact` 完整輸出);Test。

**Interfaces:** Produces: `--json` 依設計 §2 schema(`{file, direct[], indirect[]}`,欄位 presence 定死:combo 每筆必有、cross_repo/no_expand 僅 core_refs 葉有);排序=合約優先→直接優先→hop 升序。

- [ ] **Step 1: 失敗測試** — `--json` 輸出可 `json.loads`、schema 欄位齊(direct 有 hit/contract/combo 無 hop/from;indirect 有 hop/via/direction/from/combo);合約節點排 direct 之首;空影響集 → `{file, direct:[], indirect:[]}` rc 0。
```python
def t_impact_json_schema_and_sort():
    out = run_lumos_capture(["impact","--file","scripts/lumos","--repo",FIX,"--json"])
    d = json.loads(out)
    assert set(d) == {"file","direct","indirect"}
    for x in d["direct"]:  assert set(x) >= {"node","hit","contract","combo"} and "hop" not in x
    for x in d["indirect"]: assert set(x) >= {"node","hop","via","direction","from","contract","combo"}
    # 合約節點排最前
    if d["direct"]: assert d["direct"][0]["contract"] in ("IRREVERSIBLE","INVARIANT") or all(x["contract"] is None for x in d["direct"])
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §2 schema + §2.4 排序;人讀格式見設計 §2「輸出範例」。`--json` 走 `json.dumps(ensure_ascii=False)`。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): 排序 + --json schema + 人讀輸出`

---

### Task 8: config `.lumos/impact.json`(depth/ttl_min,缺檔預設)

**Files:** Modify `scripts/lumos`(新 `_impact_load_config`);Test。

**Interfaces:** Produces: `_impact_load_config(repo_root) -> {"depth":int,"ttl_min":int}`(缺檔/壞檔 → `{"depth":2,"ttl_min":20}`,fail-open)。

- [ ] **Step 1: 失敗測試** — 有 `.lumos/impact.json {"depth":3}` → depth 3、ttl 補預設 20;無檔 → 2/20;壞 json → 2/20 不拋。
```python
def t_impact_config():
    with tempfile.TemporaryDirectory() as d:
        os.makedirs(d+"/.lumos"); open(d+"/.lumos/impact.json","w").write('{"depth":3}')
        assert _impact_load_config(d) == {"depth":3,"ttl_min":20}
        open(d+"/.lumos/impact.json","w").write("{bad")
        assert _impact_load_config(d) == {"depth":2,"ttl_min":20}
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §3 config:讀 `.lumos/impact.json`(對齊 lint.json 慣例),merge 預設,任何錯 fail-open 回預設。`--depth` CLI 覆寫 config。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact): .lumos/impact.json config(depth/ttl,fail-open 預設)`

---

### Task 9: PreToolUse hook — 過濾 + 呼叫原語 + rc 處理

**Files:** Create `scripts/hooks/claude/impact-hook.py`;Test。

**Interfaces:**
- Consumes: hook stdin payload(`{tool_name, tool_input:{file_path}, session_id, cwd}`);`lumos impact` CLI。
- Produces: hook 主流程(read payload → 過濾 → subprocess `lumos impact --file --repo --json` → rc 處理)。

- [ ] **Step 1: 失敗測試** — 非 code(`.md`/docs/-knowledge/node_modules)→ 放行零動作;code 檔 → 呼叫 lumos impact;讀 `payload["tool_input"]["file_path"]`(巢狀);rc 3 → 印 debug 不注入;rc 其他非 0 → 純靜默。
```python
def t_impact_hook_filter_and_rc(monkeypatch):
    assert hook_decide({"tool_input":{"file_path":"docs/x-knowledge/a.md"}}) is None  # 架構圖檔放行
    assert hook_decide({"tool_input":{"file_path":"a.py"}}) is not None                # code 觸發
    # file_path 巢狀讀取
    assert extract_path({"tool_input":{"file_path":"x.py"}}) == "x.py"
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §3:matcher Edit/Write/MultiEdit;CODE_EXTS 抄自 `check-graph-sync.py`(20 版,註同源);路徑排除比照 `EXCLUDE_PATH_CONTAINS`+`EXCLUDE_FILENAMES`;`--repo` 取 `$CLAUDE_PROJECT_DIR`/payload cwd;rc 協定處理。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact-hook): 過濾(CODE_EXTS/EXCLUDE)+呼叫原語+rc 協定`

---

### Task 10: hook TTL 冷卻窗(marker,gettempdir,sha1,time.time,清理)

**Files:** Modify `scripts/hooks/claude/impact-hook.py`;Test。

**Interfaces:** Produces: `_ttl_should_inject(session_id, file_abs, ttl_sec) -> bool`(標記檔 `gettempdir()/lumos-impact-<sid>/<sha1[:16]>`,`< ttl` 壓、`>= ttl` 重注 + 更新)。

- [ ] **Step 1: 失敗測試** — 首次 True + 建標記;窗內第二次 False;標記時戳倒推 > ttl → True(復活);寫標記前清 >24h session 目錄。
```python
def t_impact_hook_ttl():
    sid, f = "sess1", "/abs/x.py"
    assert _ttl_should_inject(sid, f, ttl_sec=1200) is True
    assert _ttl_should_inject(sid, f, ttl_sec=1200) is False   # 窗內壓
    _backdate_marker(sid, f, 2000)                              # 倒推 2000s
    assert _ttl_should_inject(sid, f, ttl_sec=1200) is True     # 窗外復活
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §3 TTL:`tempfile.gettempdir()`、`hashlib.sha1(file_abs.encode()).hexdigest()[:16]`、內容 `time.time()` float、`now-float(content)<ttl_sec` 判、惰性清 mtime>24h 目錄(best-effort try/except)。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact-hook): TTL 冷卻窗(gettempdir/sha1/time.time/惰性清理)`

---

### Task 11: hook 注入(additionalContext)+ 指令文字 + fail-open

**Files:** Modify `scripts/hooks/claude/impact-hook.py`;Test。

**Interfaces:** Produces: 影響集非空且該注入 → 印 `{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":<清單+指令>}}`;空集合/任何錯 → 不注入、放行。

- [ ] **Step 1: 失敗測試** — 非空影響集 → stdout 是合法 JSON、含 `hookSpecificOutput.additionalContext`、含指令文字;空影響集 → 無輸出;lumos 缺席(subprocess FileNotFound)→ 不拋、不注入(fail-open)。
```python
def t_impact_hook_inject():
    out = hook_run_with_impact({"direct":[{"node":"S/A","hit":"body-inline-code","contract":"INVARIANT","combo":False}],"indirect":[]})
    j = json.loads(out); assert j["hookSpecificOutput"]["hookEventName"]=="PreToolUse"
    assert "動手前" in j["hookSpecificOutput"]["additionalContext"]
    assert hook_run_with_impact({"direct":[],"indirect":[]}) == ""   # 空集合不注入
```
- [ ] **Step 2: FAIL**。
- [ ] **Step 3: 實作** — 見設計 §3 注入方式 + 指令文字;fail-open(subprocess 例外/rc 其他非 0 → 靜默 `sys.exit(0)`);永不 block/改 permissionDecision。
- [ ] **Step 4: PASS**。
- [ ] **Step 5: Commit** `feat(impact-hook): additionalContext 注入 + 動手前分析指令 + fail-open`

---

### Task 12: hook 註冊 + 端到端 smoke

**Files:** Modify hook 註冊處(`scripts/hooks/claude/` 註冊機制,參 `check-graph-sync.py` 怎麼註冊到 settings);`scripts/templates/graph-discipline.md`(若需);Test 端到端。

**Interfaces:** Consumes: 全部前 task。Produces: PreToolUse 註冊 impact-hook;`lumos --debug` 實測注入時機(設計 §3 r5-F2)。

- [ ] **Step 1: 失敗測試** — 端到端:對本 repo 造一個 body 引用 `scripts/lumos` 的暫節點,`lumos impact --file scripts/lumos --repo . --json` 回非空 direct 含該節點;清理暫節點。
```python
def t_impact_end_to_end():
    # 用真 repo vault,注入暫探針節點 → impact 抓到 → 清理
    probe = "docs/lumos-toolchain-knowledge/Systems/_impact_probe.md"
    write(probe, "body `scripts/lumos`")
    try:
        d = json.loads(run_lumos_capture(["impact","--file","scripts/lumos","--repo",".","--json"]))
        assert any(x["node"].endswith("_impact_probe") for x in d["direct"])
    finally: os.remove(probe)
```
- [ ] **Step 2: FAIL**（註冊前 hook 不生效;或探針測試先驗原語)。
- [ ] **Step 3: 實作** — 註冊 PreToolUse hook(比照現有 claude hook 註冊、**不進 anchor**);`claude --debug` 手動實測注入時機 + stderr 備援(設計 §3 r5-F2);更新設計節點 `TEST:` 與 `verified_by`。
- [ ] **Step 4: PASS** + 全量 `python3 scripts/test_lumos.py`。
- [ ] **Step 5: Commit** `feat(impact-hook): 註冊 PreToolUse + 端到端 smoke;架構圖回填 verified_by`

---

## 落地回填
實作完成寫 `Verification/2026-..._主動影響幅度偵測.md`,`plan_refs: "[[主動影響幅度偵測_計劃]]"` 回指設計節點(意圖鏈,doctor Check 4);設計節點 `TEST:` 更新、`verified_by` append。
