---
type: project
status: done
created: 2026-07-31
updated: 2026-08-16
tags:
  - type/project
  - status/done
related:
  - "[[Projects/公開精簡版_計劃]]"
  - "[[Systems/lumos-cli-lifecycle]]"
  - "[[Systems/slim-get-一行安裝]]"
  - "[[Systems/slim-uninstall-一行卸載]]"
  - "[[Systems/slim-install-安裝器]]"
summary: |-
  KEY:本節點=[[Projects/公開精簡版_計劃]] 的**實作計畫**(spec 歸該節點,本節點只管怎麼做)。分支 `feat/public-slim-handoff`
  KEY:五個 Task 皆 TDD;測試一律進 `scripts/test_lumos.py` 的 `t_slim_*`(stdlib only、`python3 scripts/test_lumos.py` 自動收集 `t_` 前綴)
  KEY:★三支新腳本留在本 repo(工具),交付物是「生成出來的那包」落 `dist/`(gitignore)★——分支不是交付物
  KEY:★首批必接的三條裸項★(spec 放行時未結清):①S6-3 子模式判準三來源(Task 5 實測 export/search 分類)②掃描器第五種形態=裸中文散文型 token(Task 1)③T-5 需合成 fixture 才驗得到迴圈註冊該砍(Task 2)
  KEY:交付載體=凍結的新 repo(使用者裁定);push 那一下由使用者自己做(對外動作+需決定擁有者=公司非個人)。★交付目的地(2026-08-11 Enzo 補記,原本只存在人腦)=`https://github.com/citrus-android-developer` 下的 **Citrus_Lumos**★——本 repo 沒有該 remote,`dist/` 也不進版控,所以★工具鏈這邊 push 不會更新交付包★:每次改到白名單/範本/CLI 行為,都要人重新生成 `dist/` 再推 Citrus_Lumos
  KEY:2026-07-31 補追加 Task 6(分支終審前最後一個 Task,無獨立 SDD brief,由使用者直接指派)——給精簡版加一行安裝(`slim/get.sh`)/一行卸載(`slim/uninstall.sh`,固定落點 `~/.lumos-slim`),並修分支終審代碼審抓到的兩條 minor(`slim-scan.py` 候選視窗置中、`slim/skills/lumos-project-notes/{SKILL.md,reference.md}` 補掃描斷言覆蓋)。新增 [[Systems/slim-get-一行安裝]]、[[Systems/slim-uninstall-一行卸載]] 兩個 Systems 節點,落地見 [[Verification/2026-07-31_公開精簡版一行安裝卸載與代碼審修復]]
  KEY:2026-07-31 補追加 Task 7(第二輪分支終審代碼審抓到的兩條殘留 minor,無獨立 SDD brief,由使用者直接指派)——minor-1:Task 6 修的視窗置中其實還是「事後 `.find()` 猜位置」,不是「`scan_line()` 真實命中位置」,改成 `scan_line()` 自己回報 `pos`;minor-2:`uninstall.sh` 補「連續跑兩次」冪等回歸測試(調查後判定腳本本身不需改,只補測試鎖行為)。落地見 [[Verification/2026-07-31_公開精簡版代碼審第二輪minor修復]]、報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-7-report.md`
  KEY:★2026-07-31 補追加 Task 8——推翻 [S3] 原裁定,由使用者直接指派★:原裁定明文禁「注入/更新任何 CLAUDE.md」,使用者部分推翻——**開放的是附加,禁的仍是覆蓋**。新增 install.sh 第③步:在執行目錄的 CLAUDE.md 檔尾,用專屬 sentinel `<!-- LUMOS-SLIM:START/END -->`(刻意與完整版 `LUMOS:GRAPH-DISCIPLINE` 不同名)append-only 附加架構圖標籤教學(summary 符號/KEY 行合約性前綴/合約鏈括號/frontmatter 欄位/進場三步,五段內容逐字對齊 `skills/lumos-project-notes/reference.md`〈summary 欄位〉節);uninstall.sh 對稱新增第④步移除。三條寫入紀律(只准附加/冪等/可移除)皆有紅→綠回歸測試釘住,`t_slim_install_no_project_touch` 換形狀(原「worktree porcelain 為空」不再成立,改「除 CLAUDE.md 外無其他新增/修改檔案 + sentinel 外 byte-equal + .git/config 不變」)。[[Projects/公開精簡版_計劃]] [S3] 同步改寫,記明原裁定/推翻理由/新裁定範圍刀。報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-8-report.md`
  KEY:★2026-07-31 補追加 Task 9——[S3] 裁定第三次變更,推翻 Task 8「只准附加」,由使用者直接指派★:Task 8 開放的「附加、兩套規則並存」被發現本身是問題(完整版那段更早出現、自稱優先級最高,接手者的 Claude 會先讀到它、照著它引用的一堆本包沒交付的指令去撲空)。新裁定:專案若有完整版 `LUMOS:GRAPH-DISCIPLINE` 區塊,**整段移除、原地換成精簡版區塊**(不是搬到檔尾);移除前策展吸收完整版裡仍然有效的內容(核心原則/進場三步/合約鏈/可逆性標記/新補的 regen 重生標記/frontmatter 欄位補齊到 9 個)進新檔 `slim/claude-block.md`(61 行靜態範本,取代 Task 8 那版寫死在腳本裡的 heredoc);拿掉依賴已移除指令(design-loop/code-loop/pitfalls/spec-trace/signoff/init/update)才有意義的段落。完整版原文 base64 編碼備份進精簡版區塊自己的 HTML 註解裡(不新增檔案、`~/.lumos-slim` 刪掉也還原得了),`uninstall.sh` 對稱讀出位元組級還原。`scripts/slim-gen.py` 組包清單同步補 `claude-block.md`(吸取上一輪漏 get.sh/uninstall.sh 的教訓)。五組紅→綠回歸測試釘住(有完整版區塊原地取代/uninstall 位元組級還原/冪等備份不漂移/沒有完整版區塊插檔首標題後/策展正確性)。過程中順手修正既有測試一個真實 bug:`_slim_make_pkg_at` 系列測試呼叫 install.sh/get.sh 未設 `cwd`,舊行為下無害、換新邏輯後有真的動到本 repo 自己 CLAUDE.md 的風險,已補 `cwd` 隔離。[[Projects/公開精簡版_計劃]] [S3] 新增〈裁定第三次變更〉節,記明三次裁定演進、策展範圍、備份機制設計理由、已知風險(完整版若自稱自動更新,其他人跑更新流程會裝回來,兩邊來回覆蓋——已知並接受)。報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-9-report.md`
  KEY:★2026-07-31 補追加 Task 10——端到端實測(在控制端實跑,非推論)抓到真 bug,由使用者直接指派★:`slim/uninstall.sh` 拿 `~/.lumos-slim/scripts/lumos` 當 bin sha256 比對的唯一基準,但 `~/.lumos-slim` 只有走 `get.sh`(一行安裝)才會存在——README 也在教的另一條路(直接 clone 交付包、跑包內 `install.sh`)不會建立它,比對基準必然缺失,舊版把這個失敗當一票否決(`exit 2` 中止整支腳本),**CLAUDE.md 的完整版還原完全沒機會執行**,接手者從此卸載不掉;錯誤訊息「這可能是你自己的東西」在這情境下也是誤導。修法兩件事:①`install.sh` 裝 bin 時多寫一份身分證 manifest(`~/.local/share/lumos-slim/manifest.json`,含 `bin_sha256`),不放使用者專案、不依賴 `~/.lumos-slim` 存在,manifest 缺失才退回舊版 `~/.lumos-slim/scripts/lumos` 備援比對;②`uninstall.sh` 四個清理步驟(bin／skill／`~/.lumos-slim`／CLAUDE.md sentinel)改成各自獨立判斷、各自執行、互不阻擋,全部跑完才彙總報告、決定 rc(比照 `lumos doctor --ci` 三段式:0=全成功/本來沒裝,1=安全性跳過非硬錯誤,2=真正錯誤);錯誤訊息同步拆成「基準缺失」與「內容真的不符」分開講。TDD:先寫兩支新測試(不經 get.sh 直接跑 install.sh 場景、bin 拒絕不擋 CLAUDE.md 還原場景)+ 改版一支既有測試(rc 語意與步驟獨立性斷言),`git stash` 還原舊版腳本重跑確認 8 checks 翻紅,回補修復後轉綠。收尾端到端實跑:重新生成 `dist/`,clone 一份 Landmark 到 `/tmp`(用完即刪,未寫回原路徑),直接跑 `dist/install.sh` 再跑 `dist/uninstall.sh`(不經 get.sh),斷言 CLAUDE.md 與安裝前 byte-equal、`git status --porcelain` 全空——全部通過。`slim/README.md`〈怎麼移除〉〈`~/.lumos-slim` 是什麼〉兩節同步改寫。報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-10-report.md`
  KEY:★2026-08-01 補追加 Task 11——注入目標守衛,真實事故驅動(咬過兩次,由使用者直接指派)★:兩位不同的子代理驗證時忘記先 `cd` 進交付包 clone,直接在本 repo(lumos 工具鏈來源 repo)根目錄跑 `dist/install.sh`,當場改掉了來源 repo 自己的 `CLAUDE.md`(兩次都當場發現、`git checkout` 還原)。`slim/install.sh` 原本完全沒檢查 `$(pwd)` 是不是合理的注入目標。加三層守衛:①不像專案根(無 `.git`/無 `docs/*-knowledge/`/無既有 `CLAUDE.md`,或 `$(pwd)`==`$HOME`)→拒絕②目標同時具備 `skills/lumos-project-notes/`+`scripts/lumos`+`scripts/templates/graph-discipline.md` 三件套(lumos 工具鏈來源 repo 才有的組合)→拒絕——★這層才真正擋得住那兩次事故,第一層擋不住(事故現場本身就有 .git/CLAUDE.md/docs/*-knowledge)★③動手前不論①②過不過都先印出目標絕對路徑。逃生閥 `--here` 繞過①②。獨立審計(五問 rubric,含真的竄改暫存副本驗證可證偽性)抓到一個真稻草人:重現事故測試原版直接對 `slim/install.sh` 原始檔以 repo 根為 cwd 執行,先撞上與守衛無關的套件完整性檢查提前 rc=2 退出,把第二層判斷式整段刪掉舊版測試依然通過;改用真的生成 `dist/install.sh` 執行後修正,複測確認翻紅(過程中一度意外寫髒本 repo 自己的 `CLAUDE.md`,已 `git checkout` 復原)。六支新測試 `t_slim_install_guard_*`,既有五支測試補 `.git` fixture 標記通過新守衛(行為不退化)。收尾在本 repo 根目錄實跑重新生成的 `dist/install.sh`,精確重現兩次事故的情境:rc=2、`git status --porcelain CLAUDE.md` 全空。報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-11-report.md`
  KEY:★2026-08-01 補追加 Task 12——分支終審第四輪代碼審兩條殘留 finding,由使用者直接指派★:①MAJOR:Task 11 新增的 `t_slim_install_guard_here_bypasses`(驗 `--here` 繞過第二層)fixture 是空目錄,永遠不會滿足第二層三件套判定,第二層被整段刪掉這條測試照樣綠,只驗到繞過第一層(★這是這個專案第八次撞到「測試存在但沒在驗它宣稱要驗的」★)。修法:換成與 `t_slim_install_guard_rejects_source_repo` 同款的真來源 repo fixture,分兩段跑(先證明不帶 `--here` 確實被第二層擋下,再證明帶 `--here` 放行);竄改第二層 `if false` 複測確實翻紅(3/5 checks)。②MINOR:`slim/install.sh:207`(改前)算 CLAUDE.md 寫入路徑用素樸 `$(pwd)`,與第一/三層用的 `$(pwd -P)` 不一致,cwd 經 symlink 進來時稽核印出路徑與實際寫入路徑字串可能對不上。修法:改用已解過 symlink 的 `$TARGET_CLAUDE_MD`;新增 `t_slim_install_symlink_cwd_audit_path_matches_write_path`(用 `env PWD=<symlink 路徑>` 模擬互動 shell cd 進 symlink 再呼叫腳本的真實情境逼出分歧),竄改回素樸 `$(pwd)` 複測確實翻紅(2/6 checks)。額外檢查其他守衛測試(六支 `t_slim_install_guard_*` + `t_slim_uninstall_refuses_foreign_bin`):只有 `here_bypasses` 有這個毛病。`python3 scripts/test_lumos.py -k slim` 225 checks 全綠;重新生成 `dist/` 實跑重現不帶 `--here` rc=2 / 帶 `--here` rc=0 兩種情境,測完 `git checkout -- CLAUDE.md` 還原。報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-12-report.md`
  KEY:★★交付端反向漂移——來源端不知道交付端做了什麼(2026-08-12 推 delguard 時碰巧比對才發現,無任何機械守衛)★★—`Citrus_Lumos` 已經★往前跑★:①`install.py` 從「做三件事」變「做四件事」=多一套 **git hooks 分發**(放 pre-commit/post-commit 進專案 `scripts/hooks/`+寫 `.gitignore` sentinel,只複製不設 `core.hooksPath`)②`uninstall.py` 對應的第⑥步+**兩個守衛修**:`root-guard`(2026-08-11,卸載器從子目錄跑會靜默拆掉整個 repo 的 hook 守衛)、`worktree-guard`(2026-08-12,從 linked worktree 跑會拆掉主工作區的)③交付端獨有 `hooks/`、`docs/`、`WINDOWS-NOTES.md`④README 有 **48 處** hooks 敘述。★而本 repo 的 `slim/` 範本完全沒有這些★——`dist/` 生出來的 README 還寫著「不設任何機械強制、**不裝任何 hook**」「安裝器做三件事」「不會改 .git/config」,★整檔覆蓋會把 hooks 功能連同兩個守衛修一起炸掉★。2026-08-12 本次改走**最小外科更新**(只換純生成物 `scripts/lumos`+改三份範本的支數/delguard 條目,`install.py`/`uninstall.py`/`hooks/`/`docs/`/`WINDOWS-NOTES.md` 零改動,git status 驗過)。★這是第三種漂移方向★:delguard 抓「code 刪了架構圖還在講」、交付目的地是「東西存在架構圖沒記」、這條是「**交付端做了事來源端不知道**」——三者中只有第一種有機械守衛
  KEY:★2026-08-01 補追加 Task 13——bash 全部搬成 Python(Windows 真的能用了),由使用者直接指派★:`slim/install.sh`(293 行)、`slim/uninstall.sh`(246 行)、`slim/get.sh`(56 行)全是 bash,Windows 上跑不了,是原設計已知缺口。裁定不接受「盲寫一份 PowerShell,兩份邏輯各自維護」(這台開發機也沒有 PowerShell,寫了驗不了)——改成新增 `slim/install.py`/`slim/uninstall.py` ★承載全部邏輯★(stdlib only),`slim/install.sh`/`slim/uninstall.sh` 改版成只做「定位套件目錄+挑 `python3`/`python` 直譯器+轉發參數」的薄殼(~30-35 行),新增對應的 `slim/install.ps1`/`slim/uninstall.ps1`/`slim/get.ps1`(先例=本 repo 根目錄完整版 `get.ps1`)。三層守衛/CLAUDE.md 合併(base64 備份/位元組級還原)/manifest 寫入/skill 碰撞語意/四步驟互不阻擋等既有行為逐條搬過去,`scripts/test_lumos.py` 既有 19 支 `t_slim_(install|uninstall|get)*` 斷言一條未鬆(`[test:]` 綁定的測試函式名稱全部不變,只是現在測 Python 實作),新增 5 支(2 支薄殼轉發參數獨立單元測試+3 支 Windows 分支邏輯測試,用 `LUMOS_SLIM_SIMULATE_WINDOWS=1` 環境變數在非 Windows 機器上注入驗證);`scripts/slim-gen.py` 組包清單同步擴充(吸取 Task 6/Task 9 兩次「新增檔案沒同步組包清單」教訓的第三次預防性修復)。移植過程順帶消掉兩類 bug:①CLAUDE.md 路徑計算的 `pwd` vs `pwd -P` 不一致(Task 12 修的那類)在 Python 版結構性不存在(`Path.cwd()` 一律回傳實體路徑)②bash `mv` 對已存在目錄是「搬進去」、Python `Path.rename()` 對已存在非空目錄直接拋例外——skill 目錄備份的秒級時間戳在 `--force` 重裝緊接卸載的測試情境下真的撞過一次,修成遞增後綴的 `_unique_backup_path()`。`python3 scripts/test_lumos.py -k slim` 253 checks 全綠(40 支 t_slim_* 函式)。★誠實邊界★:這台機器沒有 Windows,`.cmd` shim 真機行為/`mklink`/PATH 真實生效方式/三支 `.ps1` 薄殼本身都沒有真機驗證過,只驗過 Python 分支邏輯本身——`slim/README.md`〈支援平台〉與 [[Verification/2026-08-01_slim-python移植]] 的 `valid_under`/`revalidate_when` 都留了這個標記,不得誤讀成「Windows 已完整驗證」。詳見 [[Systems/slim-install-安裝器]]、[[Systems/slim-uninstall-一行卸載]]、[[Systems/slim-gen-生成器]]、[[Systems/slim-get-一行安裝]]、[[Systems/slim-readme]] 對應 KEY 行,報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-13-report.md`
  KEY:★2026-08-01 補追加 Task 14——修兩條 Windows 專屬缺陷,由使用者直接指派★:①`.cmd` shim 寫死呼叫字面 `python`,與 `install.sh`/`install.ps1` 兩支薄殼自己「python3 優先,找不到才退 python」的判斷邏輯互相矛盾——只有 `python3.exe`、沒有 `python.exe` 的 Windows 機器裝完即壞。修法:新增 `_pick_windows_interpreter()`,安裝當下用 `shutil.which()` 偵測寫進 shim,不寫死。②同一處碰撞偵測只看 `lumos` 不看 `lumos.cmd`,使用者手動殘留 `lumos.cmd` 時非 `--force` 重裝會無聲覆寫它——修法:Windows 路徑下碰撞判斷同時看兩個檔案。①②均先寫會紅的測試、`git stash` 只還原 `slim/install.py` 重跑確認翻紅、回補後轉綠,非稻草人。③(保險性修法)`install.ps1`/`uninstall.ps1`/`get.ps1` 三支收尾的裸 `exit $LASTEXITCODE` 在 README 教的兩種呼叫方式(`irm | iex`/`&` 呼叫鏈)下會終止整個呼叫端 PowerShell session,改成 `$global:LASTEXITCODE = $LASTEXITCODE`,不再呼叫 `exit`——★這台機器沒有 PowerShell,這段語意本身完全沒有真機驗證★,只做了靜態結構檢查,腳本註解與 `slim/README.md` 都同步標了這條未驗清單,範圍刻意收窄(三支 `.ps1` 內早期分支的 `exit 2` 錯誤守衛未觸碰,列已知殘留風險)。`python3 scripts/test_lumos.py -k slim` 272 checks 全綠(新增 3 支 `t_slim_*`);重新生成 `dist/` 確認 10 個入口檔仍在。詳見 [[Systems/slim-install-安裝器]]、[[Systems/slim-uninstall-一行卸載]]、[[Systems/slim-get-一行安裝]] 對應 KEY 行、[[Verification/2026-08-01_slim-windows兩缺陷修復]],報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-14-report.md`
  KEY:★2026-08-01 補追加 Task 15——補 Task 14 明講的殘留缺陷,由使用者直接指派★:Task 14 只修了三支 `.ps1` **結尾**的裸 `exit $LASTEXITCODE`,當時明講「範圍刻意收窄」,早期錯誤分支的 `exit 2` 留著沒動(共 6 處:`install.ps1` 找不到 python 1 處、`uninstall.ps1` 找不到 python 1 處、`get.ps1` 找不到 git/pull 失敗/目的地非本包 clone/交付包不完整共 4 處)——這幾處反而更該修,因為全在錯誤路徑上,使用者最需要看到錯誤訊息的時候視窗反而被關掉,連錯在哪都來不及讀。修法:三支檔案的邏輯都包進一個函式(`Invoke-Install`/`Invoke-Uninstall`/`Invoke-Get`),每個錯誤分支印完 `Write-Error` 後改成 `return <int>`(函式層級,結束整個函式但不終止呼叫端 session),腳本最下方把函式回傳值收進 `$rc` 再寫回 `$global:LASTEXITCODE`,沿用 Task 14 選定的慣例。TDD:先擴大既有測試 `t_slim_ps1_scripts_avoid_session_killing_trailing_exit`(斷言範圍從只看結尾擴大成三支檔案程式碼行完全不含任何裸 `exit`),新增反向斷言 `t_slim_ps1_error_branches_still_halt_via_return`(驗「錯誤訊息後緊接 return,不是被拆成不擋往下繼續跑」,用 mutant 拿掉一個 `return` 複測確實翻紅,證明反向斷言真的抓得到「守衛被拆成不擋」這種失敗模式)——動 `.ps1` 前先確認翻紅(24 checks 中 15 紅),改完轉綠(27 checks)。★誠實限制不變★:這台機器沒有 PowerShell,只能做靜態結構驗證,驗不到 PowerShell 真實執行語意;三支 `.ps1` 檔案註解、`slim/README.md`〈支援平台〉、架構圖節點的未驗清單都同步把這 6 處納入,不讓人誤以為 `.ps1` 錯誤路徑已經真機驗證過。`python3 scripts/test_lumos.py -k slim` 290 checks 全綠;重新生成 `dist/` 確認 10 個入口檔仍在,且生成後的 `.ps1` 同樣不含裸 `exit`。詳見 [[Systems/slim-install-安裝器]]、[[Systems/slim-uninstall-一行卸載]]、[[Systems/slim-get-一行安裝]] 對應 KEY 行、[[Verification/2026-08-01_slim-ps1早期分支exit修復]],報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-15-report.md`
  KEY:★2026-08-01 補追加 Task 16——修分支終審對抗代碼審抓到的三條缺陷,由使用者直接指派★:①BLOCKER——三支 `.ps1` 頂部 `$ErrorActionPreference = "Stop"` 會把 `Write-Error` 升級成終止型例外,讓 Task 14/15 整段「Write-Error 後接 return、rc 寫回 $global:LASTEXITCODE」修法在錯誤路徑上等於沒做(return 執行不到、例外炸穿呼叫端、rc 賦值完成不了,呼叫端讀到殘留舊值把失敗誤判成成功)——**這是本專案第十次撞到「測試存在但沒在驗它宣稱要驗的」**,Task 15 的反向斷言驗的是原始碼長相(Write-Error 後面是不是接 return),不是執行語意。修法:6 處 `Write-Error` 都加 `-ErrorAction Continue` 明確覆寫。②MAJOR——`uninstall.py` 把 `lumos.cmd` 移除邏輯巢狀在 `lumos` 的 if 區塊裡,孤兒 `lumos.cmd`(使用者手滑只刪 `lumos`)永遠清不掉、`--force` 也救不了,牴觸 `uninstall.py` docstring 自己講的「兩者總是成對安裝/移除」。修法:移出成獨立區塊,用 shim 固定樣板內容比對(`SHIM_TEXT_RE`)當安全基準;`git stash` 因果驗證確認舊版翻紅、回補轉綠,非稻草人;過程中踩到一個實作坑並修正——`read_text()` 的 universal-newline 轉譯會把比對用的 `\r\n` 正規化掉,改用 `read_bytes().decode()`。③MINOR——`get.ps1` 的 `$ErrorActionPreference` 原本寫頂層,`irm ... | iex` 執行時會污染呼叫端互動 session(`iex` 在呼叫端當下 scope 執行,不像 `&` 呼叫會建新 script scope)。修法:搬進 `Invoke-Get` 函式內部第一行(函式子 scope 天然隔離);`install.ps1`/`uninstall.ps1` 本不受影響但一併搬進函式維持一致。★測試設計誠實聲明(這次的重點)★:①的新測試 `t_slim_ps1_write_error_noterminating_under_stop_preference` 明確在 docstring 裡界定「驗得到寫法(有沒有 -ErrorAction Continue)、驗不到 PowerShell 真實執行語意」,且明講「不把 mutant 驗證當作測試夠格的證據」——mutant 只證明抓得到想到的失敗模式,不證明抓得到真正的失敗模式。`python3 scripts/test_lumos.py -k slim` 317 checks 全綠;重新生成 `dist/` 確認 10 個入口檔仍在。詳見 [[Systems/slim-install-安裝器]]、[[Systems/slim-uninstall-一行卸載]]、[[Systems/slim-get-一行安裝]] 對應 KEY 行、[[Verification/2026-08-01_slim-終審三缺陷修復]],報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-16-report.md`
---

# 公開精簡版 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（建議）或 `superpowers:executing-plans` 逐 Task 實作。步驟用 `- [ ]` 追蹤。

> **計畫落點說明**：writing-plans 預設寫 `docs/superpowers/plans/`，但本 repo 的 CLAUDE.md 規定「計劃/設計一律寫成架構圖 `Projects/` 節點、不寫其他 repo 路徑」，故落此處（skill 本身允許使用者偏好覆蓋預設）。

**Goal:** 從既有單檔 CLI 生成一個「只留白名單讀寫維護指令」的精簡版（初版 24 支；2026-08-11 起 25 支＋delguard；2026-08-16 起 26 支＋query），配一支只動 `$HOME` 的安裝器與一份自足 README，一次性交給離職接手者。

**Architecture:** 三支新腳本留在本 repo 當**工具**（生成器／掃描器／安裝器範本），跑生成後產物落 `dist/`（gitignore）；`dist/` 通過四道驗證後由使用者 push 進一個**凍結的新 repo**。移除謂詞＝「保留指令 dispatch 的 AST 可達性閉包之補集」，不是 `cmd_` 前綴比對。

**Tech Stack:** Python 3 stdlib（`ast`／`argparse`／`subprocess`／`pathlib`）、bash（安裝器）、既有 `scripts/test_lumos.py` 測試框架。

## Global Constraints

- **零依賴家規**：只准 Python 3 stdlib 與 bash，不得 `pip install` 任何東西。
- **測試位置與跑法**：所有測試寫進 `scripts/test_lumos.py`，函式名以 `t_slim_` 開頭（`main()` 用 `globals()` 掃 `t_` 前綴自動收集）。跑法 `python3 scripts/test_lumos.py`，或 `python3 scripts/test_lumos.py -k slim` 只跑本批。**注意：`-k` 選中 0 個案例會刻意回 rc1**。
- **測試 helper**：`run(vault, *args, expect_rc=None)` 跑 lumos 子指令；`check(name, cond, detail="")` 記一條斷言；`mkvault()` 建私有 vault。**每個測試自建 `tempfile.mkdtemp()`**（既有慣例，避免帳檔互踩）。
- **★不動 Landmark★**：`/Users/enzo/backend/LandmarkMember` 全程唯讀，不得寫入。
- **保留 26 支**（2026-08-11 delguard、2026-08-16 query 入列）：`append archive backlinks context contracts decision-add decision-reindex decision-supersede decisions delguard doctor export guard links lint map new query recent rel-cascade search set show stale stats sync-verified-by`
  - ~~待辦（2026-08-11）：delguard 入列後尚未重新生成並推送~~ → **已推（2026-08-12，`Citrus_Lumos` `6593a47..e112399`）**，但推的過程發現反向漂移，見下方 ★交付端反向漂移★。
  - **入列實錄（2026-08-11）**：keep 清單實有**四面鏡子**（slim-gen `DEFAULT_KEEP`／t_slim_gen／t_slim_gate／slim-scan `KEEP`）＋交付範本三處「N 支」散文——首次入列只改了前兩面，其餘全靠**自家漂移守衛翻紅抓回**（懸空引用掃描×3、負向 choices 道、範本支數新守衛）。守衛設計驗證成功的實例，也是「鏡子太多」的證據；範本支數宣稱已由新斷言機械釘住。
  - **delguard 入列理由（2026-08-11）**：消費端 pre-commit 已帶 Gate DG，slim 缺此命令＝靜默死門（stderr 被吞＋`|| true` 放行，守衛掛著永不響）。同理由下 **cochange 仍缺席**（Gate CC 在 slim 消費端同樣靜默）——已知現狀，是否入列另議。歷史區塊（下方 Task 內文/舊測試引文）保留當時的 24 支記載，屬歷史紀錄不回改。
- **移除 29 支**：`anchor bootstrap canary ci-status ci-wait cochange code-loop compose-metrics deinit fold-check gov impact init install lint-check lint-watch loop pitfalls refcheck self-audit signoff spec-trace sqlfluff-sarif stylelint-sarif teardown test-layers testmap uninstall update`
- **指令全集真值一律取 `python3 scripts/lumos --help` 的 choices，不得硬編 53 這個數字**。
- 產物落 `dist/`，**不進版控**。

---

## File Structure

| 檔案 | 責任 | 動作 |
|---|---|---|
| `scripts/slim-scan.py` | 交付文字掃描器：掃 README／SKILL.md／reference.md，找指向已移除指令與不給 skill 的引用，輸出候選清單 | 新建 |
| `scripts/slim-gen.py` | ast 生成器：讀 `scripts/lumos` → 算可達性閉包 → 吐精簡版到 `dist/` | 新建 |
| `slim/install.sh` | 機器層安裝器（原始碼；生成時複製進 `dist/`） | 新建 |
| `slim/README.md` | 交付 README（原始碼；生成時複製進 `dist/`） | 新建 |
| `scripts/test_lumos.py` | 加 `t_slim_*` 測試 | 修改 |
| `.gitignore` | 加 `dist/` | 修改 |

**為什麼掃描器與生成器分兩支**：掃描器是**長期守衛**（交付文字一改就該重跑），生成器是**一次性建置**。責任不同、觸發時機不同，合成一支會讓守衛被建置流程綁架。

---

## Task 0：分支前置

**Files:** Modify `.gitignore`

- [ ] **Step 1: 加 dist/ 到 .gitignore**

```bash
printf '\n# 公開精簡版建置產物(衍生物,不進版控)\ndist/\n' >> .gitignore
```

- [ ] **Step 2: 確認生效**

Run: `mkdir -p dist && touch dist/probe && git status --porcelain dist/`
Expected: 空輸出（dist/ 被忽略）

- [ ] **Step 3: 補架構圖漏連結**（spec 放行時的裸項③之一）

Run: `python3 scripts/lumos append Projects/公開精簡版_計劃 related "[[Systems/lumos-cli-write]]"`
理由：[S1] 保留的寫入指令族（`set`/`append`/`new`/`archive`/`decision-*`）正是該節點記載的內容。

- [ ] **Step 4: Commit**

```bash
rm -f dist/probe
git add .gitignore docs/lumos-toolchain-knowledge/Projects/公開精簡版_計劃.md
git commit -m "chore(slim): dist/ 不進版控 + 補架構圖漏連結 lumos-cli-write"
```

---

## Task 1：交付文字掃描器

**為什麼先做這個**：它最小、最獨立，而且 spec 放行時的三條裸項有一條（第五種形態）完全靠它接住。

**Files:**
- Create: `scripts/slim-scan.py`
- Modify: `scripts/test_lumos.py`（加 `t_slim_scan`）

**Interfaces:**
- Produces：CLI `python3 scripts/slim-scan.py <檔案...> [--json]`；rc 0＝無命中／1＝有候選／2＝參數錯。JSON 形狀 `{"candidates":[{"file":str,"line":int,"token":str,"form":str,"text":str}],"total":int}`

- [x] **Step 1: 寫失敗測試**

加到 `scripts/test_lumos.py`（放在檔案末尾、`def main()` 之前）：

```python
def t_slim_scan():
    """交付文字掃描器:五種懸空引用形態都要命中,保留指令不得誤報為移除指令。"""
    import tempfile as _tf
    from pathlib import Path as _P
    root = _P(_tf.mkdtemp(prefix="gctl-slimscan-"))
    scanner = str(_P(GRAPHCTL).parent / "slim-scan.py")

    f = root / "sample.md"
    f.write_text(
        "1 前綴型: 跑 `lumos pitfalls <md>` 掃隱患\n"
        "2 裸token型: 對抗審計 loop(`canary`/`refcheck`)\n"
        "3 skill型: 走 design-loop skill 打磨\n"
        "4 span帶參型: `loop status --gate` 問收斂\n"
        "5 裸散文型: canary 驗審計員醒著\n"
        "6 乾淨行: 用 `lumos search` 找節點\n",
        encoding="utf-8")

    r = subprocess.run([sys.executable, scanner, str(f), "--json"],
                       capture_output=True, text=True)
    check("slim-scan 有命中回 rc1", r.returncode == 1, r.stderr)
    import json as _j
    d = _j.loads(r.stdout)
    forms = {c["form"] for c in d["candidates"]}
    for want in ("prefixed", "bare-token", "skill-name", "span-with-args", "prose"):
        check(f"slim-scan 命中形態 {want}", want in forms, str(sorted(forms)))
    lines = {c["line"] for c in d["candidates"]}
    check("slim-scan 不誤報保留指令 search(第6行)", 6 not in lines, str(sorted(lines)))

    # 反事實:乾淨檔必須 rc0
    g = root / "clean.md"
    g.write_text("只提保留指令: `lumos search`、`lumos doctor`、`lumos guard list`\n",
                 encoding="utf-8")
    r2 = subprocess.run([sys.executable, scanner, str(g)], capture_output=True, text=True)
    check("slim-scan 乾淨檔 rc0", r2.returncode == 0, r2.stdout + r2.stderr)
```

- [x] **Step 2: 跑測試確認失敗**

Run: `python3 scripts/test_lumos.py -k slim_scan`
Expected: FAIL（`slim-scan.py` 不存在，subprocess 回非 0／stdout 非 JSON）

- [x] **Step 3: 實作掃描器**

Create `scripts/slim-scan.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""slim-scan.py — 交付文字懸空引用掃描器(stdlib only)

掃 README/SKILL.md/reference.md,找出提及「精簡版已移除的指令」或「不交付的 skill」
的句子,輸出候選清單交人逐條裁。

★不是自動改寫器★:裸 token 形態必然有假陽性(export/set/show/loop/impact 等本身
是常見英文詞),故只出候選、不動檔案。

rc: 0=無候選 / 1=有候選 / 2=參數錯
"""
import argparse
import json
import re
import subprocess
import sys
from pathlib import Path

# 不交付的 skill(含不帶 lumos- 前綴的簡稱)
DROP_SKILLS = ("lumos-design-loop", "lumos-code-loop", "lumos-core-knowledge",
               "lumos-pitfalls-gapfill", "design-loop", "code-loop",
               "core-knowledge", "pitfalls-gapfill")

KEEP = set("""append archive backlinks context contracts decision-add decision-reindex
decision-supersede decisions doctor export guard links lint map new recent
rel-cascade search set show stale stats sync-verified-by""".split())


def all_commands(lumos: Path):
    """真值取 --help 的 choices,不硬編。"""
    r = subprocess.run([sys.executable, str(lumos), "--help"],
                       capture_output=True, text=True)
    m = re.search(r"\{([a-z0-9,\-]+)\}", r.stdout)
    if not m:
        print("ERROR: 無法從 --help 解析指令全集", file=sys.stderr)
        sys.exit(2)
    return set(m.group(1).split(","))


def scan_line(line, removed):
    """回傳 [(token, form), ...]。同一行可命中多種形態。"""
    hits = []
    # 形態 1:帶前綴
    for m in re.finditer(r"(?:python3\s+)?(?:scripts/)?lumos\s+([a-z0-9\-]+)", line):
        if m.group(1) in removed:
            hits.append((m.group(1), "prefixed"))
    # 形態 2/4:backtick span —— 取首 token 比對(涵蓋「裸 token」與「帶參數」兩種)
    for m in re.finditer(r"`([^`]+)`", line):
        span = m.group(1).strip()
        first = span.split()[0] if span.split() else ""
        first = first.lstrip("/")
        if first in removed:
            hits.append((first, "bare-token" if span == first else "span-with-args"))
    # 形態 3:skill 名(含簡稱)
    for s in DROP_SKILLS:
        if s in line:
            hits.append((s, "skill-name"))
            break
    # 形態 5:裸散文型 —— 無 backtick、無前綴,直接嵌在句子裡
    #   只認「有明確詞邊界」的出現,且該 token 不是保留指令
    for cmd in removed:
        if len(cmd) < 4:          # 太短的詞(如 gov)誤報率過高,交給形態 1/2
            continue
        if re.search(r"(?<![`\w\-])" + re.escape(cmd) + r"(?![\w\-])", line):
            if not any(h[0] == cmd for h in hits):
                hits.append((cmd, "prose"))
    return hits


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("files", nargs="+")
    ap.add_argument("--json", action="store_true")
    ap.add_argument("--lumos", default=str(Path(__file__).resolve().parent / "lumos"))
    a = ap.parse_args()

    removed = all_commands(Path(a.lumos)) - KEEP
    out = []
    for fp in a.files:
        p = Path(fp)
        if not p.is_file():
            print(f"ERROR: 檔案不存在: {fp}", file=sys.stderr)
            return 2
        for n, line in enumerate(p.read_text(encoding="utf-8").splitlines(), 1):
            for tok, form in scan_line(line, removed):
                out.append({"file": fp, "line": n, "token": tok,
                            "form": form, "text": line.strip()[:120]})

    if a.json:
        print(json.dumps({"candidates": out, "total": len(out)}, ensure_ascii=False))
    else:
        for c in out:
            print(f"{c['file']}:{c['line']}  [{c['form']}] {c['token']}\n    {c['text']}")
        print(f"\n候選 {len(out)} 條 —— ★這是候選不是判決,裸 token 與散文型必有假陽性,請逐條裁★")
    return 1 if out else 0


if __name__ == "__main__":
    sys.exit(main())
```

- [x] **Step 4: 跑測試確認通過**

Run: `python3 scripts/test_lumos.py -k slim_scan`
Expected: PASS（7 條 check 全綠；實跑為 8 條，brief 計數含 rc1 起手式與 5 形態時漏算不誤報+乾淨檔兩條，非缺陷）

- [x] **Step 5: 對真實交付檔跑一次，確認抓得到已知案例**

Run:
```bash
python3 scripts/slim-scan.py skills/lumos-project-notes/SKILL.md skills/lumos-project-notes/reference.md | tail -5
```
Expected: 候選數 > 50；且 `reference.md:85`（子命令全覽）、`reference.md:730`（`loop status --gate`）、`SKILL.md:156`（裸散文 `canary`）都在輸出裡。**這三條是 spec 三輪審計各自抓到的形態，抓不到就是掃描器沒做對。**
實跑結果：candidates=129，三條全命中（詳見 [[Verification/2026-07-31_slim-scan掃描器落地]]）。

- [x] **Step 6: Commit**

```bash
git add scripts/slim-scan.py scripts/test_lumos.py
git commit -m "feat(slim): 交付文字掃描器 —— 五種懸空引用形態 + 候選交人裁"
```

---

## Task 2：ast 生成器

**Files:**
- Create: `scripts/slim-gen.py`
- Modify: `scripts/test_lumos.py`（加 `t_slim_gen`、`t_slim_gen_loop_registration`）

**Interfaces:**
- Consumes：Task 0 的 `dist/` 慣例
- Produces：`python3 scripts/slim-gen.py [--out dist] [--emit-manifest]`；產出 `dist/scripts/lumos`。`--emit-manifest` 印 JSON `{"keep_funcs":[...],"drop_funcs":[...]}`

- [ ] **Step 1: 寫失敗測試（合成 fixture 驗迴圈註冊）**

這條是 spec 放行時的裸項③：現行白名單下**沒有真實的「該砍的迴圈註冊指令」**（唯一迴圈註冊的 `links`/`backlinks` 都在保留清單），所以必須造合成 fixture 才驗得到。

```python
def t_slim_gen_loop_registration():
    """合成 fixture:迴圈註冊的指令在該砍時真的被砍掉。
    ★現行白名單下無真實對象(links/backlinks 都保留),故必須合成★(spec T-5)。"""
    import tempfile as _tf
    from pathlib import Path as _P
    root = _P(_tf.mkdtemp(prefix="gctl-slimgen-loop-"))
    src = root / "toy.py"
    src.write_text(
        "import argparse, sys\n"
        "def cmd_keepme():\n    print('keep'); return 0\n"
        "def cmd_dropme():\n    print('drop'); return 0\n"
        "def main():\n"
        "    ap = argparse.ArgumentParser()\n"
        "    sub = ap.add_subparsers(dest='cmd')\n"
        "    for name, hlp in (('keepme','k'), ('dropme','d')):\n"
        "        sub.add_parser(name, help=hlp)\n"
        "    args = ap.parse_args()\n"
        "    if args.cmd == 'keepme':\n        return cmd_keepme()\n"
        "    if args.cmd == 'dropme':\n        return cmd_dropme()\n"
        "    return 2\n"
        "if __name__ == '__main__':\n    sys.exit(main())\n",
        encoding="utf-8")

    gen = str(_P(GRAPHCTL).parent / "slim-gen.py")
    out = root / "out.py"
    r = subprocess.run([sys.executable, gen, "--src", str(src), "--outfile", str(out),
                        "--keep", "keepme"], capture_output=True, text=True)
    check("slim-gen 合成 fixture rc0", r.returncode == 0, r.stdout + r.stderr)
    txt = out.read_text(encoding="utf-8")
    check("迴圈註冊的 dropme 被砍出註冊清單", "'dropme'" not in txt and '"dropme"' not in txt, txt[:400])
    check("迴圈註冊的 keepme 仍在", "keepme" in txt, txt[:400])
    check("cmd_dropme 函式被移除", "def cmd_dropme" not in txt, txt[:400])
    h = subprocess.run([sys.executable, str(out), "--help"], capture_output=True, text=True)
    check("產物 --help 不含 dropme", "dropme" not in h.stdout, h.stdout)
```

- [ ] **Step 2: 寫第二個失敗測試（真檔生成）**

```python
def t_slim_gen():
    """對真實 scripts/lumos 生成:產物 --help 只剩保留指令、compileall 過、無 dangling handler。"""
    import tempfile as _tf
    import re as _re
    from pathlib import Path as _P
    root = _P(_tf.mkdtemp(prefix="gctl-slimgen-"))
    gen = str(_P(GRAPHCTL).parent / "slim-gen.py")
    out = root / "lumos"

    r = subprocess.run([sys.executable, gen, "--outfile", str(out)],
                       capture_output=True, text=True)
    check("slim-gen 真檔 rc0", r.returncode == 0, r.stdout + r.stderr)

    h = subprocess.run([sys.executable, str(out), "--help"], capture_output=True, text=True)
    m = _re.search(r"\{([a-z0-9,\-]+)\}", h.stdout)
    got = set(m.group(1).split(",")) if m else set()
    keep = set("""append archive backlinks context contracts decision-add decision-reindex
decision-supersede decisions doctor export guard links lint map new recent
rel-cascade search set show stale stats sync-verified-by""".split())
    check("產物 --help == 保留 24 支", got == keep, f"多={sorted(got-keep)} 少={sorted(keep-got)}")

    c = subprocess.run([sys.executable, "-W", "error::SyntaxWarning",
                        "-m", "py_compile", str(out)], capture_output=True, text=True)
    check("產物 py_compile 0 SyntaxWarning", c.returncode == 0, c.stderr)

    # dangling handler = 0:產物內被呼叫但未定義的 cmd_*/run_* 名稱
    import ast as _ast
    tree = _ast.parse(out.read_text(encoding="utf-8"))
    defined = {n.name for n in _ast.walk(tree) if isinstance(n, _ast.FunctionDef)}
    called = {c.func.id for c in _ast.walk(tree)
              if isinstance(c, _ast.Call) and isinstance(c.func, _ast.Name)}
    dangling = {n for n in (called - defined) if n.startswith(("cmd_", "run_", "_"))}
    check("產物 dangling handler = 0", not dangling, str(sorted(dangling)))
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `python3 scripts/test_lumos.py -k slim_gen`
Expected: FAIL（`slim-gen.py` 不存在）

- [ ] **Step 4: 實作生成器**

> ★**必讀：為什麼是「行級手術」不是 `ast.unparse`**★（2026-07-31 pre-flight 裁定 A1）
> `ast.unparse` 從語法樹重建程式碼，而**語法樹裡沒有註解**——實測會把 `scripts/lumos` 的 **686 行註解全部剝光**、格式全部重排。那些註解裡有大量「為什麼這樣寫」的事故紀錄（W4 junction 誤刪來源、cp950 編碼坑…），正是 code 讀不出來、最該留給接手者的東西。
> 這與 spec 的核心價值主張正面衝突：〈誠實天花板〉寫「Python 是原始碼語言……**他們改得動**」。**一個沒有註解、格式被重排的 12000 行檔案，接手的人改不動。**
> ★**正解＝用 AST 只拿「要刪的行號範圍」，然後刪那些行；保留的部分一個字元都不動。**★ 唯一例外是「混合保留／移除的迴圈註冊」，該小塊（2–3 行）允許重排。

Create `scripts/slim-gen.py`。核心四段：算 root → 算閉包 → 收集行號編輯 → 行級套用。

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""slim-gen.py — 從單檔 CLI 生成「只留白名單指令」的精簡版(stdlib only)

★移除謂詞不是「cmd_ 前綴」★(lint-watch 實作叫 _lint_watch_mode、保留的 doctor
叫 run_doctor)——正解是「保留指令 dispatch 的 AST 可達性閉包之補集」。

★root 集合必須含 module-level 語句與 class body★,否則 `_SKILLS = _skills_list()`
這種模組層賦值會讓 helper 落進補集,產物 import 期就 NameError 全滅。
"""
import argparse
import ast
import re
import sys
from pathlib import Path

DEFAULT_KEEP = """append archive backlinks context contracts decision-add decision-reindex
decision-supersede decisions doctor export guard links lint map new recent
rel-cascade search set show stale stats sync-verified-by""".split()


def _called(node, universe):
    out = set()
    for c in ast.walk(node):
        if isinstance(c, ast.Call) and isinstance(c.func, ast.Name):
            out.add(c.func.id)
        elif isinstance(c, ast.Name) and isinstance(c.ctx, ast.Load) and c.id in universe:
            out.add(c.id)            # 函式當一級物件傳遞
    return out & universe


def _cmd_names(test):
    return {n.value for n in ast.walk(test)
            if isinstance(n, ast.Constant) and isinstance(n.value, str)}


def build(src_text, keep):
    tree = ast.parse(src_text)
    funcs = {n.name: n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}
    main = funcs.get("main")
    if main is None:
        raise SystemExit("ERROR: 找不到 main()")

    roots = set()
    # root ①:保留指令的 dispatch 分支
    for node in ast.walk(main):
        if isinstance(node, ast.If):
            ns = _cmd_names(node.test)
            if ns and (ns & set(keep)):
                for st in node.body:
                    roots |= _called(st, set(funcs))
    # root ②:module-level 語句與 class body(★不可省★)
    for node in tree.body:
        if not isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
            roots |= _called(node, set(funcs))
    roots |= _called(main, set(funcs)) & {"main"}
    roots.add("main")

    seen, stack = set(roots), list(roots)
    while stack:
        f = stack.pop()
        for g in _called(funcs[f], set(funcs)):
            if g not in seen:
                seen.add(g); stack.append(g)
    drop = set(funcs) - seen
    return tree, funcs, seen, drop


def _first_const(e):
    """迴圈註冊元素 ('name','help') → 'name';非此形狀回 None。"""
    if isinstance(e, ast.Tuple) and e.elts and isinstance(e.elts[0], ast.Constant):
        v = e.elts[0].value
        return v if isinstance(v, str) else None
    if isinstance(e, ast.Constant) and isinstance(e.value, str):
        return e.value
    return None


def _add_parser_name(node):
    """Assign/Expr 是不是 sub.add_parser("<cmd>", ...);是則回 cmd。"""
    c = node.value if isinstance(node, (ast.Assign, ast.Expr)) else None
    if (isinstance(c, ast.Call) and isinstance(c.func, ast.Attribute)
            and c.func.attr == "add_parser" and c.args
            and isinstance(c.args[0], ast.Constant)
            and isinstance(c.args[0].value, str)):
        return c.args[0].value
    return None


def collect_edits(tree, drop, keep, removed):
    """回傳 (dels, inserts)。
    dels    = [(起行, 迄行)] 1-based inclusive,整行刪除
    inserts = [(行號, 文字)] 在該行位置插入(用於混合迴圈註冊的重寫)
    ★只記行號範圍,不重建任何保留的程式碼——註解與格式因此原字保留★
    """
    dels, inserts = [], []
    for n in tree.body:
        if isinstance(n, ast.FunctionDef) and n.name in drop:
            start = min([n.lineno] + [d.lineno for d in n.decorator_list])
            dels.append((start, n.end_lineno))

    for n in ast.walk(tree):
        # subparser 註冊(Assign 形式 p = sub.add_parser(...) 與 Expr 形式)
        nm = _add_parser_name(n) if isinstance(n, (ast.Assign, ast.Expr)) else None
        if nm is not None and nm in removed:
            dels.append((n.lineno, n.end_lineno))
            continue
        # dispatch 分支:條件裡的指令名全部屬移除清單才砍
        if isinstance(n, ast.If):
            ns = _cmd_names(n.test)
            if ns and ns <= removed:
                dels.append((n.lineno, n.end_lineno))
                continue
        # 迴圈註冊
        if isinstance(n, ast.For) and isinstance(n.iter, ast.Tuple):
            names = [_first_const(e) for e in n.iter.elts]
            if names and all(x is not None for x in names):
                kept = [e for e, x in zip(n.iter.elts, names) if x in keep]
                if not kept:
                    dels.append((n.lineno, n.end_lineno))          # 全砍
                elif len(kept) != len(n.iter.elts):
                    # ★唯一允許重排的小塊★:整塊刪掉,改插入過濾後的等價迴圈
                    node2 = ast.For(target=n.target,
                                    iter=ast.Tuple(elts=kept, ctx=ast.Load()),
                                    body=n.body, orelse=n.orelse, type_comment=None)
                    ast.fix_missing_locations(node2)
                    indent = " " * n.col_offset
                    txt = "\n".join(indent + ln for ln in ast.unparse(node2).splitlines())
                    dels.append((n.lineno, n.end_lineno))
                    inserts.append((n.lineno, txt))
    return dels, inserts


def apply_edits(text, dels, inserts):
    lines = text.splitlines(keepends=True)
    kill = [False] * (len(lines) + 2)
    for a, b in dels:
        for i in range(a, b + 1):
            if i < len(kill):
                kill[i] = True
    ins = {}
    for ln, txt in inserts:
        ins.setdefault(ln, []).append(txt)
    out = []
    for i, ln in enumerate(lines, 1):
        for txt in ins.get(i, []):
            out.append(txt + "\n")
        if not kill[i]:
            out.append(ln)
    return "".join(out)


def main():
    ap = argparse.ArgumentParser()
    here = Path(__file__).resolve().parent
    ap.add_argument("--src", default=str(here / "lumos"))
    ap.add_argument("--outfile", default=str(here.parent / "dist" / "scripts" / "lumos"))
    ap.add_argument("--keep", nargs="*", default=None, help="覆蓋預設保留清單(測試用)")
    ap.add_argument("--emit-manifest", action="store_true")
    a = ap.parse_args()

    keep = set(a.keep) if a.keep is not None else set(DEFAULT_KEEP)
    text = Path(a.src).read_text(encoding="utf-8")
    tree, funcs, seen, drop = build(text, keep)

    # 移除清單真值:從 subparser 註冊掃出全部指令名,減去 keep(不硬編)
    allc = set()
    for n in ast.walk(tree):
        nm = _add_parser_name(n) if isinstance(n, (ast.Assign, ast.Expr)) else None
        if nm:
            allc.add(nm)
        if isinstance(n, ast.For) and isinstance(n.iter, ast.Tuple):
            for e in n.iter.elts:
                x = _first_const(e)
                if x:
                    allc.add(x)
    removed = allc - keep

    dels, inserts = collect_edits(tree, drop, keep, removed)
    new_text = apply_edits(text, dels, inserts)

    # 產物必須自己 parse 得過(行級手術最容易壞的地方就是刪出語法洞)
    try:
        ast.parse(new_text)
    except SyntaxError as e:
        print(f"ERROR: 產物語法壞了(行級手術刪出洞): {e}", file=sys.stderr)
        return 1

    out = Path(a.outfile)
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(new_text, encoding="utf-8")
    out.chmod(0o755)

    kept_comments = sum(1 for ln in new_text.splitlines() if ln.lstrip().startswith("#"))
    if a.emit_manifest:
        import json
        print(json.dumps({"keep_funcs": sorted(seen), "drop_funcs": sorted(drop),
                          "removed_cmds": sorted(removed), "kept_comment_lines": kept_comments},
                         ensure_ascii=False))
    else:
        print(f"✓ 生成 {out}  保留 {len(seen)} 函式 / 移除 {len(drop)} / 保住 {kept_comments} 行註解")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4b: 加一條「註解必須留住」的測試**（A1 裁定的守衛——沒有它，日後有人改回 `ast.unparse` 不會有任何東西翻紅）

```python
def t_slim_gen_keeps_comments():
    """★行級手術的存在理由★:產物必須保住絕大多數註解。
    若有人把生成器改回 ast.unparse,這條會翻紅。"""
    import tempfile as _tf
    from pathlib import Path as _P
    repo = _P(GRAPHCTL).parent.parent
    out = _P(_tf.mkdtemp(prefix="gctl-slimcmt-")) / "lumos"
    r = subprocess.run([sys.executable, str(repo / "scripts" / "slim-gen.py"),
                        "--outfile", str(out)], capture_output=True, text=True)
    check("slim-gen rc0", r.returncode == 0, r.stdout + r.stderr)
    src_c = sum(1 for ln in (repo / "scripts" / "lumos").read_text(encoding="utf-8").splitlines()
                if ln.lstrip().startswith("#"))
    out_c = sum(1 for ln in out.read_text(encoding="utf-8").splitlines()
                if ln.lstrip().startswith("#"))
    check(f"★產物保住 ≥60% 註解★(原 {src_c} 行)", out_c >= src_c * 0.6, f"產物只剩 {out_c} 行")
    # 抽驗一條有事故脈絡的註解必須在
    check("事故註解仍在(W4 junction)", "W4" in out.read_text(encoding="utf-8"), "")
```

- [ ] **Step 5: 跑測試**

Run: `python3 scripts/test_lumos.py -k slim_gen`
Expected: PASS。**若 `產物 --help == 保留 24 支` 紅，讀 diff 找出哪支沒被砍乾淨——多半是註冊形式（`Expr` vs `Assign` vs 迴圈）沒涵蓋到，補 `_Pruner` 的對應 visit。**

- [ ] **Step 6: 實跑一次真生成並手動冒煙**

```bash
python3 scripts/slim-gen.py --emit-manifest | python3 -m json.tool | head -5
python3 dist/scripts/lumos --help | head -3
```

- [ ] **Step 7: 全套回歸**

Run: `python3 scripts/test_lumos.py`
Expected: 既有測試全綠（生成器不動 `scripts/lumos` 本身，應零影響）

- [ ] **Step 8: Commit**

```bash
git add scripts/slim-gen.py scripts/test_lumos.py
git commit -m "feat(slim): ast 生成器 —— 可達性閉包補集 + root 含 module-level"
```

---

## Task 3：機器層安裝器

**Files:**
- Create: `slim/install.sh`
- Modify: `scripts/test_lumos.py`（加 `t_slim_install_no_project_touch`）

**Interfaces:**
- Consumes：`dist/` 目錄結構（`dist/scripts/lumos`、`dist/skills/lumos-project-notes/`）
- Produces：`slim/install.sh`，用 `$(dirname "$0")` 定位自身所在的交付包

- [x] **Step 1: 寫反誤傷測試（★本 Task 最重要的一步★）**

```python
def t_slim_install_no_project_touch():
    """★反事實★ 安裝器不得觸及專案層:跑完 worktree porcelain 空 + .git/config 不變。
    且不得無備份覆寫既有機器層資產。"""
    import tempfile as _tf, os as _os, shutil as _sh
    from pathlib import Path as _P
    root = _P(_tf.mkdtemp(prefix="gctl-sliminst-"))

    # 造一個假交付包
    pkg = root / "pkg"
    (pkg / "scripts").mkdir(parents=True)
    (pkg / "skills" / "lumos-project-notes").mkdir(parents=True)
    (pkg / "scripts" / "lumos").write_text("#!/usr/bin/env python3\nprint('slim')\n",
                                           encoding="utf-8")
    (pkg / "scripts" / "lumos").chmod(0o755)
    (pkg / "skills" / "lumos-project-notes" / "SKILL.md").write_text("# skill\n",
                                                                     encoding="utf-8")
    _sh.copy2(_P(GRAPHCTL).parent.parent / "slim" / "install.sh", pkg / "install.sh")
    (pkg / "install.sh").chmod(0o755)

    # 造一個假專案 repo
    proj = root / "proj"
    proj.mkdir()
    subprocess.run(["git", "init", "-q", str(proj)], check=True)
    (proj / "CLAUDE.md").write_text("# 專案紀律\n", encoding="utf-8")
    (proj / "scripts").mkdir()
    (proj / "scripts" / "own.py").write_text("print(1)\n", encoding="utf-8")
    subprocess.run(["git", "-C", str(proj), "add", "-A"], check=True)
    subprocess.run(["git", "-C", str(proj), "-c", "user.email=t@t", "-c", "user.name=t",
                    "commit", "-qm", "init"], check=True)
    cfg_before = (proj / ".git" / "config").read_text(encoding="utf-8")

    fake_home = root / "home"
    (fake_home / ".local" / "bin").mkdir(parents=True)
    env = dict(_os.environ, HOME=str(fake_home))
    r = subprocess.run(["bash", str(pkg / "install.sh")], cwd=str(proj),
                       capture_output=True, text=True, env=env)
    check("安裝器 rc0", r.returncode == 0, r.stdout + r.stderr)

    st = subprocess.run(["git", "-C", str(proj), "status", "--porcelain"],
                        capture_output=True, text=True)
    check("★專案 worktree porcelain 為空★", st.stdout.strip() == "", st.stdout)
    check("★專案 .git/config 前後相同★",
          (proj / ".git" / "config").read_text(encoding="utf-8") == cfg_before, "")
    check("全域指令已裝", (fake_home / ".local" / "bin" / "lumos").exists(), "")
    check("skill 已實體複製(非 symlink)",
          (fake_home / ".claude" / "skills" / "lumos-project-notes" / "SKILL.md").is_file()
          and not (fake_home / ".claude" / "skills" / "lumos-project-notes").is_symlink(), "")
    check("不裝任何 Claude hook",
          not (fake_home / ".claude" / "hooks").exists(), "")

    # ★反誤傷★:既有一般檔不得被刪
    (fake_home / ".local" / "bin" / "lumos").unlink()
    (fake_home / ".local" / "bin" / "lumos").write_text("USER OWN\n", encoding="utf-8")
    r2 = subprocess.run(["bash", str(pkg / "install.sh")], cwd=str(proj),
                        capture_output=True, text=True, env=env)
    check("既有一般檔 → 拒絕 rc2", r2.returncode == 2, r2.stdout + r2.stderr)
    check("既有一般檔內容未被動",
          (fake_home / ".local" / "bin" / "lumos").read_text(encoding="utf-8") == "USER OWN\n", "")
```

- [x] **Step 2: 跑測試確認失敗**

Run: `python3 scripts/test_lumos.py -k slim_install`
Expected: FAIL（`slim/install.sh` 不存在）

- [x] **Step 3: 實作安裝器**

Create `slim/install.sh`：

```bash
#!/usr/bin/env bash
# install.sh — 公開精簡版 機器層安裝器
#
# ★只做兩件事★:①全域 lumos 指令 ②實體複製 skill 到 ~/.claude/skills/
# ★明確不做★:不 scaffold 架構圖、不注入/更新任何 CLAUDE.md、不 vendor 工具進專案、
#            不設 core.hooksPath、不裝任何 Claude hook。
#
# 包的位置 = 本腳本所在目錄(隨包走,不需要參數)。
set -eu

PKG="$(cd "$(dirname "$0")" && pwd)"
BIN="${HOME}/.local/bin"
SKILLS="${HOME}/.claude/skills"
SRC_CLI="${PKG}/scripts/lumos"
SRC_SKILL="${PKG}/skills/lumos-project-notes"

[ -f "$SRC_CLI" ]   || { echo "ERROR: 找不到 ${SRC_CLI}" >&2; exit 2; }
[ -d "$SRC_SKILL" ] || { echo "ERROR: 找不到 ${SRC_SKILL}" >&2; exit 2; }

FORCE=0
[ "${1:-}" = "--force" ] && FORCE=1

mkdir -p "$BIN" "$SKILLS"

# ① 全域指令 —— 碰撞語意沿用完整版 cmd_install 的階梯
DST_CLI="${BIN}/lumos"
if [ -e "$DST_CLI" ] || [ -L "$DST_CLI" ]; then
  if [ "$FORCE" -eq 0 ]; then
    echo "⚠ ${DST_CLI} 已存在,加 --force 覆寫" >&2
    exit 2
  fi
  rm -f "$DST_CLI"
fi
cp "$SRC_CLI" "$DST_CLI"
chmod +x "$DST_CLI"
echo "✓ 全域指令: ${DST_CLI}"

# ② skill —— ★實體複製,不是 symlink★(來源=交付包,但複製後與包解耦)
DST_SKILL="${SKILLS}/lumos-project-notes"
if [ -e "$DST_SKILL" ] || [ -L "$DST_SKILL" ]; then
  if [ "$FORCE" -eq 0 ]; then
    echo "⚠ ${DST_SKILL} 已存在,加 --force 覆寫(★會先備份★)" >&2
    exit 2
  fi
  BAK="${DST_SKILL}.bak.$(date +%Y%m%d%H%M%S)"
  mv "$DST_SKILL" "$BAK"
  echo "  已備份既有 skill → ${BAK}"
fi
cp -R "$SRC_SKILL" "$DST_SKILL"
echo "✓ skill: ${DST_SKILL}"

case ":${PATH}:" in
  *":${BIN}:"*) ;;
  *) echo "⚠ ${BIN} 不在 PATH,請自行加入 shell 設定檔" >&2 ;;
esac

echo
echo "裝好了。驗證: lumos --help"
echo "★這是凍結快照,不是發布通道——不會有更新。出問題請直接改 Python 原始碼。★"
```

- [x] **Step 4: 跑測試確認通過**

Run: `python3 scripts/test_lumos.py -k slim_install`
Expected: PASS（9 條 check 全綠）

- [x] **Step 5: 實測 `$(dirname "$0")` 的邊界**（spec [S4-c] 明列未經審計、必須實測）

```bash
mkdir -p "/tmp/slim probe/pkg/scripts" "/tmp/slim probe/pkg/skills/lumos-project-notes"
cp slim/install.sh "/tmp/slim probe/pkg/"
echo '#!/usr/bin/env python3' > "/tmp/slim probe/pkg/scripts/lumos"
touch "/tmp/slim probe/pkg/skills/lumos-project-notes/SKILL.md"
ln -sf "/tmp/slim probe/pkg/install.sh" /tmp/slim-link.sh
HOME=/tmp/slim-fakehome bash /tmp/slim-link.sh 2>&1 | tail -3
```
Expected: 路徑含空白時仍正確定位；**透過 symlink 呼叫時 `dirname "$0"` 解析到 symlink 所在目錄而非真實包目錄——若此情境失敗，改用 `readlink -f` 或在 README 明講「不要用 symlink 呼叫 install.sh」**。把結論寫回 spec [S4-c]。

- [x] **Step 6: Commit**

```bash
git add slim/install.sh scripts/test_lumos.py
git commit -m "feat(slim): 機器層安裝器 —— 只動 \$HOME、碰撞拒絕/備份、\$(dirname \$0) 定位"
```

---

## Task 4：skill 修剪 + README

**Files:**
- Create: `slim/README.md`
- Create: `slim/skills/lumos-project-notes/`（從既有複製後修剪）
- Modify: `scripts/test_lumos.py`（加 `t_slim_readme_assertions`）

- [ ] **Step 1: 複製 skill 到交付源目錄**

```bash
mkdir -p slim/skills
cp -R skills/lumos-project-notes slim/skills/
```

- [ ] **Step 2: 跑掃描器出候選**

```bash
python3 scripts/slim-scan.py slim/skills/lumos-project-notes/SKILL.md \
  slim/skills/lumos-project-notes/reference.md --json > /tmp/slim-candidates.json
python3 -c "import json;d=json.load(open('/tmp/slim-candidates.json'));print(d['total'])"
```

- [ ] **Step 3: 逐條裁決並修剪**

★**這一步是人工的，不能自動化**★——裸 token 與散文型必有假陽性（`export`／`set`／`show` 是常見英文詞）。逐條看候選，對每條決定：改寫句子／刪整段／判為假陽性。

修剪原則（spec [S4-a]／已裁定①）：
- **只修懸空引用，紀律語氣照舊不動**
- **語氣豁免保的是「話」不是「話所在的段落」**——例如 `SKILL.md:14` 的「紅燈不過夜……不得靜默收工」要留，但同段的 `lumos ci-wait` 教學要刪
- `reference.md:85` 的「子命令全覽」整行必須改寫成只列 24 支保留指令

- [ ] **Step 4: 重跑掃描器確認清乾淨**

Run: `python3 scripts/slim-scan.py slim/skills/lumos-project-notes/*.md`
Expected: rc 0，或剩下的候選你能逐條說出「這是假陽性，理由是 X」

- [ ] **Step 5: 寫 README 內容斷言測試**

```python
def t_slim_readme_assertions():
    """README 必要章節與關鍵句的內容斷言(spec [S5])。"""
    from pathlib import Path as _P
    p = _P(GRAPHCTL).parent.parent / "slim" / "README.md"
    txt = p.read_text(encoding="utf-8")
    for key, name in (
        ("lumos search", "進場三步"),
        ("lumos context", "進場三步-context"),
        ("lumos contracts", "進場三步-contracts"),
        ("鐵則", "frontmatter 四條鐵則"),
        ("移除的是入口不是全部程式碼", "功能子集聲明(★後半句才是 helper 澄清★)"),
        ("凍結快照", "★凍結聲明:不是發布通道★"),
        ("install-hooks.sh", "明講不要跑哪些"),
        ("doctor", "合約鏈與 doctor 解法"),
    ):
        check(f"README 含「{name}」", key in txt, f"缺 {key!r}")
    # ★交付文字掃描器也要掃 README★
    scanner = str(_P(GRAPHCTL).parent / "slim-scan.py")
    r = subprocess.run([sys.executable, scanner, str(p)], capture_output=True, text=True)
    check("README 無懸空引用(rc0)", r.returncode == 0, r.stdout)
```

- [ ] **Step 6: 跑測試確認失敗，再寫 README**

Run: `python3 scripts/test_lumos.py -k slim_readme` → FAIL

Create `slim/README.md`，必須包含（每項都被上面的斷言鎖住）：
1. 怎麼裝（`./install.sh`）、怎麼確認裝好（`lumos --help`）
2. 進場三步：`lumos search` → `lumos context` → `lumos contracts`
3. frontmatter 四條鐵則（從 `reference.md` 逐字轉錄）
4. 合約鏈是什麼、`doctor` 為什麼會擋、怎麼解
5. **明講範圍**：功能子集，不含對抗審計 loop；**移除的是入口不是全部程式碼**（共用 helper 仍在，別誤讀成功能還在）
6. **★明講不要跑哪些★**：不要跑 `scripts/install-hooks.sh`、不要照專案 `CLAUDE.md` 去 clone lumos-toolchain 跑 `install.sh`——並誠實說明「**本 README 壓不住專案自己的 CLAUDE.md**，那份優先級更高」
7. **★凍結聲明★**：這是凍結快照，不是發布通道，不會有更新；出問題請直接改 Python 原始碼

- [ ] **Step 7: 跑測試確認通過**

Run: `python3 scripts/test_lumos.py -k slim_readme` → PASS

- [ ] **Step 8: Commit**

```bash
git add slim/ scripts/test_lumos.py
git commit -m "feat(slim): 修剪後的 skill + 自足 README(含凍結聲明與內容斷言)"
```

---

## Task 5：四道驗證 + 交付凍結

**Files:** Modify `scripts/slim-gen.py`（複製 slim/ 內容進 dist/）、`scripts/test_lumos.py`（加 `t_slim_gate`）

- [ ] **Step 1: 讓生成器組出完整交付包**

在 `slim-gen.py` 的 `main()` 末尾加：

```python
    # 組交付包:slim/ 的內容原樣複製進 dist/
    import shutil
    pkg_src = here.parent / "slim"
    pkg_dst = out.parent.parent
    if pkg_src.is_dir():
        for item in ("install.sh", "README.md", "skills"):
            s = pkg_src / item
            if s.is_dir():
                shutil.copytree(s, pkg_dst / item, dirs_exist_ok=True)
            elif s.is_file():
                shutil.copy2(s, pkg_dst / item)
        (pkg_dst / "install.sh").chmod(0o755)
        print(f"✓ 交付包: {pkg_dst}")
```

- [ ] **Step 2: 寫四道驗證測試**

```python
def t_slim_gate():
    """交付前四道機械驗證(spec [S6])。★四道都不可跳★"""
    import tempfile as _tf, re as _re, json as _j, os as _os, shutil as _sh
    from pathlib import Path as _P
    repo = _P(GRAPHCTL).parent.parent
    root = _P(_tf.mkdtemp(prefix="gctl-slimgate-"))
    dist = root / "dist"
    r = subprocess.run([sys.executable, str(repo / "scripts" / "slim-gen.py"),
                        "--outfile", str(dist / "scripts" / "lumos")],
                       capture_output=True, text=True)
    check("gate: 生成 rc0", r.returncode == 0, r.stdout + r.stderr)
    cli = dist / "scripts" / "lumos"

    # 第 1 道 負向:--help choices 不得出現任何移除指令(逐支列名)
    keep = set("""append archive backlinks context contracts decision-add decision-reindex
decision-supersede decisions doctor export guard links lint map new recent
rel-cascade search set show stale stats sync-verified-by""".split())
    full = subprocess.run([sys.executable, GRAPHCTL, "--help"],
                          capture_output=True, text=True).stdout
    allc = set(_re.search(r"\{([a-z0-9,\-]+)\}", full).group(1).split(","))
    removed = allc - keep
    h = subprocess.run([sys.executable, str(cli), "--help"],
                       capture_output=True, text=True).stdout
    got = set(_re.search(r"\{([a-z0-9,\-]+)\}", h).group(1).split(","))
    check("★第1道 負向★ choices 不含任何移除指令", not (got & removed),
          str(sorted(got & removed)))
    check("★全覆蓋★ 保留∪移除 == --help 全集且零交集",
          (keep | removed) == allc and not (keep & removed), "")

    # 第 2 道 正向:子模式 —— ★驗 spec S6-3 的三來源判準,對真 parser 斷言★
    def _help(*a):
        return subprocess.run([sys.executable, str(cli), *a, "--help"],
                              capture_output=True, text=True).stdout
    # 來源③ 旗標帶 choices → 機械枚舉那類
    check("S6-3: export --format 有 choices(→機械枚舉)",
          "{mermaid,dot,html}" in _help("export").replace(" ", ""), _help("export")[:300])
    # 來源② positional 帶 choices
    check("S6-3: rel-cascade verb 有 choices(→機械枚舉)",
          "{confirm,prune,list,resume}" in _help("rel-cascade").replace(" ", ""),
          _help("rel-cascade")[:300])
    # 來源① subparsers
    g = _help("guard")
    check("S6-3: guard 有 7 支 subparser(→機械枚舉)",
          "{list,scaffold,bind,audit,trace,kill-add,kill}" in g.replace(" ", ""), g[:300])
    # 純旗標型:整份 help 不得出現任何 choices 的 {..} 形式
    for cmd in ("search", "archive"):
        check(f"S6-3: {cmd} 無任何 choices(→純旗標型)", "{" not in _help(cmd), _help(cmd)[:300])
    vault = mkvault()
    d = subprocess.run([sys.executable, str(cli), "--vault", str(vault), "doctor"],
                       capture_output=True, text=True)
    check("★正向 doctor 實跑★(不只 --help)", d.returncode in (0, 1), d.stderr[:300])

    # 第 3 道 等價:goldset search 30 條,完整版 vs 精簡版結果一致
    gs = _j.loads((repo / "governance" / "eval" / "retrieval-goldset.json")
                  .read_text(encoding="utf-8"))
    qs = [q["query"] for q in gs["search"][:10]]      # 取前 10 條做冒煙,全量在 Step 4
    same = 0
    for q in qs:
        a = subprocess.run([sys.executable, GRAPHCTL, "--vault", str(vault),
                            "search", q, "--files-only"], capture_output=True, text=True)
        b = subprocess.run([sys.executable, str(cli), "--vault", str(vault),
                            "search", q, "--files-only"], capture_output=True, text=True)
        if a.stdout == b.stdout:
            same += 1
    check("★第3道 等價★ search 前10條兩版一致", same == len(qs), f"{same}/{len(qs)}")

    # 第 4 道 真機預演在 Step 4 手動跑(需乾淨 HOME)
```

- [ ] **Step 3: 跑測試**

Run: `python3 scripts/test_lumos.py -k slim_gate`
Expected: PASS

- [ ] **Step 4: 手動跑第 4 道（真機預演，乾淨 HOME + 冪等重跑）**

```bash
python3 scripts/slim-gen.py
export FAKE=/tmp/slim-rehearsal && rm -rf "$FAKE" && mkdir -p "$FAKE"
git clone -q /Users/enzo/backend/LandmarkMember "$FAKE/proj" 2>/dev/null || \
  echo "★改用一份可拋棄的 clone,不要動原 repo★"
( cd "$FAKE/proj" && git status --porcelain > "$FAKE/before.txt" )
HOME="$FAKE/home" bash dist/install.sh
( cd "$FAKE/proj" && git status --porcelain > "$FAKE/after.txt" )
diff "$FAKE/before.txt" "$FAKE/after.txt" && echo "✓ 專案 worktree 未被動"
HOME="$FAKE/home" bash dist/install.sh --force && echo "✓ 冪等重跑成功"
HOME="$FAKE/home" "$FAKE/home/.local/bin/lumos" --vault "$FAKE/proj/docs/landmark-knowledge" doctor
```
Expected: 專案 worktree 無變化、第二次跑成功、`doctor` 在真架構圖上跑得動。

- [ ] **Step 5: 全量等價驗證（goldset 30 條）**

```bash
python3 - <<'EOF'
import json, subprocess, sys
gs = json.load(open("governance/eval/retrieval-goldset.json"))
vault = "docs/lumos-toolchain-knowledge"
bad = []
for q in gs["search"]:
    a = subprocess.run([sys.executable, "scripts/lumos", "--vault", vault, "search", q["query"], "--files-only"], capture_output=True, text=True)
    b = subprocess.run([sys.executable, "dist/scripts/lumos", "--vault", vault, "search", q["query"], "--files-only"], capture_output=True, text=True)
    if a.stdout != b.stdout: bad.append(q["query"])
print(f"不一致 {len(bad)}/{len(gs['search'])}", bad[:5])
EOF
```
Expected: `不一致 0/30`

- [ ] **Step 6: 全套回歸 + 分支終審**

```bash
python3 scripts/test_lumos.py
python3 scripts/lumos pitfalls --diff main..HEAD | tail -3
```
若 `tier: high` → **調用 `lumos-code-loop` skill 做對抗代碼審**，收斂後 `lumos code-loop pass --note "<理由>"` 留痕才能 push。

- [ ] **Step 7: Commit + 交付**

```bash
git add scripts/slim-gen.py scripts/test_lumos.py
git commit -m "feat(slim): 四道機械驗證 + 交付包組裝"
```

**交付（★由使用者執行★）**：`dist/` 內容 push 進新 repo 並打 tag／設 archived。**repo 擁有者必須是公司／團隊而非個人帳號**——否則重演「維護者離開，東西跟著消失」，那正是本案要解的問題。

---

## Task 17（計劃外，2026-08-01 真跑冒煙抓到）：卸載補第⑤步——manifest 自己也要清掉

**觸發**：對打包後的 `dist/`（不是 repo 內副本）做端到端真跑冒煙時發現——卸載跑完會把 `~/.local/share/lumos-slim/manifest.json` 留在使用者 `$HOME`，彙總卻印「✓ 全部完成」。**2011 條測試全綠都看不到**，因為沒有任何一條去斷言它該消失。

★盲區型別★：這次是「**根本沒去驗**」，不是「斷言寫錯」——與本專案已撞十次的「測試存在但沒在驗它宣稱要驗的」互補，兩者都不是靠加測試數量能解的；接住它的是**對交付產物本身的端到端真跑**（本專案第二次由真跑接住測試套件盲區，第一次是兩行安裝路徑下卸載完全失效）。

**修法**：`slim/uninstall.py` 加第⑤步，移除 manifest 與其空掉的父目錄 `lumos-slim/`。★唯一的資料相依（非控制相依，不違反「各步互不阻擋」那條 ★INVARIANT★）★：①/①b 任一份 bin 檔案基於安全考量未移除時，manifest 刻意保留並印一句為什麼——它是使用者之後加 `--force`／手動確認時唯一的比對基準。★爆炸半徑★：只刪該檔與其空父目錄，`~/.local/share` 底下其他一律不碰。

**還原翻紅釘**：突變 A（⑤整段移除）→ `t_slim_uninstall_removes_manifest_when_bin_cleared` 翻紅；突變 B（`if bin_cleared:` → `if True:`）→ `t_slim_uninstall_keeps_manifest_when_bin_refused` 翻紅；還原後 `-k slim_uninstall` 75 passed / 0 failed。

**順手修的兩處過時文字**：`slim/claude-block.md` 仍寫「由 `install.sh` 自動維護」（邏輯已全數改成 Python）；`slim/skills/.../reference.md:679` 指向本包未交付的設計全文與 golden 語料路徑（見 [[Systems/slim-skill-修剪]]，`slim-scan.py` 掃不到路徑型懸空引用）。

---

## 收尾：寫回架構圖

- [ ] 建 `Verification/2026-XX-XX_公開精簡版交付.md`，`plan_refs` 指回 `[[Projects/公開精簡版_計劃]]`
- [ ] 記：四道驗證的實跑結果、`$(dirname "$0")` 邊界實測結論、skill 修剪的假陽性裁決數
- [ ] 把 spec 未結清的 10 條現況更新進 `.lumos/settle/公開精簡版-settle.json`（實作驗證過的可推進到 `mech-ok`）
- [ ] `python3 scripts/lumos doctor`


## 待辦

- [ ] ★**把交付端的 hooks 功能回流進 `slim/` 範本**（2026-08-12 開）★——不做的話，任何人重新生成 `dist/` 再整檔覆蓋都會炸掉交付端的 hooks 功能與兩個守衛修。回流後 `slim-gen.py` 組包清單要同步擴充（第四次預防點）。
- [ ] **交付前比對紀律**：推 `Citrus_Lumos` 前一律先 `diff -rq dist/ <clone>/` 逐檔判「誰比較新」，**禁止整檔覆蓋**；判不出來就停下來問人。
