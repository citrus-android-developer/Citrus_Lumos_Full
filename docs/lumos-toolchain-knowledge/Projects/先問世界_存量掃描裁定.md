---
type: project
status: done
created: 2026-07-07
updated: 2026-07-07
related:
  - "[[自主loop加法偏食]]"
  - "[[lumos-cli-lifecycle]]"
  - "[[pitfalls-code-loop]]"
tags:
  - type/project
  - status/done
summary: |-
  FLAG:DECISION
  KEY:「先問世界」存量掃描(2026-07-07,三路深度研究:①追溯/ADR=Doorstop/StrictDoc/Sphinx-needs/adr-tools ②hooks/mutation=pre-commit/lefthook/mutmut/cosmic-ray/Stryker ③doc-drift/AI-review/SDD/judge=Swimm/PR-Agent/CodeRabbit/spec-kit/OpenSpec/promptfoo/Giskard)——真讀 docs/原始碼/patent/學術文獻,對照 lumos 現況,裁定 borrow-now 16 / borrow-later 7 / skip 14
  KEY:最大收穫=suspect 語意(兩層合一設計):Doorstop 邊級(連結攜帶目標 summary 內容指紋,不符=suspect,明確 clear 才解除)+ Swimm 行級(節點引用的 code 段內容 hash,difflib 重定位吸收搬家,dismiss 留痕)——直擊「世界變了 code 沒變」的機械解,全場唯一完全空缺的漂移語意;唯一需 design-loop 的中型項
  KEY:立即可落(純文字/prompt 4 條):PR-Agent 抑噪紀律(給不出具體場景不准標/禁無路徑臆測/findings 上限)、[NEEDS CLARIFICATION] 未解=擋收斂、mutation 算子速查(ROR+LCR 預設/跟 diff 形態/同比較式僅 3 非冗餘變體)、timeout=skipped 不算 finding + Survived(斷言缺口)vs NoCoverage(測試缺,更強)
  KEY:小 code 10 條:decisions 鏈完整性 Check/decision-add --supersedes 成對寫/rebase 偵測跳 pre-commit/LUMOS_SKIP 細粒度跳閘留痕/hook-local.sh 消費端 override/mutation-filter(~25行 diff 行過濾)/FP learnings 留痕(辯方殺的假陽性持久化+下輪注入,context 注入非硬濾)/收斂即凍結 golden 語料(auditor 校準前置,語料=時間資產)/計劃節點 AC-n 編號驗收+核銷/code-loop 輪間增量審(ledger 已綁 sha 地基現成)
  KEY:誠實反向發現=lumos 在「決策時效性」(valid_under/revalidate_when/ADR 四欄位)領先全部研究對象;Check T 認真方法強於 StrictDoc;Check D 機械守衛對單人+AI 強於 spec-kit constitution SemVer 修憲儀式
  KEY:貫穿發現(體質印證)=成熟工具三共識與 lumos 同宗——①狀態存工件本身非外部 DB ②機械先驗、LLM 後判 ③抑噪走 context 注入的機率性抑制而非硬過濾表
  DECISION:[2026-07-07] 家規定案=borrow-design(預設,借精神原生實作)/build(真沒輪子)/adopt(例外須理由,零依賴下幾乎恆排除);skip 的理由同等重要(delta-spec=第二真相源禁忌/LLM effort=逆決定論/constitution 儀式<機械守衛)
  DEP:[[自主loop加法偏食]]
  TEST:批次1(純文字5條)已落地 2026-07-07——抑噪紀律+刻意不借findings硬上限(防污染G2)/算子速查/timeout+Survived-NoCoverage/[NEEDS CLARIFICATION]慣例/收斂即凍結golden——落點 code-loop+design-loop SKILL+orchestrator prompt(鏡像防散落);批次2(小code 10條)與 suspect語意(design-loop)待做
---
# 「先問世界」存量掃描裁定(2026-07-07)

三路平行深度研究(WebSearch/WebFetch 真讀官方 docs、GitHub 原始碼、Swimm patent US11132193B1、學術文獻 Offutt 1996/Kurtz FSE 2016),對照 `scripts/lumos` 實碼逐項核實 lumos 現況後裁定。緣起:[[自主loop加法偏食]] 的外延——SDD 管線沒有「先問世界」步驟(已同步制度化為 PRIOR-ART 三問)。**家規:borrow-design(借精神原生實作,零依賴不變),不 adopt。**

## Borrow-now(16 條,按落地成本排序)

### 立即可落(純文字/prompt,4 條)
| 借什麼 | 來源 | 落點 |
|---|---|---|
| 抑噪紀律:「給不出具體場景的低嚴重度疑慮不准標」「不能從 diff 指出受影響路徑不准臆測」+ findings 上限 | PR-Agent(原始碼實證) | lumos-code-loop SKILL reviewer prompt |
| `[NEEDS CLARIFICATION: 問題]` 標記慣例:計劃節點內未解 → design-loop gate 視同 blocker | spec-kit | 慣例 + loop status --gate 一行 grep |
| mutation 算子速查:ROR+LCR 預設、跟 diff 形態調(計算密集→AOR/條件密集→ROR+LCR)、同一比較式只植 3 個非冗餘變體(`<=`/`true`/`false`);無普適最優集(Kurtz FSE 2016) | Offutt/PItest/Stryker | lumos-code-loop SKILL 步驟 7 |
| timeout → `skipped(timeout)` 不算 finding;活變異分 **Survived**(測試跑到但沒紅=斷言缺口)vs **NoCoverage**(行根本沒被執行=測試缺,更強 finding) | Stryker/cosmic-ray | 同上 |

### 小 code(10 條)
| 借什麼 | 來源 | 落點 | 備註 |
|---|---|---|---|
| decisions 鏈完整性 Check:`valid:false` 缺 `superseded_by`/`ended` → warn;`superseded_by` 含 wikilink 驗可解析 | mdbook-lint ADR010/013 | doctor 新 Check(parse_decisions 現成) | 堵手改 frontmatter 繞過 write-path 防護的現存洞 |
| `decision-add --supersedes "<子字串>"`:新決策帶 `supersedes:` + 舊決策 supersede 手術,原子成對寫 | adr-tools | 組合兩個現有 surgical 函式 | 現在讀現行決策看不到「它取代了誰」 |
| rebase state 偵測(`.git/rebase-merge` 存在)→ pre-commit 放行 | pre-commit framework | scripts/hooks/pre-commit | |
| `LUMOS_SKIP=<gate-id>` 細粒度跳閘 + stderr/bypass-log 留痕 | pre-commit SKIP 語意 | hooks | 比 --no-verify 全跳細,且留痕 |
| `.lumos/hook-local.sh` 消費端 override(gitignore + source-in) | lefthook local config | hooks | 消費專案自訂/停用的正規出口(現在只能吃全套) |
| `lumos mutation-filter`:`git diff -U0` hunk 行號集 JSON(~25 行 stdlib) | cosmic-ray cr-filter-git(唯一原生 diff-scoping) | 新小子命令 | code-loop 步驟 7 的機械輔助 |
| FP learnings 留痕:辯方裁定假陽性的 finding(pattern+理由)→ governance/code-loop/learnings.jsonl(規劃路徑,批次2待做未建),下輪 reviewer prompt **注入為 context**(非硬濾,保留重判空間) | CodeRabbit learnings | code-loop | 重複假陽性污染 findings-exhaustion 收斂信號 |
| 收斂即凍結:converged spec 快照 + 辯方裁決後存活 findings → governance/golden/(規劃路徑,批次2待做未建) | Giskard meta-evaluation | design-loop 收斂尾 | 零判斷成本純搬運;auditor 校準(borrow-later)的前置,語料=時間資產 |
| 計劃節點 `## 驗收場景`(`AC-1: GIVEN…WHEN…THEN…` 編號)+ 核銷檢查(計劃 done 但 AC 無核銷→warn) | spec-kit/OpenSpec | 慣例 + Check 4 延伸 | ⚠ 借的是「機器可核銷編號」非「思考模板」(2026-06-12 已否決模板化盤問,定位要守住);GWT 直通 guard scaffold --claim |
| code-loop 輪間增量審:round 2+ 只審 `上輪 head_sha..HEAD` + 復核上輪存活 findings;fallback 全審 | PR-Agent(stateless comment 錨實證) | code-loop(ledger 已綁 sha) | |

### 中(1 條,唯一需正式 design-loop)
**suspect 語意(兩層合一)**——「世界變了 code 沒變」的機械解,lumos 唯一完全空缺的漂移語意:
- **邊級(Doorstop)**:Verification 寫入/重驗時記「被驗 Systems 的 summary block sha8」進 governance/link-stamps.json(規劃路徑,中型項待 design-loop 未建);doctor 比對現行 hash → 不符=suspect 驗證;`lumos stamp clear` 明確重蓋章。血換教訓:**只 hash 載重欄位**(否則 typo 修正全圖 suspect)。
- **行級(Swimm patent)**:節點引用 `path:line` 時存行內容 hash+摘錄 baseline;doctor 重驗:內容不變=綠;**變了但 difflib 能重定位同 snippet=自動修行號(吸收最大宗假陽性「只是搬家」)**;定位不到=suspect;+ `dismiss --note` 留痕(Swimm 自己沒有的逃生口,避免強制 review 疲勞)。
- Swimm 證明**不用 AST、純 diff+內容比對堪用**——正對零依賴體質。動 anchor/Check P 合約邊界 → 過 design-loop。

## Borrow-later(7 條)
self_audit 日期→summary-hash(Doorstop review stamp;suspect ledger 建好順手)/ 未覆蓋 Systems 檢查+`coverage: exempt` 豁免旗標(豁免先行否則全噪音)/ `.lumos/warnings.json` 專案自訂宣告式檢查(限三種斷言禁 eval;等第一個專案特有需求)/ findings 必附機器可驗座標(G1 擴到 review findings)/ golden replay 校準 auditor(等 golden 語料 10+ 份;外部人標錨,非已 rejected 的自證悖論)/ vendor-lock.json 記 SHA / hook-config 統一 CODE_EXTS 宣告。

## Skip(14 條,理由同等重要)
需求層級樹、traceability matrix 視圖、查詢語言、ROLE 標籤、環偵測、Proposed/Rejected 狀態(計劃節點+alternatives_considered 已更好)、code 檔 sha 引用(天天 suspect 必噪)、staged-stash、fail_fast、parallel gates、coverage-guided 選測試(3-5 抽樣不需要)、**LLM 判 review effort(risk-tiered 機械分級已覆蓋,LLM 判是倒退)**、**constitution SemVer 修憲(Check D 機械守衛對單人+AI 強於治理儀式)**、**delta-spec 格式(第二真相源,犯架構圖唯一真相禁忌)**、inter-rater κ(n 太小)、Swimm auto-rewrite 散文(它自己都只敢改機械錨)。

## 誠實反向發現(lumos 領先處)
- **決策時效性**(`valid_under`/`revalidate_when`/`stale --match`/ADR 四欄位):領先全部研究對象——整個 ADR 生態沒人做「accepted 之後還算不算數」。
- Check T 認真方法(discover 白名單)強於 StrictDoc 的 marker 存在性檢查。
- 反向 impact(code→節點)是 StrictDoc 沒有的。

## 貫穿發現(體質印證)
成熟工具三共識,lumos 全數同宗:①**狀態存工件本身**非外部 DB(PR-Agent comment 錨/Swimm .swm 進 repo ↔ lumos governance/ 留痕)②**機械先驗、LLM 後判**(Swimm 不用 AST/CodeRabbit sandbox 先 grep ↔ lumos refcheck/pitfalls)③**抑噪走 context 注入**非硬過濾表(CodeRabbit learnings ↔ FP 留痕移植時守住此語意)。
