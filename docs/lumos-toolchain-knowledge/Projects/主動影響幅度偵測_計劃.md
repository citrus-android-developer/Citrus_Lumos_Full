---
type: project
status: done
created: 2026-07-05
updated: 2026-07-20
self_audit: sonnet/2026-07-20
tags:
  - type/project
  - status/done
related:
  - "[[lumos-refcheck]]"
  - "[[lumos-cli-read]]"
  - "[[anchor-integrity]]"
  - "[[檢索優化_計劃]]"
summary: |-
  FLAG:DECISION
  KEY:補「pull 非 push」缺口——動手前主動算出將被改動 code 的 downstream 影響節點集、推到 Claude 眼前;現況影響幅度資料有建模(雙向 edges+合約鏈)、查詢原語齊(context/guard trace/backlinks)但無「主動掃描」,靠紀律+pre-commit gate 只保證「有沒有碰架構圖」不保證「有沒有看到波及面」
  FLOW:Claude 正要 Edit/Write/MultiEdit code → PreToolUse hook 攔(只 code 副檔名)→ 查 session TTL 快取 → lumos impact --file <path> --json 算清單 → hook 注入 additionalContext(不擋)→ Claude 動手前判直接/間接節點會不會受影響、消噪、該同步就同步
  KEY:分工=lumos 只算不判(出直接/間接分層清單)｜Claude 只判不算(語意深度分析消噪)｜hook 只搬運+節流(攔截/過濾/TTL/注入)
  KEY:影響集=遞移展開;直接關聯(1-hop,body inline-code 路徑反查命中;core_refs 是跨repo節點指針非code路徑、不做code反查 r7-F2)+ 間接關聯(hop 1..depth:related/verified_by/plan_refs/body-wikilink 走 env.edges 雙向、core_refs 當跨repo不可展開葉;summary DEP:/VERIFY: 不涵蓋列後續);每筆標層級+方向(backlink/outlink r7-F1)+是否帶合約(INVARIANT/IRREVERSIBLE 排前)
  KEY:演算法即時、零落地索引(方案 A);反查需 impact 呼叫時重讀盤每個 .md 餵 _refcheck_scan(r1-F1:load_vault 不存 body、:184 先刪 inline-code,無法直接複用記憶體);邊集=env.edges 的 related/verified_by/plan_refs+body wikilink(r5-F5,雙向);core_refs 當跨repo不可展開葉(r7-F2);summary 的 DEP:/VERIFY: 不進 env.edges 列後續;避 staleness/避漂移 artifact
  KEY:方向不由 lumos 判——全撈標方向(改 X 受影響主要是反向依賴/backlinks),交 Claude 判(全撈+AI 消噪原則)
  DECISION:trigger=PreToolUse hook(Claude 動手前;人手動改 code 不觸發=明確接受範圍)
  DECISION:把關強度=注入+動手前分析(附指令要 Claude 判並回應,不強制真改;非純 FYI、不串 commit 對帳)
  DECISION:hook 永不 block、永不改 permissionDecision;空影響集不注入;退出碼協定(rc0=成功/rc3=vault 找不到印 debug/其他=出錯純靜默 fail-open)
  DECISION:dedup=TTL 冷卻窗(預設 20min,.lumos/impact.json 可調,對齊 lint.json/compose-metrics.json per-feature 慣例)取代「per-session 一次」——長 session 隔久回頭再改同檔會復活、不啞巴;連續 iterate 存檔靜音
  DEP:[[lumos-refcheck]]
  DEP:[[lumos-cli-read]]
  DEP:[[anchor-integrity]]
  TEST:已實作(branch feat/impact,634 passed;9 輪 design-loop + TDD 12 task + opus 終審[live 重現 C1/I1 2 blocker 真機修]);VERIFY:[[2026-07-05_主動影響幅度偵測]]
  DECISION:[2026-07-20]PRIOR-ART 複查裁定不借 code-review-graph(AST 呼叫圖擴大候選集)——釘定快照實測候選池中位32/最大102、自由席僅1-10、完美排序+0.0pp、必看漏11條中11條在池內被閾值砍;召回從未失敗,瓶頸在篩選不在供給
  KEY:跨專案覆蓋率實測(2026-07-20,6專案)——覆蓋率一律用「動手次數加權」報,按檔案數算嚴重低估(Landmark 檔案20.6% vs 動手55.2%、最常改前10支10/10有覆蓋);低覆蓋專案(KDS 5.8%/Frasers 8.8%)是架構圖沒建非機制失效(KDS 全圖僅7條有效code路徑引用)
decisions:
  - content: trigger 用 PreToolUse hook(Claude 動手前注入),非 pre-commit
    id: d1
    context: 使用者原寫 pre-commit/hook,但兩者時機差很多;在意 loop engineering 順順執行
    why_chosen: PreToolUse 是真·動手前(code 未寫)、貼合 autonomous loop;pre-commit 是動手後偏晚。代價=只在 Claude session 內生效、人手動改 code 不觸發,使用者明確接受此範圍
    decided: 2026-07-05
    valid: true
  - content: 影響集即時純記憶體算、零落地索引(方案 A)
    id: d2
    context: 反查索引可落地(.lumos/impact-index.json)加速大架構圖
    why_chosen: lumos 本來就全圖載入記憶體(200 篇無感);落地索引引入 staleness + 多一個會漂移 artifact,違 vault-free/fail-open。使用者選 A
    decided: 2026-07-05
    valid: true
  - content: dedup 用 TTL 冷卻窗(預設 20min)取代 per-session 一次
    id: d3
    context: 使用者習慣單一長 session 保脈絡保證架構圖寫入完整
    why_chosen: per-session 一次會讓長 session 隔久回頭改同檔時、早先注入已被捲走/壓縮 → 第二次修改變啞巴,偏偏那是提醒最該出現時。TTL 把連續 iterate(靜音)與隔久回頭(復活)乾淨分開
    decided: 2026-07-05
    valid: true
  - content: .lumos/impact.json 獨立 feature 檔存 config(非併入 config.json)
    id: d4
    context: r1-F8 審計員誤認 .lumos/config.json 是統一配置入口、建議併入;r3-F2 代碼實證 config.json 只放 test_profile/platforms,lint/compose-metrics/lint-watch 各用獨立 .lumos/*.json
    why_chosen: 對齊真實 per-feature 慣例(lint.json/compose-metrics.json/lint-watch.json);design-loop 允許用更深代碼事實翻正早輪審計修正
    decided: 2026-07-05
    valid: true
verified_by:
  - "[[2026-07-05_主動影響幅度偵測]]"
---
# 主動影響幅度偵測_計劃

> **狀態**:**已實作並通過 opus 終審**(branch feat/impact,634 passed)。設計經 `lumos-design-loop` 9 輪對抗審計(canary 9/9、抓 2 真算法 bug)→ writing-plans 12 task → subagent-driven TDD → opus whole-branch 終審(live 重現 C1/I1 2 blocker 真機修)。驗證見 [[2026-07-05_主動影響幅度偵測]]。

## 定位:自動修復的偵測地基

**本原語是 lumos 主打「關聯節點自動修復」的基礎建設。** `lumos impact --json` 的輸出不只餵給本計劃的 advisory 注入,更是**下游自動修復消費端的機器可讀輸入**——故 `--json` schema 必須**machine-actionable 且無歧義**:直接/間接分層、`hit`/`via` 邊型、`contract`/`combo` 標記、`node` rel 路徑,每一欄都要下游能不靠人判就消費。這抬高了 schema 精確度、邊集完整性(不漏 plan_refs/body-wikilink)、合約偵測正確性(IRREVERSIBLE 走對 RE)的標準——它們不是本計劃的錦上添花,是自動修復地基的承重牆。

## 背景:pull 非 push 的缺口

如今有 lumos 關聯節點輔助,維護某節點功能時**能不能主動偵測相關節點的影響幅度?** 查完架構圖結論:**影響幅度的資料完整建模了**(雙向 edges、合約→測試→Verification 鏈),**查詢原語也齊了**(`context`/`guard trace`/`backlinks`/`map`/`stale --candidate`)——**但「你正在改 X、這是波及面」不會自己跳出來**。

它靠兩件事驅動,都不是主動掃描:
1. **紀律**:CLAUDE.md「架構圖先行」*告訴* agent 動手前跑 `context`/`contracts`/`guard trace`——靠 agent 記得、誠實。
2. **機械閘**:pre-commit graph-sync gate *強迫*你碰架構圖——但它「改完了才擋」,且只判「有沒有碰架構圖」,**不算波及面**。

→ 它是 **pull(你去查)**,不是 **push(它來提醒)**。與記憶 `knowledge-sync-scatter-needs-mechanical-guard`(非架構圖節點)同病根:同步散落會漏、需機械守衛逼。本計劃補這個缺口。

## §1 架構與分工

> 落點(2026-08-08 補鏈,link-candidates 人裁採納):本計劃的 hook 端實作=`scripts/hooks/claude/impact-hook.py`(攔截/節流/注入三職)。

沿用 lumos「Claude 編排、lumos 出原語」:

```
Claude 正要 Edit/Write/MultiEdit 一支 code
        ↓
[PreToolUse hook]  攔截 → 過濾(只 code 副檔名) → 查 session TTL 快取(這支檔近期注入過?)
        ↓ 未在冷卻窗內
  lumos impact --file <path> --json        ← lumos 出機械完整清單
        ↓
  hook 把清單 + 動手前分析指令 注入 session(additionalContext,不擋)
        ↓
Claude 動手前:判直接/間接節點會不會被這次改動波及、需不需同步 → 消噪 → 該改的改
```

- **lumos 只算、不判**:出「直接/間接」分層清單。
- **Claude 只判、不算**:拿清單做語意深度分析、消噪。
- **hook 只搬運 + 節流**:攔截、過濾、per-file TTL 快取、注入。

## §2 `lumos impact` 原語

**輸入**:`lumos impact --file <code路徑>`(**必填**)/ `--json` / `--depth N` / `--repo <root>`(r4-F5:hook 必傳,缺則退回 `find_vault`,見 §3)。
> r8-F3:**v1 只做 `--file <code路徑>` 一種入口**(hook 唯一用法);先前「也支援 `lumos impact <節點>` 給人手動查」**砍掉**(YAGNI)——節點→節點影響是不同算法(無 code→node 反查步驟),硬塞會留一整塊未定義分支。日後真要人手查節點鄰域,已有 `lumos context`/`map`/`backlinks` 覆蓋。

**演算法**(方案 A:即時、純記憶體、零落地索引):
1. **建 code→node 反查**:掃全節點 body inline-code 裡的 `code路徑`(複用 `_refcheck_scan` 的抽取)。
   - ⚠ **不掃 `core_refs`**(r7-F2,grep 實證):`core_refs` 是**跨 repo 節點指針**(doctor Check C `scripts/lumos:526-551` 讀作 `repo_root.parent/<seg>` 的 core-knowledge `.md` 節點路徑),**不是 code 引用路徑**——拿它比對「被改動的 code 檔」永遠不命中。code→node 反查**只靠 body inline-code**。(前 6 輪一直把 core_refs 誤當 code 反查來源,r7 修正。)
   - ⚠ **`_refcheck_scan` 的 `top_dirs` 過濾**(r7-F6):`_refcheck_scan`(`scripts/lumos:3944-3948`)會**靜默跳過**首段不在 repo 頂層目錄集的 token(`token.split("/")[0] not in top_dirs`)。故 body 裡寫非頂層開頭的 code 路徑不會被反查到——這是機械限制,列入 §4 天花板。
   - ⚠ **不能直接複用現有 `load_vault` 記憶體**(r1-F1):`Note.__slots__`(`scripts/lumos:160`)**不存 body**;且 `load_vault:184` 在算 `targets` *之前* 就用 `INLINE_CODE_RE.sub("","")` **把 inline-code 整段刪掉**——記憶體裡連反查原料都沒有,`_refcheck_scan(text,...)` 吃的又是原始 text 字串。→ **實作二選一(spec 選前者)**:(a) impact 呼叫時**重新讀盤每個 .md** 把 text 餵 `_refcheck_scan`(即時、零落地索引成立,符合方案 A);(b) 擴充 `Note` 存 body inline-code spans。選 (a):200 篇讀盤成本無感、不動 Note class。
   - ⚠ **路徑規範化**(r3-F5):hook 傳來的是**絕對路徑**(`/Users/.../scripts/lumos`),但節點 body/`core_refs` 記的是 **repo-relative token**(`scripts/lumos`)。反查前須把絕對路徑轉 repo-relative(`os.path.relpath(abs, repo_root)`)再比對,否則永遠 miss。`_refcheck_scan` 需 `repo_root` 參數(見 §3 hook 傳 `--repo`)。
2. **直接關聯(1-hop)** = 反查命中的節點。
3. **間接關聯** = 從直接節點集當種子沿邊 BFS 展開。**hop 語意**(r8-F5):hop = 距最近直接節點的 BFS 距離,`hop ∈ 1..depth`(hop 1 = 直接節點的鄰居);`depth`(預設 2,見 §3 config)= 最大 hop。(廢除舊「2..depth」記號——那與 schema 的 hop=1 矛盾。)
   - ⚠ **visited/cycle guard**(r8-F4):BFS 必須維護 `seen` set(**種入全部直接節點**),已在 direct 或已訪的節點**不再進 indirect**(lumos 架構圖 related 常互指、A↔B 環正常;複用 `map` 的 `seen` 模式 `scripts/lumos:908-923`)。否則直接節點會沿環重入 indirect,下游自動修復讀到「同節點既 direct 又 indirect」矛盾資料。
   - ⚠ **雙向 direction 衝突**(r8-F7):若 dest 同時經 `out_e`(outlink)與 `in_e`(backlink)可達 → **標 `backlink` 優先**(反向依賴=改 source 波及 dest,自動修復最該看)。
   - ⚠ **邊集分兩路**(r1-F2 + r2-F1):`env.edges`(`scripts/lumos:251-263`)由 `n.targets` 建,而 `n.targets` 只收 **wikilink `[[..]]` 格式**且**不含 summary block scalar 內的**(`load_vault:186-197` 依鐵則 2 整段排除 block、`fullmatch [[..]]` 濾非 wikilink)。故:
     - **`related` / `verified_by` / `plan_refs`(YAML list wikilink)+ body 內的 `[[..]]`**(r5-F5)→ 全都真在 `env.edges`(`n.targets = extract_targets(body_clean) + fm_targets`,body wikilink 與 plan_refs 都入),**雙向**含 backlinks,直接走 BFS。BFS 別只過濾 related/verified_by、漏掉 plan_refs 與 body wikilink 邊。
     - **`core_refs`**(r2-F1:純文字路徑非 wikilink,`scripts/lumos:526-551` 讀作跨 repo 節點路徑)→ **不在 `env.edges`**,且目標節點**住在 core-knowledge repo、不在本 `env.notes`**(r7-F2)。故:直接節點若有 `core_refs`,**把該 core 節點當「不可展開的跨 repo 葉」輸出**(標 `via:core_refs`、`cross_repo:true`、`no_expand:true`)——**不試圖 BFS 進它**(`env.notes[dest]` 會 miss)。讀法 `as_list(n.fields.get("core_refs"))`(r3-F8:值可能 str/list,直接 iterate 字串會拆單字元)。
     - **summary 的 `DEP:[[..]]`/`VERIFY:[[..]]`** 語意邊 → **不在 `env.edges`**,列為獨立後續工作項(需另建 summary-line 解析器繞過鐵則 2),v1 明列不涵蓋。
   - ⚠ **到達路徑標記需新 infra**(r1-F5):`env.edges` 只存 `(source,dest)` pair、**無 edge type**。要標 `via`,**選 BFS-time 二次反查**(r4-F4),**含 wikilink 所在端**(r9-F1,關鍵——`env.edges` 是 `out_e[a].append(b)`/`in_e[b].append(a)`,wikilink 永遠寫在「連出去」那一端):
     - **outlink**(dest ∈ `out_e[frontier]`):wikilink 在 **frontier** → 讀 `frontier.fields` 找指向 dest 的 wikilink → 該欄位即 `via`。
     - **backlink**(dest ∈ `in_e[frontier]`):wikilink 在 **dest**(dest 連向 frontier)→ 讀 **`dest.fields`** 找指向 frontier 的 wikilink → 該欄位即 `via`。(**別讀 frontier.fields**——它不含指向 dest 的連結,會全 miss。)
     - 比對走既有 `link_target()`+`env.resolve()` 把 `"[[stem]]"` 轉 rel 再比(r6-F6,同全 lumos pattern)。
     - **body wikilink fallback**(r5-F3):`n.fields` 只有 frontmatter,若在「wikilink 所在端」的任何 frontmatter 欄位都找不到 → 該邊來自 body(`extract_targets(body_clean)`)→ 判 `via:body-wikilink`。
     - **不改 `env.edges` 建構**(全 lumos 共用核心 infra);二次反查只在 impact 自己 BFS 內做、隔離。
4. **排序規則**:合約節點(★INVARIANT★/★IRREVERSIBLE★)優先 → 直接關聯優先於間接 → 同層依 hop 距離升序。每筆標:**層級**(直接/間接第幾 hop)+ **到達路徑**(直接=命中特徵 `hit`;間接=邊型 `via`,見 §2 schema)+ **是否帶合約**。
   - ⚠ **合約偵測分兩軸**(r6-F3):`★INVARIANT★`/`★DEBT★` 走 `extract_contracts`(`scripts/lumos:934`,只掃這兩者);`★IRREVERSIBLE★` **不在** `extract_contracts`、走獨立 `IRREVERSIBLE_RE`(`:1184`,可逆性軸)。impact 要拿到 `IRREVERSIBLE` 合約標記須**另外用 `IRREVERSIBLE_RE` 掃 summary**,只調 `extract_contracts` 永遠拿不到。

**輸出範例**(人讀,與下方 `--json` 同一組資料;depth=2):
```
改動 scripts/lumos (由讀盤 refcheck 抽取)
── 直接關聯 (2) ──
  ⚠合約 Systems/lumos-refcheck   ★INVARIANT★★COMBO★ (body inline-code 命中)
        Systems/lumos-cli-read              (body inline-code 命中)
── 間接關聯 (2) ──
  hop1  Systems/pitfalls-lint-adapter   backlink ← via related ← lumos-cli-read
  hop1  core-knowledge/systems/rule     outlink ← via core_refs ← lumos-cli-read [跨repo葉,不展開]
```
> 註(r1-F4):本架構圖的 `lumos-refcheck.md` **無 `core_refs` 欄位**;它被反查命中是靠 body inline-code 的 `` `scripts/lumos` ``——這也印證 code→node 反查只認 body inline-code(r7-F2)。

**`--json` schema**(r2-F7;下游自動修復消費端的機器可讀契約——每欄型別/可空/枚舉/presence 皆定死,不靠人判):
```json
{
  "file": "scripts/lumos",
  "direct": [
    {"node": "Systems/lumos-refcheck", "hit": "body-inline-code", "contract": "INVARIANT", "combo": true},
    {"node": "Systems/lumos-cli-read", "hit": "body-inline-code", "contract": null, "combo": false}
  ],
  "indirect": [
    {"node": "Systems/pitfalls-lint-adapter", "hop": 1, "via": "related",
     "direction": "backlink", "from": "Systems/lumos-cli-read", "contract": null, "combo": false},
    {"node": "core-knowledge/systems/rule", "hop": 1, "via": "core_refs",
     "direction": "outlink", "from": "Systems/lumos-cli-read",
     "cross_repo": true, "no_expand": true, "contract": null, "combo": false}
  ]
}
```
（範例:兩筆 indirect 皆 `hop:1`=直接節點 `lumos-cli-read` 的鄰居,`from` 指該直接節點,hop 語意一致。）
- **欄位定義**(每筆物件):
  - `node`(str,必有):vault rel 路徑;**跨 repo 葉例外** = core-knowledge 節點路徑(`cross_repo:true` 時)。
  - `direct.hit`(str,必有,r3-F3)= 反查命中特徵,**枚舉只 `body-inline-code`**(r7-F2:core_refs 已不做 code 反查,故 hit 不再有 `core_refs`)。
  - `indirect.via`(str,必有)= BFS 邊型,**完整枚舉 5 種**(r5-F5/r7-F5,一處列全):`related`/`verified_by`/`plan_refs`/`body-wikilink`/`core_refs`。
  - `indirect.direction`(str,必有,**r7-F1**)= `backlink`(dest 連向 source,即改 source 波及 dest 的反向依賴,自動修復最該看)或 `outlink`(source 連向 dest)。從 `env.edges` 的 `in_e`/`out_e` 哪側取得決定。`direct` 無此欄。
  - `indirect.hop`(int,必有)= 距最近直接節點 BFS 距離,`1..depth`;`indirect.from`(str,必有,r8-F6)= **BFS 路徑上前一跳(近一層)節點的 rel 路徑**——hop=1 時 `from` 是某直接節點,hop=k 時是 hop=k-1 的節點(非固定 direct)。`direct` 無 `hop`/`from`。
  - `cross_repo`/`no_expand`(bool,r7-F2)= **僅 `via:core_refs` 的跨 repo 葉有**(true);其餘筆**不含這兩 key**(下游用 `.get(k, false)`)。
  - `contract`(str|null,必有)∈ `INVARIANT`/`IRREVERSIBLE`/`null`(取最高:`IRREVERSIBLE`>`INVARIANT`)。
  - `combo`(bool,**必有、每筆都出、無 ★COMBO★ 則 `false`**,r3-F6/r4-F6/r7-F3):`★COMBO★` 是 `★INVARIANT★` 同行後綴修飾非獨立 marker(偵測邏輯 `scripts/lumos:725` 的 `"★COMBO★" not in inv`,r9-F6:`:718` 只是 Check K 段首註解非偵測碼),故不入 `contract` enum、走獨立布林。
  - **`★DEBT★` 映射**(r9-F7):`extract_contracts` 雖也掃 `★DEBT★`,但 DEBT 是「可改的偶然行為」非合約 → `contract` 一律映 `null`(不入 enum、不排前);純 DEBT 節點在影響清單無合約標記。

**方向語意**:改 X 時「誰受影響」= 主要是**反向依賴**(誰連到我 / backlinks),但 lumos 不替你判方向——**全撈、標方向**,交 Claude 判(符合「全撈、AI 消噪」原則)。

**方案取捨**(為何 A):落地索引(B)引入 staleness + 多一個會漂移的 artifact;只複用 guard trace(C)漏掉沒綁合約的節點,與「不能只看直接關聯」的把關意圖矛盾。lumos 本來就全圖載入記憶體(200 篇無感),A 永遠新鮮、零維護。

## §3 PreToolUse hook

- **matcher**:`Edit|Write|MultiEdit`(r1-F7:**不含 NotebookEdit**——lumos-toolchain 無 notebook;若未來要涵蓋須顯式加進 matcher,不靠括號註解)。
- **vault/repo 發現**(r3-F4):hook 以 subprocess 呼叫 `lumos impact`,cwd 未必是 repo root、`find_vault(cwd)` 可能失敗。故 hook **顯式傳 `--repo`**(取 `$CLAUDE_PROJECT_DIR`,fallback hook payload 的 `cwd`,同 `check-graph-sync.py:348-355`);`lumos impact` 支援 `--repo`(缺則退回 `find_vault`)。**「vault 找不到」與 fail-open 要能區分**(r6-F5,靠退出碼協定):`lumos impact` rc 定義——**0**=成功(有/無影響集都算,json 照出);**3**=vault 找不到(hook 收 rc=3 → 印一行 debug 提醒「非架構圖專案或 vault 未找到」,不注入不擋);**其他非 0**=lumos 內部出錯 → fail-open 純靜默放行。hook 據 rc 區分「該察覺的靜默失效(3)」vs「該吞的出錯(其他)」。(修正 DECISION 行舊述「非架構圖專案一律靜默」——非架構圖/vault 缺 = rc 3 印 debug,不是純靜默。)
- **過濾**:讀 hook payload 的 **`tool_input.file_path`**(r8-F9:`file_path` 在 `tool_input` 巢狀 dict 內、非頂層——`payload["tool_input"]["file_path"]`,MultiEdit 亦同;巢狀結構參 `verification-rot-check.py:270-273`,r9-F8 註:該處讀的是 `tool_input.command`,僅示範「tool_input 是巢狀 dict」、key 依工具而異)→ 只對 code 副檔名跑。**路徑排除比照 `check-graph-sync.py`**(r8-F8/r9-F5,**兩道都要**):`EXCLUDE_PATH_CONTAINS`(`:38-45`,排除整個 `/docs/`、`node_modules`、`bin`、`obj`、`__pycache__` 等)+ `EXCLUDE_FILENAMES`(`:46-48`,排 `package-lock.json`/`yarn.lock`/`pnpm-lock.yaml`);**單一源複用同兩份、勿自訂更窄的 `docs/*-knowledge/`**。非 code → 直接放行、零動作。
  - ⚠ **CODE_EXTS 來源**(r1-F6):現有三份 CODE_EXTS **不一致**——`scripts/hooks/claude/check-graph-sync.py:24`(Python set,20 副檔名)、`scripts/hooks/pre-commit:67`(bash 正則,同 20)、`scripts/lumos` `CODE_EXTS_T`(Python set,**僅 12**,缺 `.kts/.mjs/.cc/.cpp/.h/.swift/.c/.hpp`)。新 hook 是 Python,**單一源取 `check-graph-sync.py` 的 CODE_EXTS(20 版)**——但 `check-graph-sync.py` 是獨立腳本非 package(無 `__init__.py`)、**無法 import**(r2-F8),故**抄一份到 hook 並在兩處留「同源 check-graph-sync.py」註解**;順帶記一筆:`lumos CODE_EXTS_T` 的 12-vs-20 分歧屬既有債,本計劃不順手改(避免擴大範圍)、只在此標記。
- **per-file 節流(TTL 冷卻窗)**:標記檔 `<tmpdir>/lumos-impact-<session_id>/<filepath hash>` 記時間戳。`session_id` 取 hook payload 的 `session_id` 欄位(UUID,天然路徑安全,無需截斷/hash)(r2-F6)。
  - **跨平台 tmpdir**(r1-F10):用 `tempfile.gettempdir()`(Windows→`%TEMP%`、Linux→`/tmp`、macOS→`$TMPDIR`),**不寫死 `$TMPDIR`**(本專案有 Windows 支援)。
  - **hash 演算法**(r1-F11):`hashlib.sha1(filepath_abs.encode()).hexdigest()[:16]`(定死,不留實作者自選)。
  - **時間戳格式**(r3-F7):標記檔內容 = `time.time()` 的 Unix float(單一數字);讀取時 `now - float(content) < ttl_sec` 判冷卻窗。不用 mtime(避免 touch/rsync 誤動)。
  - **清理**(r4-F7):寫標記前**惰性清理**——刪掉本機 `lumos-impact-*` 下 mtime > 24h 的 session 目錄(一次掃、best-effort、失敗不影響主流程)。避免長期不重開機的機器累積(macOS /tmp 重開機自清,Linux/Win 不一定)。
  - 再次編輯同檔:距上次注入 **< TTL → 壓掉**(壓「連續 iterate 存檔洗版」);**≥ TTL → 重新注入**(長 session 隔久回頭、脈絡已遠,復活)。預設 TTL **20 分**(見下方 config)。
  - **為何 TTL 不是 per-session 一次**:使用者習慣單一長 session 保脈絡。「session 終身一次」會讓 turn 90 回頭改同檔時、turn 5 的注入早被捲走/壓縮 → 第二次修改變**啞巴**,偏偏那是提醒最該出現時。TTL 把「連續 iterate(靜音)」與「隔久回頭(復活)」乾淨分開。
  - (可選加碼:影響集內容變了無視 TTL 立即重注;主要修正仍是 TTL。)
- **注入方式**:`{"hookSpecificOutput": {"hookEventName": "PreToolUse", "additionalContext": "<清單+指令>"}}`,**永不 block、永不改 permissionDecision**。
  - r5-F2(辯方查證):此格式是 **Claude Code 官方有文件的能力**(官方 hooks docs 明列 PreToolUse 支援 `additionalContext`),本機 superpowers plugin 已生產使用(`.../superpowers/6.1.0/hooks/session-start:43`)+ 有測試。**但本 repo 現有 hooks 走 stderr、無此格式先例** → 實作時用 `claude --debug` **實測一次注入時機**(PreToolUse 的 additionalContext 注在 tool result 旁),並**保留 stderr 備援**(現有 hook 已證此路可行),萬一版本行為變動可退回。
- **空集合**:`lumos impact` 回空 → 不注入(不洗版)。
- **注入的指令文字**(附清單後):「動手前先判上列直接/間接節點會不會被你這次改動影響、需不需要同步更新架構圖。消掉不相關的,對真正受影響的:該同步就同步,不確定就記一句。」
- **fail-open**(依上方 rc 協定):`lumos` 不在 PATH / `lumos impact` 內部出錯(rc 非 0 且非 3)→ **純靜默放行**;非架構圖專案 / vault 找不到(rc 3)→ **印一行 debug 後放行**(可察覺,不混入純靜默)。任何情形**絕不擋 Claude 幹活**。
- **config**(r3-F2 翻案 r1-F8):用 **`.lumos/impact.json`**(`{"depth": 2, "ttl_min": 20}`)——對齊真實 per-feature 慣例:`lint`→`.lumos/lint.json`(`scripts/lumos:4079`)、`compose-metrics`→`.lumos/compose-metrics.json`(`:4529`)、`lint-watch`→`.lumos/lint-watch.json`,都是**獨立 feature 檔**;`.lumos/config.json`(`:1037`)只放 `test_profile`/`platforms` 核心測試設定。r1-F8 審計員誤認 config.json 是「統一入口」→ r2 據此改錯,此輪以代碼實證翻正。缺檔 → 用預設 depth=2/ttl=20min(fail-open)。

## §4 誠實天花板(收斂後向人講明)

1. **只抓「架構圖 body 寫下 code 路徑」的節點**(r8-F1 同步 r7-F2):某節點跟這支 code 有關但從沒在 **body inline-code** 寫下該路徑 → 反查看不到(GIGO,同 refcheck)。(`core_refs` 不算——它是跨 repo 節點指針非 code 路徑。)影響偵測完整度 = 架構圖 body code 引用的完整度。
2. **只在 Claude session 內生效**:人手動改 code 不觸發(PreToolUse 限定)。選 trigger 時明確接受的範圍。
3. **「Claude 分析過」≠「影響真的處理了」**:注入只保證 Claude *看到*、被要求判;判得對不對、有沒有真同步是沒閉合的迴歸(同 design-loop 整合性天花板)。它是**可觀測 + 摩擦 + push**,不是 oracle。
4. **新建檔(Write 新路徑)幾乎必回空集合**(r4-F9):架構圖還沒有節點引用尚不存在的檔 → 反查回空、不注入。這是**預期行為非缺陷**(新檔的影響要等它被寫進架構圖/被引用後才算得出)。
5. **body inline-code 的 `top_dirs` 靜默過濾**(r7-F6):`_refcheck_scan` 只認首段落在 repo 頂層目錄集內的路徑 token;body 若把 code 路徑寫成非頂層開頭(或相對怪路徑),不會被反查到、也不報錯。屬「只抓架構圖寫對路徑的」的細機械限制。

## §5 測試

- **原語單元**(`scripts/test_lumos.py`):反查索引從 fixture 正確建 code→node(只認 body inline-code、不認 core_refs);`--depth` 邊界(depth=1 只出 hop1、depth=2 出 hop1-2);`direct.hit`/`indirect.via`(5 種枚舉)/`direction`(backlink/outlink、雙向衝突取 backlink)/`hop`(距最近直接節點)/`from`(前一跳)/`contract`(IRREVERSIBLE>INVARIANT、走 IRREVERSIBLE_RE)/`combo`(每筆必有、無則 false)欄位正確;**cycle guard**(A↔B 環:直接節點不重入 indirect);**core_refs 跨 repo 葉**(cross_repo/no_expand、不 BFS 進);路徑規範化(絕對→rel);未知檔/新建檔回空;`--repo` rc 協定(rc0 成功/rc3 vault 缺/其他出錯);`--json` schema presence 契約。
- **hook**:code 副檔名才跑(EXCLUDE_PATH_CONTAINS 排除 /docs/ 等)、非 code 放行、讀 `tool_input.file_path`、TTL 冷卻窗(窗內壓/窗外復活/惰性清 >24h)、空集合不注入、rc3 印 debug、rc 其他 fail-open 不擋。
- **anchor**:⚠(r1-F9 + r4-F8)`ANCHOR_FILES`(`scripts/lumos:3818-3824`)是**硬碼 list**,目前**只含 5 項**(r6-F4:`pre-commit`/`pre-push`/`post-commit` 三個 git hooks + `test_lumos.py`/`test_autonomous_loop.py` 兩個 test runner);現有 `scripts/hooks/claude/*.py`(check-graph-sync 等)**都未 anchor**。→ 本 impact hook **比照現有 claude/ hooks 現況:預設不 anchor**(保持一致);若日後要把整批 claude/ hooks 納入 anchor 屬獨立強化議題,非本計劃範圍。(移除前輪「須手動加進 ANCHOR」的預設——那與現況不符。)

## 審計修正紀錄(lumos-design-loop)

> **F 編號跳號說明**(r5-F7):每輪審計員找到的 finding 中,有一條是 loop 刻意植入的校準用瑕疵(canary),**不折入真檔**——故各輪列出的 F 編號會缺該號(如 r2 缺 F2、r3/r4 缺 F1),屬正常、非遺漏。

**r1**(canary type=a caught;存活嚴重度 blocker;auditor sonnet;10 真 finding 折入):
- **F1 blocker**(opus 辯方存活):「複用 load_vault+_refcheck_scan」不成立——`Note.__slots__` 不存 body、`load_vault:184` 算 targets 前先刪 inline-code。→ §2.1 改為 impact 呼叫時重讀盤餵 `_refcheck_scan`(方案 A 仍成立)。
- **F2 blocker**(opus 辯方存活,探針實測):summary block 的 `DEP:[[..]]` 不進 `env.edges`(鐵則 2 排除)。→ §2.3 邊集限 frontmatter `related`/`verified_by`/`core_refs`;summary `DEP:`/`VERIFY:` 語意邊列後續工作項。
- **F4 major**:輸出範例「core_refs 直指」錯(`lumos-refcheck` 無 core_refs、實為 body 命中)。→ §2 範例更正 + 加註。
- **F5 major**:`env.edges` 無 edge type,「到達路徑」標記需新建 infra。→ §2.3 標明非複用。
- **F6 major**:CODE_EXTS 三版分歧(20 vs 12)。→ §3 單一源取 `check-graph-sync.py`(20 版),記 lumos CODE_EXTS_T 既有債。
- **F7 major**:matcher 與「NotebookEdit 一併」自相矛盾。→ §3 移除 NotebookEdit。
- **F8 major**:`.lumos/impact.json` 與現有 `.lumos/config.json` 分裂。→ §3 config 合入 `.lumos/config.json` 的 `impact` key。**⚠ 此修正已被 r3-F2 翻案(代碼實證 config.json 非統一入口)——現行以 `.lumos/impact.json` 為準。**
- **F9 minor**:`ANCHOR_FILES` 硬碼,「自動進機制」誤導。→ §5 標明手動加 + `anchor approve`。
- **F10 minor**:`$TMPDIR` 跨平台。→ §3 用 `tempfile.gettempdir()`。
- **F11 minor**:hash 未指定。→ §3 定 `sha1[:16]`。

**r2**(canary type=b caught;存活嚴重度 blocker;auditor sonnet;7 真 finding 折入):
- **F1 blocker**(opus 辯方存活):r1 修引入的新錯——`core_refs` 是純文字路徑非 wikilink(doctor Check C `scripts/lumos:526-551` 當檔案路徑讀),`load_vault:193` 的 `fullmatch [[..]]` 濾掉它 → **不進 `env.edges`**。→ §2.3 改為邊集分兩路:`related`/`verified_by` 走 env.edges 雙向、`core_refs` 另讀 `n.fields` 手動補邊。**⚠ 此「手動補邊進 BFS」已被 r7-F2 覆寫**——core_refs 目標在 core-knowledge repo、不在 `env.notes`,現行改為「當不可展開跨 repo 葉輸出、不 BFS 進」,以 §2.3 現文為準。
- **F3 major**:summary KEY 行(舊)含 DEP 與 §2.3 矛盾。→ 更新 KEY 行。
- **F4 major**:summary FLOW 漏 `<path>`。→ 補。
- **F5 major**:summary FLOW 漏 `MultiEdit`。→ 補。
- **F6 minor**:session_id 路徑安全性。→ §3 標明取 payload session_id(UUID 天然安全)。
- **F7 minor**:`--json` schema 未定。→ §2 補 schema block。
- **F8 minor**:`check-graph-sync.py` 非 module 無法 import。→ §3 改「抄一份並註同源」。

**r3**(canary type=c caught;存活嚴重度 blocker;auditor sonnet;7 真 finding 折入):
- **F2 blocker**(代碼實證翻案):`config.json` 只放 `test_profile`/`platforms`;`lint`/`compose-metrics`/`lint-watch` 各用獨立 `.lumos/*.json`(`scripts/lumos:4079`/`:4529`)。r1-F8 誤認 config.json 為統一入口 → r2 改錯。→ config 翻回 `.lumos/impact.json`(對齊 per-feature 慣例)。
- **F3 major**:`--json` `via` enum 漏 `plan_refs`/body-wikilink,且直接命中特徵與 BFS 邊型混層。→ 拆 `direct.hit` vs `indirect.via`,補漏。
- **F4 major**:hook subprocess cwd 未必 repo root、`find_vault` 可能失敗且被 fail-open 吸收。→ hook 顯式傳 `--repo`(CLAUDE_PROJECT_DIR/payload cwd),vault 找不到印 debug 與 fail-open 區分。
- **F5 major**:`_refcheck_scan` 複用缺路徑規範化(絕對→repo-relative)。→ §2.1 補 `os.path.relpath`。
- **F6 minor**:`contract` enum 漏 `★COMBO★`(Check K)。→ 補。
- **F7 minor**:TTL 時戳格式未定。→ 定 `time.time()` Unix float。
- **F8 minor**:`n.fields["core_refs"]` 可能 str/list。→ 標明走 `as_list()`。

**r4**(canary type=d caught;存活嚴重度 major=文件衛生;auditor sonnet;8 真 finding 折入,**無 blocker**):
- **F2 major**:`decisions[]` 漏 config 翻案 ADR。→ 補 ADR 條目。
- **F3 major**:r1-F8 審計紀錄未標「已被 r3 翻案」。→ 標註以 r3 為準。
- **F4 major**:via-label build-time vs BFS-time 二選一未選。→ 選 BFS-time 二次反查(隔離、不動 env.edges)。
- **F5 minor**:`--repo` 未入 §2 輸入。→ 補。
- **F6 minor**:`★COMBO★` 是 INVARIANT 同行後綴非獨立 marker。→ schema 改 `combo:true` 布林,不入 contract enum。
- **F7 minor**:TTL tmpdir 無清理。→ 惰性清 >24h session 目錄。
- **F8 minor**:現有 claude/ hooks 均未 anchor,「須 anchor」前提不符現況。→ 比照現況預設不 anchor。
- **F9 minor**:Write 新檔邊界未列 §4。→ 補天花板 4。

**r5**(canary type=a caught;辯方後存活嚴重度 major;auditor sonnet;6 真 finding 折入):
- **F2 blocker→minor**(opus 辯方駁倒):`additionalContext` 注入是 Claude Code 官方有文件能力(superpowers `session-start:43` 生產先例+測試),「本 repo 零先例=不可靠」以偏概全。→ §3 加「`--debug` 實測注入時機 + stderr 備援」note,非 blocker。
- **F3 major**:BFS-time 二次反查對 body-wikilink 邊無 fallback(`n.fields` 無 body)。→ §2.3 補:邊不在任何 frontmatter 欄位 → `via:body-wikilink`。
- **F4 major**:§2 排序規則誤標 `(r1-F3)`,但 r1-F3 是 canary 號。→ 移除誤引。
- **F5 major**:§2.3 邊集散文漏 `plan_refs` 與 body wikilink(兩者也在 env.edges)。→ 補進「走 BFS 的邊」列。
- **F6 minor**:core_refs 註「見 §1」但 §1 未提 core_refs。→ 移除壞引。
- **F7 minor**:審計紀錄 F 編號跳號無說明(跳號=canary 佔號)。→ 紀錄開頭加通則說明。

**r6**(canary type=b caught;存活嚴重度 major;auditor sonnet;5 真 finding 折入;**cap=6 達頂**):
- **F1 major**:summary KEY 邊集行未同步 r5-F5(漏 plan_refs/body-wikilink)。→ 同步。
- **F3 major**:`contract:IRREVERSIBLE` 偵測未指定——`extract_contracts`(`:934`)只掃 INVARIANT/DEBT。→ §2.4 補「另用 `IRREVERSIBLE_RE`(`:1184`)掃」。
- **F4 minor**:§5「5 個核心 git hooks」誤述(實 3 hook + 2 test runner)。→ 更正。
- **F5 minor**:vault-not-found vs fail-open 退出碼協定未定。→ 定 rc 0/3/其他。
- **F6 minor**:二次反查 wikilink↔rel 比對函式未指定。→ 標明 `link_target()`+`env.resolve()`。

**r7**(續磨,覆寫 cap;canary type=c caught;存活嚴重度 blocker;auditor sonnet;5 真 finding 折入):
- **F1 blocker**:schema 無方向欄位,`via` 分不出 out/in-edge → 破壞「機器可讀供自動修復」。→ schema 加 `direction`(backlink/outlink),§5 方向測試變可驗。
- **F2 major(深,前 6 輪漏)**:`core_refs` 是**跨 repo 節點指針非 code 路徑**(grep 實證 `:526-551`)。→ §2.1 反查**不掃 core_refs**(只 body inline-code);§2.3 core_refs 當**跨 repo 不可展開葉**(不 BFS 進 env.notes 外節點)。
- **F3 major**:`combo` 位置/預設未定。→ schema 定「每筆必有、無則 false」。
- **F5 minor**:`via` 五種枚舉散兩處。→ schema 一處列全。
- **F6 minor**:`_refcheck_scan` `top_dirs` 靜默過濾未說明。→ §2.1 標明 + §4 天花板 5。

**r8**(續磨;canary type=d caught;存活嚴重度 major;auditor sonnet;8 真 finding 折入):
- **F1 major**:§4.1 天花板未同步 r7-F2(仍寫「core_refs/body 反查」)。→ 改「只 body inline-code」。
- **F3 major**:`lumos impact <節點>` 模式算法全空白。→ **CUT**(YAGNI,v1 只 `--file`;節點鄰域已有 context/map/backlinks)。
- **F4 major**:BFS 無 visited set,直接節點沿環重入 indirect。→ §2.3 加 `seen`(種入全直接節點,複用 map:908)。
- **F5 major**:「2..depth」記號 vs schema hop=1 矛盾。→ 定 hop=距最近直接節點 1..depth、廢舊記號、schema 範例改一致(兩筆 hop=1)。
- **F6 minor**:`from` 語意(前一跳 vs direct)未定。→ 定為 BFS 前一跳節點。
- **F7 minor**:雙向 direction 衝突未解。→ backlink 優先。
- **F8 minor**:路徑排除源(spec 比 check-graph 窄)。→ 比照 `EXCLUDE_PATH_CONTAINS`。
- **F9 minor**:`file_path` 在 `tool_input` 巢狀。→ 標明 `payload["tool_input"]["file_path"]`。

**r9**(續磨;canary type=a caught;存活嚴重度 blocker;auditor sonnet;7 真 finding 折入):
- **F1 blocker(真算法 bug)**:BFS 二次反查對 backlink 邊 source/dest 反轉——wikilink 永遠在「連出端」,backlink 須讀 `dest.fields`(非 frontier.fields)。→ §2.3 拆 outlink/backlink 兩向讀對端。
- **F3 major**:summary KEY 殘留「2..depth」(r8-F5 廢除、固化漏)。→ 改 hop 1..depth。
- **F4 major**:r2-F1 紀錄「core_refs 手動補邊」未標被 r7-F2 覆寫。→ 加覆寫標注。
- **F5 minor**:漏 `EXCLUDE_FILENAMES`。→ 補第二道排除。
- **F6 minor**:`:718` 是 Check K 段註非偵測碼。→ 改引 `:725`/`:930`。
- **F7 minor**:`★DEBT★`→`contract` 映射未明。→ 明訂映 `null`。
- **F8 minor**:`rot-check:273` 引的是 `command` 非 `file_path`。→ 註明僅示範巢狀結構。

## 收斂狀態(cap 達頂 → 使用者要求續磨)

前 6 輪(cap=6)。canary **6/6 全抓到**(type a/b/c/d 輪替)。findings 軌跡 **10→7→7→8→6→5**、嚴重度 **blocker×3 → doc-hygiene → major → major(0 blocker)**:架構層已收斂(r1-r3 真 blocker 全抓全修)、r4 起只剩清晰度/實作細節。

**續磨決定(使用者)**:此原語是自動修復地基(見〈定位〉),使用者要求**覆寫 cap、續收斂到完美**。觀察到 r4-r6 不少 finding 是「逐輪增量折入」造成的**內部漂移**(summary↔body↔diagram↔fail-open 不同步、citation 對到 canary 號)——故先做一次**整體固化 pass**(非 canary 輪):同步 §1 流程圖 MultiEdit、修 §3 fail-open↔rc 協定矛盾、排序規則措辭對齊 hit/via、補「自動修復地基」定位。目標:殺掉「內部漂移」finding class 後,續跑 canary 輪直到**真的連 2 輪只抓到 canary(乾淨)**。

**續磨結果(r7-r9)**:canary 9/9 全抓。續磨挖出 2 個前 6 輪漏的**真算法 bug**(r7 core_refs 語意誤用、r9 backlink 二次反查 source/dest 反轉)——證明對「自動修復地基」續磨有實質正確性回報,非空轉。

**收尾決定(使用者選 TDD 路,2026-07-05)**:9 輪後判斷——設計層高價值 finding 已榨出(2 真 bug 修掉),findings 不枯竭主因是 (i) 折入漂移自傷 ~2/輪(見 [[design-loop折入漂移_機械守衛]])(ii) 剩餘精度/正確性缺口**由實作階段真單元測試釘更狠**(那 2 個 bug 測試也會抓)。故**不硬追機械 gate PASS**(K-streak 對此密集 spec 近乎不可達),改**轉 writing-plans + TDD**:§5 測試清單逐條落地即是收斂的可執行證據。此為使用者對 gate 的顯式知情覆寫(非靜默跳過)。

**誠實天花板**:收斂只證「醒著的審計員逐輪找不到更嚴重的」,不證無更深問題;canary-caught/severity/誤判三判均植入者自判、無外部閉合(見 lumos-design-loop skill〈誠實天花板〉)。**真正的正確性驗證移交 TDD**。

## PRIOR-ART 複查(2026-07-20):code-review-graph 裁定不借

`PRIOR-ART:` ① 最小解=既有 impact 反查已足(供給不缺,見下量測)② 世界解過沒=有,`github.com/tirth8205/code-review-graph`(tree-sitter AST 呼叫圖 + SQLite + 30 MCP tools,算 blast radius 給 AI 省 token)③ 裁定=**皆不取**(非 borrow-design——它解的問題本專案不存在)。

**它做什麼**:機器解析 AST 建**結構圖**(函式/類別/呼叫/測試覆蓋邊),回答「改 X 會波及哪些檔」。與本架構圖是**語意圖**(為什麼/邊界/★合約★)正交,不存在取代關係。核心賣點=**擴大候選集**。

**為什麼不需要**(釘定快照 285d429 實測,harness 與 `governance/eval/retrieval_eval.py` 同數:P@8=0.7298、必看 19/30):

| 量 | 數字 |
|---|---|
| 候選池(解除閾值/名額) | 中位 **32**、最大 **102** |
| 實際吐出的自由席 | **1–10** |
| 完美排序能多賺 | **+0.0 個百分點**(自由席已是最佳序) |
| 必看(標2)漏掉的 11 條,有幾條是「撈不到」 | **0 條**——11/11 全在候選池,是被閾值/名額砍掉 |

**結論**:召回從未失敗,每一次遺漏都是**篩選決策**。候選已超供 10 倍,再灌供給只會讓閾值砍更多。它自報 precision 0.58 亦為同一病症。

⚠ **裁定的性質(勿誤讀)**:上述數字**全部來自本工具鏈自己的 goldset 快照**,未對該工具做任何實測或對照。裁定邏輯=「我的瓶頸不在供給,故擴供給的工具對我無用」——是**基於自身量測的推論**,不是「評估過 code-review-graph 的效果」。若日後瓶頸性質改變(例如架構圖覆蓋補起來、候選變稀),此裁定需重估。

### 承上:那 11 條必看被砍掉,出路是什麼?(2026-07-20 自足性審計補)

初版寫本節時把兩條路都封死了——排序(+0.0pp)、且指向補覆蓋——但**補覆蓋救不了這 11 條**:它們的節點**早就在架構圖裡、也早就進了候選池**,是被閾值/名額砍掉的。漏列了真正對症的第三條:

**候選路徑三選一**(尚未裁定,列此避免證據鏈懸空):
1. **重排自由席** — 實測 **+0.0pp**,✗ 排除。
2. **補架構圖覆蓋** — 對「候選池是空的」有效(KDS/Frasers 型),但**對這 11 條無效**(它們已在池內)。
3. **重新權衡閾值/名額本身**(`dyn_coef=0.65`/`free_quota=10`)— **唯一對症**,但直接與精度反向:v1.2 正是靠調緊 0.55→0.65 換到 P@8 0.7298。真實取捨=**+3pp 精度 ↔ 5 條必看視野**(見 [[Verification/2026-07-11_hook面v1.1轉正]] 更正註)。

**現狀=已知有 11 條必看被砍、trade-off 已攤開、但無人裁定**。這是 v1.3 的題目,目前**沒有掛勾的待辦**——明記於此,免得下次又繞回「調排序」這條已證偽的路。

## 跨專案覆蓋率實測(2026-07-20)

量「動手改一支 code → impact 跳不跳得出東西」。方法:復用本尊 `_refcheck_scan` 建 code→node 索引(indirect 由 direct 展開,direct 空則整組空,故等價);抽查 24 支與真 CLI **24/24 一致**。

| 專案 | 架構圖節點 | 按檔案數覆蓋 | **按動手次數覆蓋**(近300 commits) | 最常改前10支 |
|---|---:|---:|---:|---:|
| lumos-toolchain | 142 | 53.6% | **97.7%** | 7/9 |
| LandmarkMember | 268 | 20.6% | **55.2%** | **10/10** |
| mOrangePos | 109 | 17.9% | **48.4%** | 9/10 |
| Citrus_KDS | 58 | 3.8% | 5.8% | 1/10 |
| FrasersKiosk | 10 | 5.1% | 8.8% | 1/10 |
| Compass_Kiosk | 3 | 0% | 0% | — |

**KEY:按檔案數算會嚴重低估**——大量 code 檔一輩子改一次。Landmark 檔案數 20.6% 但動手命中 55.2%、最常改的前 10 支 10/10 有覆蓋:架構圖自然長在常動的地方,這是它該有的樣子。**回報覆蓋率一律用動手加權,別用檔案數。**

**KEY:低覆蓋專案是架構圖沒建,不是機制失效**——KDS 整個架構圖只有 **7 條**有效 code 路徑引用(且 7 條全對得上,非寫錯);Frasers 僅 10 節點。門檻現象:節點過百且有寫 code 路徑習慣 → 動手覆蓋上 50%;未過門檻趴在個位數。**樣本僅 6 專案、2 個等同未導入,不可當因果**。

此為 [[檢索優化_計劃]] §6 v1.2 既有判斷(「LM 絕對值低於 0.70 主因前端檔無架構圖覆蓋,排序不可修」)的**量化補證**,非新發現。

⚠ **這個數字量的是「跳不跳得出東西」,不是品質**:方法上覆蓋率完全等同於「有沒有 code 路徑反查命中」(indirect 由 direct 展開,direct 空則整組空);24/24 抽查驗證的也是這個等價。**97.7% 不代表推播有用**——推播品質是 P@8/必看視野那組指標,兩者別混。

## 落地後回指

實作完成的 Verification 用 `plan_refs: "[[主動影響幅度偵測_計劃]]"` 回指本節點(意圖鏈,doctor Check 4 把關)。
