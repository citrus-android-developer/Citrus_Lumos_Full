---
type: system
status: done
created: 2026-07-31
updated: 2026-08-03
tags:
  - type/system
  - status/done
  - risk/不可逆
plan_refs:
  - "[[Projects/公開精簡版_計劃]]"
summary: |-
  FLOW:`slim/install.sh` 用 `$(cd "$(dirname "$0")" && pwd)`(先解 symlink 鏈)定位交付包 → 檢查 `scripts/lumos`/`skills/lumos-project-notes` 存在(找不到即 rc2)→ 解析 `--force`/`--here` 旗標 → **★2026-08-01 Task 11 新增★ ⓪ 注入目標守衛**(印目標絕對路徑 → 未帶 `--here` 則過第一層不像專案根檢查、第二層來源 repo 三件套檢查,任一擋下即 rc2、不建目錄不動任何檔案)→ 做三件事:①複製 `scripts/lumos` 到 `~/.local/bin/lumos`(chmod +x),★2026-07-31 Task 10 新增★同時把安裝當下的 bin sha256 寫進身分證 manifest(`~/.local/share/lumos-slim/manifest.json`,不放使用者專案、不依賴 `~/.lumos-slim` 存在)②實體複製(`cp -R`,非 symlink)`skills/lumos-project-notes` 到 `~/.claude/skills/`③(Task 9 裁定第三次變更)讀取隨包帶的 `claude-block.md` 範本,在執行目錄(專案根)的 `CLAUDE.md` 裡放策展過的精簡版紀律區塊(`<!-- LUMOS-SLIM:START/END -->`)——專案若已有完整版 `LUMOS:GRAPH-DISCIPLINE` 區塊,先把原文 base64 編碼進備份標記再**原地整段取代**;沒有就插在檔首「# 標題」之後(沒標題插最前面);CLAUDE.md 不存在就直接建立 → ①②碰撞語意:目標已存在且無 `--force` → rc2 拒絕;帶 `--force` 時 bin 直接覆寫(manifest 仍照寫,反映最新那次安裝)、skill 目錄先備份成 `.bak.<timestamp>` 才覆寫;③不設碰撞閘,靠比對「有無完整版區塊/精簡版區塊已存在」三態決定行為 → 結尾檢查 PATH 是否含 `~/.local/bin`,非阻斷提示
  KEY:★只動 $HOME 與專案 CLAUDE.md 這一份檔案,不碰專案其餘任何東西★(★2026-07-31 使用者裁定推翻原「不注入/更新任何 CLAUDE.md」的裁定,範圍刀見下條★)——仍不 scaffold 圖譜、不 vendor 工具進專案、不設 core.hooksPath、不裝任何 Claude hook(對比完整版 install.sh 尾端 `_sync_global_claude` 會把 4 支被禁 hook 裝回去,精簡版安裝器刻意不繼承這段)。★2026-07-31 Task 10★:寫入 `~/.local/share/lumos-slim/manifest.json` 不算破例——那是 `$HOME` 下的工具自身狀態,不是使用者專案檔案
  KEY:★裁定演進三階(spec [S3],別誤讀成一次到位)★——①原裁定:絕不碰 CLAUDE.md ②Task 8:只准附加、檔尾、絕不覆蓋完整版區塊、兩套規則並存 ③**Task 9(本次)**:兩套規則並存本身是問題——完整版那段自稱「優先級最高/第一個工具呼叫必須是 lumos」,接手者的 Claude 會先讀到它、照著它引用的 design-loop/code-loop/pitfalls 等 13 處本包沒交付的指令去撲空。裁定改成:有完整版區塊就**整段移除、原地換成精簡版區塊**(不是繼續並存);移除前先策展吸收完整版裡仍然有效的內容(合約鏈/可逆性標記/regen 重生標記/frontmatter 欄位,見 `slim/claude-block.md`),只拿掉依賴已移除指令才有意義的段落。詳見 [[Projects/公開精簡版_計劃]] [S3]〈裁定第三次變更〉
  KEY:★INVARIANT★ CLAUDE.md 注入必須原地取代/檔首插入,sentinel(`<!-- LUMOS-SLIM:START/END -->`)以外的既有內容 byte-equal 保留(改=毀使用者/其他工具寫進 CLAUDE.md 的內容=breaking);有完整版區塊就在它原本的位置換掉,沒有就插在檔首標題之後,都不是搬到檔尾 [test:t_slim_install_no_project_touch,t_slim_install_replaces_full_discipline_block_in_place] [audit:sonnet/2026-07-31](獨立審計實測:在暫存副本把寫入改成整檔覆寫,綁定測試確實翻紅,非稻草人)
  KEY:★INVARIANT★ CLAUDE.md 注入必須冪等——重跑安裝器只更新自己 sentinel 之間那塊內容,不疊出第二塊、不因重跑而漂移,備份標記(FULL-BACKUP)也不會被重新編碼或洗成 NONE [test:t_slim_install_claude_md_idempotent,t_slim_install_backup_survives_idempotent_reinstall] [audit:sonnet/2026-07-31](獨立審計實測:在暫存副本把「已存在 sentinel 則替換」的分支關掉,強迫每次都走插入路徑,綁定測試確實翻紅——第二次跑完出現兩塊 sentinel)
  KEY:★INVARIANT★ 完整版區塊被取代前必須先把原文(含它自己的 sentinel)位元組級備份——base64 編碼藏進精簡版區塊自己的 HTML 註解裡(`<!-- LUMOS-SLIM:FULL-BACKUP:BASE64:... -->`),解碼後須與原文逐位元組相同,這是 [[Systems/slim-uninstall-一行卸載]] 能精確還原的前提 [test:t_slim_install_replaces_full_discipline_block_in_place] [audit:sonnet/2026-07-31](備份設計理由:自足、隨 CLAUDE.md 本身走,不新增檔案到使用者專案、`~/.lumos-slim` 被刪掉也還原得了)
  KEY:★INVARIANT★(★2026-07-31 Task 10 新增★)裝 bin 時必須同步寫身分證 manifest(`~/.local/share/lumos-slim/manifest.json`,含安裝當下對 `~/.local/bin/lumos` 算出的 `bin_sha256`),讓 [[Systems/slim-uninstall-一行卸載]] 有★不依賴 `~/.lumos-slim` 存不存在★的比對基準——這是本次修的 bug 的正面解法:接手者若走 README 也在教的「直接 clone 交付包到任意路徑跑 install.sh」而非 `get.sh`,`~/.lumos-slim` 從頭到尾不會存在,舊版卸載腳本的唯一比對基準因此永遠缺失 [test:t_slim_uninstall_direct_install_restores_claude_md] [audit:sonnet/2026-07-31](因果驗證:`git stash` 還原成 Task 10 前的 install.sh/uninstall.sh 重跑此測試確實翻紅——bin 比對基準缺失→exit 2→CLAUDE.md 沒被還原,回補修復後轉綠)
  KEY:★INVARIANT★(2026-08-01 Task 11 新增)CLAUDE.md 注入動手前必須先過三層目標守衛,拒絕時 rc=2 且 CLAUDE.md 不得被動——①不像專案根(無 `.git`、無 `docs/*-knowledge/`、無既有 `CLAUDE.md`,或 `$(pwd)`==`$HOME`,用 `pwd -P`/`cd -P` 解 symlink 後比對)→拒絕②目標同時具備 `skills/lumos-project-notes/`+`scripts/lumos`+`scripts/templates/graph-discipline.md` 三件套(lumos 工具鏈來源 repo 才有的組合)→拒絕③不論①②過不過,動手前一律先印出「目標專案」與「將修改」的絕對路徑。★真實事故驅動,別誤讀成第一層擋得住那兩次★:兩位不同的子代理驗證時忘記先 `cd` 進交付包 clone,直接在本 repo(lumos 工具鏈來源 repo)根目錄跑 `dist/install.sh`,當場改掉了來源 repo 自己的 `CLAUDE.md`(兩次都當場發現、`git checkout` 還原)——事故現場本身就有 `.git`/`CLAUDE.md`/`docs/*-knowledge`,「像不像專案根」的第一層完全擋不住,真正擋住的是第二層(來源 repo 三件套判定,沿用 `scripts/lumos` 的 `cmd_update`/`cmd_deinit`「`root == _lumos_src()` → 拒絕」同款自我保護精神)。逃生閥 `--here` 繞過①②,③一律照印、不受影響 [test:t_slim_install_guard_rejects_empty_dir,t_slim_install_guard_rejects_home_dir,t_slim_install_guard_rejects_source_repo,t_slim_install_guard_repro_real_incident,t_slim_install_guard_here_bypasses,t_slim_install_guard_normal_project_still_works] [audit:sonnet/2026-08-01](獨立審計實測抓到一個真的稻草人,非空手放行:`t_slim_install_guard_repro_real_incident` 原版直接對 `slim/install.sh` 原始檔以 repo 根為 cwd 執行——但 `slim/` 底下沒有 `scripts/lumos`,會先撞上與本守衛無關的套件完整性檢查提前以 rc=2 退出,**把第二層判斷式整段刪掉這個舊版測試依然通過**;改用 `scripts/slim-gen.py` 真的生成一份 `dist/install.sh` 再以 repo 根為 cwd 執行後修正。修正後複測:把第二層 `if` 換成 `if false; then` 造假陽性,綁定測試確實翻紅(rc 從 2 變 0、`CLAUDE.md` 真的被覆寫)——此步驟意外把本 repo 自己的 `CLAUDE.md` 寫髒,已 `git checkout` 立即復原並確認 `git status --porcelain CLAUDE.md` 清空。其餘五支測試審計時逐一確認皆非稻草人)
  KEY:★2026-08-01 分支終審第四輪代碼審,兩條殘留 finding★——①MAJOR:`t_slim_install_guard_here_bypasses` 舊 fixture 是空目錄,永遠不會觸發第二層(三件套判定),第二層被整段刪掉舊測試照樣綠,只驗到「`--here` 繞過第一層」(★這是這個專案第八次撞到「測試存在但沒在驗它宣稱要驗的」★)。修法:fixture 換成與 `t_slim_install_guard_rejects_source_repo` 同款的假來源 repo(三件套齊備+`.git`/CLAUDE.md),分兩段跑(不帶 `--here` 先證明確實被第二層擋下,帶 `--here` 再證明放行)[test:t_slim_install_guard_here_bypasses] [audit:sonnet/2026-08-01](竄改驗證:把第二層 `if` 換成 `if false; then`,3/5 checks 翻紅,含核心的「不帶 --here 應被擋下」那條;竄改後立即還原)。②MINOR:`slim/install.sh:207`(改前)算 CLAUDE.md 寫入路徑用素樸 `$(pwd)`,與第一層/第三層用的 `$(pwd -P)` 不一致,cwd 經 symlink 進來時稽核印出路徑與實際寫入路徑字串可能對不上,弱化第三層價值。修法:改用已解過 symlink 的 `$TARGET_CLAUDE_MD`(重用第一層算好的值,不再重叫 `pwd`)[test:t_slim_install_symlink_cwd_audit_path_matches_write_path] [audit:sonnet/2026-08-01](竄改驗證:改回素樸 `$(pwd)`,2/6 checks 翻紅——稽核路徑=實體路徑、實際寫入路徑=symlink 邏輯路徑,確實分岔;竄改後立即還原)。額外檢查其他守衛測試(六支 `t_slim_install_guard_*` + `t_slim_uninstall_refuses_foreign_bin`):只有 `here_bypasses` 有這個毛病,其餘皆確認 fixture 能真的觸發宣稱要測的分支。詳見 [[Verification/2026-08-01_公開精簡版代碼審第四輪修復]]、報告見 `.superpowers/sdd/公開精簡版_實作計畫/task-12-report.md`
  KEY:★install 的例外語意與 [[Systems/slim-uninstall-一行卸載]] 刻意相反,別「好心統一」(2026-08-02 補記)★——uninstall 宣告「各步互不阻擋」(一步失敗其餘照跑,逐步吞 `OSError` 再彙總);install 的 `main()` 是★遇錯早退★(`rc != 0` 就 return),因為每一步依賴前一步,裝到一半就該停,而不是把 skill 裝進一個沒有 CLI 的環境。★把 uninstall 那套 try/except-繼續跑搬過來會改掉 install 的順序語意,那不是修 bug 是製造 bug★。install 這邊唯一該修的是**例外別以 traceback 形式丟給使用者**:`_main_guarded()` 只在最外層接 `OSError` → 印一句可行動訊息(常見原因 + 排除後重跑 `--force`)+ rc2,★控制流一行都沒動★。★2026-08-02 同一次 review 抓到姊妹漏洞★:`_main_guarded()` 第一版只接 `OSError`,但 `_merge_claude_md_text()` 讀的是★使用者既有的★ CLAUDE.md——那份檔案不是我們寫的、編碼不歸我們管,不是合法 utf-8 時拋 `UnicodeDecodeError`(繼承 `ValueError`,★不是 OSError★)攔不到,照樣 traceback + rc1。已補一條獨立 except 分支,訊息明講「本安裝器只處理 utf-8;不會在看不懂內容的情況下改寫你的 CLAUDE.md」,並由 `t_slim_install_non_utf8_claude_md_reports_cleanly` 斷言該檔位元組原封不動。★同日對交付包端到端真跑又抓到一條:兩個 except 分支的訊息不對稱★——`OSError` 那條有講「已完成的步驟不會被回滾」,`UnicodeDecodeError` 那條沒講,而那時 ①CLI 與 ②skill 其實已經裝好了;不講使用者會以為整件事失敗、跑去手動清理。★錯誤訊息之間的不對稱本身就是缺陷★,已補齊並綁斷言(含一條「前置:①②這時真的已裝好」——否則主斷言是在講一件沒發生的事)。★通則:同一個失敗面的多條訊息必須講一樣多★。半成品狀態(例如 CLI 裝好了、skill 沒裝成)是已知且刻意保留的——精簡版不做交易式回滾(回滾自己也會失敗,而且刪使用者的東西風險比留著高),重跑 `--force` 收斂;測試因此刻意不斷言「已完成步驟被回滾」(斷言不存在的行為=假需求)。★合約候選,本次刻意不標成硬合約★:「install 必須遇錯早退」看起來像合約,但目前只有 code 現況與這段推理支撐,沒有 decisions[] 載明當初意圖——依「不確定是不是合約就不標/嚴禁從現況 code 反推」鐵則留在 KEY,待人裁定再升格 [test:t_slim_install_filesystem_error_reports_cleanly_without_traceback 綁的是 traceback 那半,不是早退那半]
  KEY:skill 碰撞語意是新寫,不是沿用完整版 `_link_or_copy`(對既有實體目錄直接 rmtree、無備份無拒絕,牴觸本安裝器自己的反誤傷測試)
  KEY:★symlink 呼叫邊界(2026-07-31 實測,spec [S4-c] 明列未經審計必須實測)★——`$(dirname "$0")` 素樸寫法解析到 symlink 所在目錄而非真實包目錄,實測必壞(rc2 報「找不到 scripts/lumos」);裁定採(a)修正寫法而非(b)只寫錯誤訊息:開頭加手捲 symlink 解析迴圈(macOS 無 `readlink -f`,逐層 readlink 接回所在目錄再判斷是否還是 symlink)。已實測 3 種邊界全過:絕對路徑 symlink/相對路徑 2 層 symlink 鏈/路徑含空白
  KEY:2026-07-31 Task 6 補上一行安裝／卸載入口——本腳本本身(除 Task 10 新增的 manifest 寫入外)未改動(`$(dirname "$0")` 定位、碰撞語意皆維持原樣),但新增的 [[Systems/slim-get-一行安裝]](`slim/get.sh`)把交付包 clone 到固定落點 `~/.lumos-slim` 再呼叫本腳本,解掉「使用者把交付包搬走/刪掉,全域指令就斷了」的既有問題;[[Systems/slim-uninstall-一行卸載]](★2026-07-31 Task 10 改版★)比對基準優先讀本腳本寫的 manifest,`~/.lumos-slim/scripts/lumos` 降為 manifest 缺失時的備援
  DEP:scripts/test_lumos.py t_slim_install_no_project_touch｜t_slim_install_claude_md_idempotent｜t_slim_install_replaces_full_discipline_block_in_place｜t_slim_install_backup_survives_idempotent_reinstall｜t_slim_claude_block_curation｜t_slim_uninstall_direct_install_restores_claude_md｜t_slim_install_guard_rejects_empty_dir｜t_slim_install_guard_rejects_home_dir｜t_slim_install_guard_rejects_source_repo｜t_slim_install_guard_repro_real_incident｜t_slim_install_guard_here_bypasses｜t_slim_install_guard_normal_project_still_works｜t_slim_install_symlink_cwd_audit_path_matches_write_path｜slim/claude-block.md(策展範本,slim-gen.py 組包清單已補)
  TEST:225 checks 全綠(`python3 scripts/test_lumos.py -k slim`)。t_slim_install_no_project_touch 覆蓋沒有完整版區塊情境(改斷言為插檔首標題後、既有內容 byte-equal、附加而非覆蓋);t_slim_install_claude_md_idempotent 覆蓋冪等;t_slim_install_replaces_full_discipline_block_in_place(新)覆蓋有完整版區塊情境——原地取代、備份 base64 解碼後與原文逐位元組相同;t_slim_install_backup_survives_idempotent_reinstall(新)覆蓋「重跑安裝器不把備份洗掉/不二次包裹」;t_slim_claude_block_curation(新)斷言策展範本不含 design-loop/code-loop/core-knowledge/pitfalls/spec-trace/signoff/lumos init/lumos update 字串、且含合約鏈標記/[test:/FLOW:/KEY:/valid_under/佚失: 等仍有效內容;★Task 10 新增★ manifest 寫入的正面驗證在 [[Systems/slim-uninstall-一行卸載]] 的 `t_slim_uninstall_direct_install_restores_claude_md`(不經 get.sh 情境下驗 manifest 確實提供了可用的比對基準);★Task 11 新增★ 六支 `t_slim_install_guard_*` 覆蓋三層守衛(空目錄拒絕/`$HOME` 特例硬擋/來源 repo 三件套拒絕/對本 repo 根目錄實跑 `dist/install.sh` 重現真實事故/`--here` 逃生閥/正常專案不退化),既有 `t_slim_uninstall_*`/`t_slim_get_idempotent` 系列補 `.git` 標記讓 fixture 通過新守衛(見對應測試內註解);★2026-08-01 代碼審第四輪新增/改寫★`t_slim_install_guard_here_bypasses` 換成真的來源 repo fixture(分兩段跑,3→5 條斷言);新增 `t_slim_install_symlink_cwd_audit_path_matches_write_path`(6 條斷言)覆蓋 symlink cwd 下稽核路徑與實際寫入路徑一致性,兩條皆有竄改驗證翻紅記錄
  KEY:★2026-08-01 補追加 Task 13——bash 全部搬成 Python(stdlib only),由使用者直接指派★:`slim/install.sh` 原本承載全部邏輯(293 行純 bash),Windows 上跑不了。改成 ★`slim/install.py` 承載全部邏輯★,`slim/install.sh`(改版後僅 ~35 行)只做兩件事:①沿用原本解 symlink 鏈的迴圈定位套件目錄②挑 `python3`/`python`(Windows 常只有後者)轉發參數執行 `install.py`。新增 `slim/install.ps1`(Windows 對應薄殼,同款轉發邏輯,先例=本 repo 完整版根目錄 `get.ps1`)。三層守衛/CLAUDE.md 合併/manifest 寫入/skill 碰撞語意等全部行為**逐條**搬進 Python,一條不掉——本節點上面所有標記為合約的 KEY 行綁定的 `[test:]` 測試函式名稱一個沒變(只是現在測的是 Python 實作),合約鏈因此不受影響。★移植順帶消掉一整類 bug★:Task 12 修的「CLAUDE.md 寫入路徑用素樸 `$(pwd)` vs 守衛用 `$(pwd -P)`」不一致問題,在 Python 版結構性不存在——`Path.cwd()` 底層就是 `os.getcwd()`,一律回傳實體路徑,沒有第二種路徑表示法可以打架。Windows 支援(全域指令 `.cmd` shim+內容副本配對、skill 目錄實體複製、PATH 提示分平台)讀 `scripts/lumos` 的 `_IS_WIN`/`cmd_install` 分支照做;★這台機器沒有 Windows,無法真機驗證★,`IS_WIN` 可用 `LUMOS_SLIM_SIMULATE_WINDOWS=1` 環境變數注入在非 Windows 機器上驗**分支邏輯**(3 支新測試),測不到 mklink/真實 cmd.exe 執行行為/PATH 真實生效方式,`slim/README.md`〈支援平台〉與 [[Verification/2026-08-01_slim-python移植]] 都留了誠實標記。過程中發現一個真 bug:skill 目錄備份路徑用秒級時間戳(`time.strftime`),`--force` 重裝緊接著卸載兩次備份可能落在同一秒——bash `mv` 對已存在目錄是「搬進去」不報錯,Python `Path.rename()` 對已存在非空目錄直接拋 `OSError`,`t_slim_install_backup_survives_idempotent_reinstall` 實測踩到;修法是遞增後綴的 `_unique_backup_path()`。19 支既有 `t_slim_(install\|uninstall\|get)*` 測試斷言零鬆動,額外新增 2 支薄殼轉發參數獨立單元測試(用假 `install.py`/`uninstall.py` 只印 argv,驗轉發本身不被業務邏輯污染)+ 3 支 Windows 分支測試。詳見 [[Verification/2026-08-01_slim-python移植]],報告 `.superpowers/sdd/公開精簡版_實作計畫/task-13-report.md`
  KEY:★INVARIANT★(★2026-08-01 Task 14 修復①★)`.cmd` shim 呼叫用的直譯器名稱不得寫死字面 `python`——安裝當下用 `_pick_windows_interpreter()`(依序 `shutil.which("python3")`/`shutil.which("python")`)偵測寫進 shim,兩者都找不到才退回 `"python"`。理由:Task 13 移植時沿用的舊寫法與 `install.sh`/`install.ps1` 兩支薄殼自己「python3 優先,找不到才退 python」的判斷邏輯互相矛盾——只有 `python3.exe`、沒有 `python.exe` 的 Windows 機器上,`install.ps1` 用 `python3` 把安裝跑完、印出「裝好了」,但產生的 shim 卻寫死呼叫 `python`,之後每次打 `lumos` 都得到 `'python' is not recognized`,裝完即壞 [test:t_slim_install_windows_shim_does_not_hardcode_python_when_only_python3_available] [audit:sonnet/2026-08-01](因果驗證:`git stash` 只還原 `slim/install.py`、用只有 `python3`、沒有 `python` 的自製 PATH 環境重跑此測試,「不是寫死字面 python」「確實記錄成偵測到的 python3」兩條斷言確實翻紅,回補修復後轉綠)
  KEY:★INVARIANT★(★2026-08-01 Task 14 修復②★)Windows 路徑下,全域指令的碰撞偵測(是否需要 `--force`)必須同時看 `lumos`(`dst_script`)與 `lumos.cmd`(`dst_shim`)——只看前者的舊寫法,若使用者手動刪了 `lumos` 忘了刪 `lumos.cmd`(單獨殘留),非 `--force` 重裝會直接無聲覆寫殘留的 `lumos.cmd`,繞過「碰撞需要 `--force`」這條保護的存在意義 [test:t_slim_install_windows_collision_detects_orphan_cmd_shim] [audit:sonnet/2026-08-01](因果驗證:同一份 `git stash` 前的舊版,只放殘留 `lumos.cmd`、不放 `lumos`,跑不帶 `--force` 的模擬安裝,「視為碰撞」與「內容未被覆寫」兩條斷言確實翻紅——舊版 rc0 且直接覆寫殘留內容;回補修復後轉綠,且帶 `--force` 仍能正常覆寫,對稱驗證未修過頭)
  KEY:(★2026-08-01 Task 14 修復③,保險性修法,★這段語意本身未在真機驗證★)`install.ps1` 收尾原本是裸的 `exit $LASTEXITCODE`——PowerShell 的 `exit` 在 README 教的兩種呼叫方式(`irm ... | iex` 經 [[Systems/slim-get-一行安裝]] 的 `get.ps1`、`& "路徑\install.ps1"` 兩行版)下都會終止整個呼叫端 session,使用者貼上指令、安裝其實成功了,但畫面印完「裝好了」之後整個 PowerShell 視窗突然關閉,會被誤以為是崩潰。改成不呼叫 `exit`,把子行程(`install.py`)的 rc 寫回 `$global:LASTEXITCODE` 供呼叫端在同一 session 內讀取;`uninstall.ps1`/`get.ps1` 同批同款修復,見 [[Systems/slim-uninstall-一行卸載]]/[[Systems/slim-get-一行安裝]]。取捨:若改用 `powershell.exe -File install.ps1` 這種獨立行程啟動方式(README 沒教),行程本身回給作業系統的 exit code 會固定是 0,不受此修法反映失敗——README 教的兩種呼叫方式都在同一 session 內執行,不受影響。★這台機器沒有 PowerShell,無法真機驗證這段語意★,只做了靜態結構檢查 [test:t_slim_ps1_scripts_avoid_session_killing_trailing_exit](非真機驗證,見 `slim/README.md`〈支援平台〉與各 `.ps1` 檔案內的同款誠實聲明)。範圍刻意收窄:三支 `.ps1` 內仍有幾處早期分支的 `exit 2`(找不到 python3/python、找不到 git 等錯誤守衛)理論上有同款風險,本次未觸碰,列為已知殘留風險。詳見 [[Verification/2026-08-01_slim-windows兩缺陷修復]],報告 `.superpowers/sdd/公開精簡版_實作計畫/task-14-report.md`
  KEY:(★2026-08-01 Task 15,補上一行 KEY 標的殘留風險,★這段語意本身仍未在真機驗證★)上一條 KEY 留的「早期分支 exit 2 已知殘留風險」已補——`install.ps1` 找不到 python3/python 那個錯誤分支的裸 `exit 2` 改成邏輯包進 `Invoke-Install` 函式、印完 `Write-Error` 後 `return 2`(函式層級,結束整個函式但不終止呼叫端 session),腳本最下方把函式回傳值收進 `$rc` 再寫回 `$global:LASTEXITCODE`,`uninstall.ps1`/`get.ps1` 同批同款修復(共 6 處,見 [[Systems/slim-uninstall-一行卸載]]/[[Systems/slim-get-一行安裝]])。反向斷言 [test:t_slim_ps1_error_branches_still_halt_via_return](靜態檢查:錯誤訊息後緊接 return,而非讓錯誤分支被拆成不擋往下繼續跑)+ 擴大後的 [test:t_slim_ps1_scripts_avoid_session_killing_trailing_exit](涵蓋範圍從只看結尾擴大成全檔無裸 exit)。詳見 [[Verification/2026-08-01_slim-ps1早期分支exit修復]],報告 `.superpowers/sdd/公開精簡版_實作計畫/task-15-report.md`
  KEY:(★2026-08-01 Task 16 修復①③,分支終審抓到 BLOCKER,補 Task 15 真正的漏洞)三支 `.ps1` 頂部的 `$ErrorActionPreference = "Stop"` 會把 `Write-Error` 這種非終止型錯誤升級成終止型例外(等同 `throw`)——Task 15 的反向斷言 `t_slim_ps1_error_branches_still_halt_via_return` 只驗了「Write-Error 後面緊接著 return 2」這個原始碼長相,完全沒注意到 `Stop` 之下 `Write-Error` 那行自己就會先拋例外,`return 2` 根本執行不到、例外炸穿 `Invoke-Install`、連 `$rc = Invoke-Install ...` 都完成不了、`$global:LASTEXITCODE` 永遠寫不進去,呼叫端讀到殘留舊值(很可能是 0)把失敗誤判成成功——**這正好打掉 Task 14/15 整段修法的目的,且發生在它最該保護的錯誤路徑上**;這是本專案第十次撞到「測試存在但沒在驗它宣稱要驗的」(驗長相不驗語意)。修法①:`install.ps1` 唯一一處 `Write-Error` 加 `-ErrorAction Continue` 明確覆寫——這是 PowerShell 官方支援的逐次呼叫覆寫機制,對單一 cmdlet 呼叫的 `-ErrorAction` 會蓋過 `$ErrorActionPreference`;選這個而非拆 try/catch 或改用 `[Console]::Error`,理由:①官方支援的逐次呼叫覆寫、語意上最直接對應意圖②維持既有「Write-Error 後緊接 return」結構③訊息仍然真的走 Write-Error(進 stderr、保留 PowerShell 錯誤記錄語意)。`$ErrorActionPreference = "Stop"` 本身保留,只放行 Write-Error 這條刻意非終止的分支。修法③(一致性處理,本檔案原本不受影響——經 `&` 呼叫本來就有自己的 script scope,不像 [[Systems/slim-get-一行安裝]] 的 `get.ps1` 被 `iex` 污染呼叫端 session,細節見該節點):`$ErrorActionPreference = "Stop"` 搬進 `Invoke-Install` 函式內部第一行;頂層 `$Pkg = Split-Path -Parent $MyInvocation.MyCommand.Path` 刻意留在函式外(`$MyInvocation` 在函式內語意會變,指向函式自己而非整支腳本的呼叫資訊)。[test:t_slim_ps1_write_error_noterminating_under_stop_preference](靜態結構檢查,驗得到「Write-Error 本行有無 -ErrorAction Continue」這種寫法,驗不到 PowerShell 真實執行時 Stop 語意是否真的被這個參數蓋掉,見該測試 docstring 誠實邊界——不把 mutant 驗證當作「測試夠格」的證據)。★這段語意本身仍未在真機驗證★。詳見 [[Verification/2026-08-01_slim-終審三缺陷修復]],報告 `.superpowers/sdd/公開精簡版_實作計畫/task-16-report.md`
verified_by:
  - "[[Verification/2026-07-31_slim-install安裝器落地]]"
  - "[[Verification/2026-07-31_公開精簡版交付]]"
  - "[[Verification/2026-07-31_公開精簡版一行安裝卸載與代碼審修復]]"
  - "[[Verification/2026-07-31_slim-claude-md注入]]"
  - "[[Verification/2026-07-31_slim-claude-md第三次裁定取代與備份還原]]"
  - "[[Verification/2026-07-31_slim-uninstall步驟獨立化與manifest基準修復]]"
  - "[[Verification/2026-08-01_slim-install注入目標守衛]]"
  - "[[Verification/2026-08-01_公開精簡版代碼審第四輪修復]]"
  - "[[Verification/2026-08-01_slim-python移植]]"
  - "[[Verification/2026-08-01_slim-windows兩缺陷修復]]"
  - "[[Verification/2026-08-01_slim-ps1早期分支exit修復]]"
  - "[[Verification/2026-08-01_slim-終審三缺陷修復]]"
  - "[[Verification/2026-08-01_slim-manifest殘留與代碼審六輪]]"
  - "[[Verification/2026-08-02_slim三缺陷修復_實驗產出]]"
  - "[[Verification/2026-08-03_Windows回歸測試三缺陷]]"
  - "[[Verification/2026-08-03_Windows真機三輪驗證通過]]"
related:
  - "[[Systems/slim-get-一行安裝]]"
  - "[[Systems/slim-uninstall-一行卸載]]"
---
# slim-install-安裝器

公開精簡版交付前的機器層安裝器。隨交付包一起走(`$(dirname "$0")` 定位自身,不吃路徑參數),做三件事:①裝全域 `lumos` 指令,★2026-07-31 Task 10 新增★同步寫身分證 manifest(`~/.local/share/lumos-slim/manifest.json`,含安裝當下的 bin sha256)②實體複製 `skills/lumos-project-notes` 到 `~/.claude/skills/`(非 symlink,交付包可被刪掉/搬走後 skill 仍在)③(Task 9 裁定第三次變更)在執行目錄(專案根)的 `CLAUDE.md` 裡放策展過的精簡版紀律區塊,取代掉完整版(若有)。**裁定演進**:①原裁定絕不碰 CLAUDE.md ②Task 8 開放只准附加、檔尾、絕不覆蓋完整版區塊、兩套規則並存 ③Task 9 發現「兩套規則並存」本身是問題,裁定改成有完整版區塊就整段移除、**原地換成**精簡版區塊;移除前策展吸收完整版裡仍然有效的內容、先把原文位元組級備份(base64 藏進精簡版區塊自己的 HTML 註解裡),`uninstall.sh` 能精確還原 ④**Task 10(本次,端到端實測抓到真 bug)**:發現 `uninstall.sh` 原本拿 `~/.lumos-slim/scripts/lumos` 當比對基準,但那個固定落點只有走 `get.sh` 才會存在——README 也在教的「直接 clone 交付包跑 install.sh」這條路不會建立它,導致卸載時比對基準必然缺失、腳本中止、CLAUDE.md 還原沒機會執行。裁定:install.sh 多寫一份 manifest 當穩定身分證,不依賴 `~/.lumos-slim`。**只動使用者 `$HOME` 與這一份 CLAUDE.md,不碰專案其餘任何檔案**——反誤傷測試 `t_slim_install_no_project_touch`。仍明確不做:不 scaffold 圖譜、不 vendor 工具進專案、不設 `core.hooksPath`、不裝任何 Claude hook。⑤**Task 11(本次,真實事故驅動——同一支腳本被子代理在本 repo 根目錄誤跑過兩次,見上方 ★INVARIANT★)**:在做那三件事之前,先加一道「注入目標守衛」——不像專案根/是 lumos 工具鏈來源 repo 兩種危險目標都拒絕(rc2、不動任何檔案),動手前不論過不過都先印出目標絕對路徑;`--here` 旗標明示可繞過前兩層。詳見 [[Projects/公開精簡版_實作計畫]] Task 3、Task 8、Task 9、Task 10(★端到端實測抓到真 bug:manifest 身分證 + uninstall 步驟互不阻擋★)、Task 11(★注入目標守衛★)、Task 12(★分支終審第四輪代碼審修復:`--here` 逃生閥回歸測試 fixture 換真、CLAUDE.md 寫入路徑改用已解 symlink 的 `TARGET_CLAUDE_MD`★)。

規格/設計脈絡見 `.superpowers/sdd/公開精簡版_實作計畫/task-3-brief.md`／`task-9-report.md`／`task-10-report.md`／`task-11-report.md`(SDD 產出,非圖譜路徑,依計畫落地於此);spec [S3] 裁定第三次變更、[S4-c] 見 [[Projects/公開精簡版_計劃]]。
