---
type: system
status: done
created: 2026-08-19
updated: 2026-08-19
aliases:
  - 工作流全貌
  - workflow overview
  - 流程圖
tags:
  - type/system
  - status/done
  - scope/graph-governance
summary: |-
  FLOW:①寫計劃(brainstorm+架構圖先行三步)→②設計審(design-loop 處置閘)→③寫 code(impact hook 端筆記+pre-commit 擋不同步)→④終審+收尾(code-loop 對抗審+Verification 節點+自足性審計蓋章)→⑤推送(pre-push 硬擋+CI 兜底)→⑥doctor 巡檢(含 Check S 提醒過期自審)→有漏補鏈→架構圖健康
  KEY:本節點=全工作流的白話總覽圖(mermaid,對外解說/新人導覽用)——每步標注對應機制名,內行外行都能對回去;各機制的權威細節在各自節點與 skill,本圖不承載規格
  KEY:每一步都有「長連結」的動作:①出生帶線(參考誰/解哪個 Issue)②每折一洞多一線+審計留痕③實作中發現的相依當場記④證據雙向互指(plan_refs/verified_by)——設計哲學=沒有任何時間點需要靠記憶力回想全部
  KEY:自足性審計的位置=④收工儀式內(乾淨子代理只讀架構圖還原比對,self-audit 蓋章);⑥doctor Check S 軟提醒過期未審當兜底。★已知坑:在「從 Claude 開出來的終端」裡啟動的 claude 會繼承 CLAUDE_CODE_CHILD_SESSION 而禁子代理→自足性審計被跳過(判法/解法見本文〈已知坑〉節;同型記憶帳=terminal-leaked-child-session-marker)★
  DEP:[[Systems/design-loop]]｜[[Systems/pitfalls-code-loop]]｜[[Systems/lumos-cli-read]]｜skills/lumos-project-notes(自足性審計規格)｜scripts/hooks(pre-commit/pre-push)
related:
  - "[[Systems/design-loop]]"
  - "[[Systems/pitfalls-code-loop]]"
  - "[[Systems/lumos-cli-read]]"
---
# 開發工作流總覽(白話版)

> 用途:對外解說與新人導覽的一張圖。每步的白話描述+對應機制名;規格細節在各機制自己的節點,本圖刻意不展開。

```mermaid
flowchart TD
    A["① 動手前先寫計劃<br/>規矩:先翻筆記庫、把相關的舊筆記讀過,才准開寫<br/>🔧 機制:brainstorm(需求釐清+方案取捨)<br/>+架構圖先行三步 search→context→contracts"]
    A1(["🔗 筆記一出生就帶線<br/>「我參考了誰」「我在解哪個問題」"])
    A -.->|讀過誰就連誰| A1

    A --> B["② 設計稿送審<br/>請幾個沒看過內文的人專門挑毛病<br/>→ 逐條改進稿子 → 每條有交代才過關<br/>🔧 機制:design_loop(處置閘收斂)"]
    B1(["🔗 每改一個毛病就多一條線<br/>審完的結論也寫回筆記"])
    B -.->|一輪一輪累積| B1

    B --> C["③ 動手寫程式(每次提交)<br/>系統自動把「跟你改的檔案有關的筆記」端到眼前<br/>想只改程式不更新筆記?提交會被擋下來<br/>🔧 機制:impact hook(端筆記)+pre-commit(擋提交)"]
    C1(["🔗 寫的過程發現「原來這兩塊有牽連」<br/>當場記一筆,不留到最後憑回憶"])
    C -.->|發現當下就記| C1

    C --> D["④ 收工前的終審+收尾<br/>風險高的分支,請一批人專挑程式的毛病<br/>寫一篇「做完了、怎麼驗的」紀錄,計劃結案<br/>再派一個只准讀筆記庫的乾淨幫手<br/>驗證「光看筆記,能不能還原這整件事」<br/>🔧 機制:code_loop(對抗代碼審)+自足性審計(self-audit 蓋章)"]
    D1(["🔗 證據互相指認<br/>驗證紀錄指回「當初的計劃」<br/>被改的模組筆記指向「這次的驗證」"])
    D -.->|收工儀式的一部分| D1

    D --> P["⑤ 推送上遠端<br/>推之前最後一道門:全庫健檢+驗證器沒被動過手腳<br/>+高風險分支沒審完就是推不出去<br/>🔧 機制:pre-push(硬擋)+CI 事後兜底"]

    P --> E["⑥ 定期健檢(隨時可跑)<br/>順便提醒「哪些筆記太久沒做自足性審計」<br/>🔧 機制:doctor(全圖巡檢+Check S 軟提醒)"]
    F{"有漏嗎?"}
    E --> F
    F -->|"沒人連的孤兒筆記/只連了單向的線/點了會斷的連結"| G["把線補上"]
    G --> E
    F -->|無| H(["✅ 筆記庫健康<br/>從頭到尾沒有哪一刻<br/>需要靠腦袋記住全部"])

    style A1 fill:#e8f4e8,stroke:#4a4,color:#333
    style B1 fill:#e8f4e8,stroke:#4a4,color:#333
    style C1 fill:#e8f4e8,stroke:#4a4,color:#333
    style D1 fill:#fdf3d8,stroke:#c93,color:#333
    style P fill:#fce8e8,stroke:#c66,color:#333
    style H fill:#e0eefc,stroke:#369,color:#333
```

## 機制對應一句話版

| 步驟 | 機制 | 管什麼 |
|---|---|---|
| ① | brainstorm+架構圖先行 | 想清楚再動筆 |
| ② | design_loop | 設計稿別帶病進實作 |
| ③ | impact hook+pre-commit | 每次提交都同步筆記 |
| ④ | code_loop+自足性審計 | 合併前有人對抗式挑過毛病;筆記本身能自證 |
| ⑤ | pre-push+CI | 沒過關的東西物理上推不出去 |
| ⑥ | doctor | 定期抓漏、補鏈 |

## 已知坑(自足性審計被跳過)

「不允許子代理,跳過自審」的根因不是設定被關,而是該 session 在**子會談環境**跑:從 Claude 內開出的終端會繼承 `CLAUDE_CODE_CHILD_SESSION`,其中啟動的 claude 被視為子代理、不得再生子代理。判法:`env | grep CHILD_SESSION` 有值即中;解法:換一個非 Claude 衍生的終端。漏掉的自審由 doctor Check S 提醒,回正常 session 補蓋章。
