# lumos 全景圖(白話總覽)

> 面向第一次聽到 lumos 的人。技術細節見 [架構圖即合約.md](架構圖即合約.md);設計脈絡見 [架構圖即合約-對外論述.md](架構圖即合約-對外論述.md)。

**lumos 是什麼(一句話)**:一套幫你「跟 AI 一起開發時,不會亂來、規則有據可查、出錯能及早發現」的工具。

下面這張圖,是它怎麼幫你把關——從**平時改一段 code**,到**進實作前審一份新設計**,到**每天沒人盯著也能自己跑**。

```mermaid
flowchart TD
    classDef base fill:#fee2e2,stroke:#dc2626
    classDef book fill:#fef3c7,stroke:#d97706
    classDef check fill:#dbeafe,stroke:#2563eb
    classDef ai fill:#dcfce7,stroke:#16a34a
    classDef loop fill:#e0e7ff,stroke:#4f46e5
    classDef human fill:#fce7f3,stroke:#db2777
    classDef anchor fill:#f3e8ff,stroke:#9333ea

    subgraph Z["⓪ 平時改 code 時:規則本被逼著跟上 (最日常的地基)"]
        Z1["改完 code 忘了更新規則本 → 當下就軟提醒"]
        Z2["真要存檔(commit)沒帶規則本 → 直接擋下,進不了版控"]
        Z3["硬要繞過?可以,但會留下記錄<br/>(誰繞過、繞幾次,事後查得到)"]
        Z4["存檔後自動掃:以前驗證過的結論,<br/>被這次改動弄失效了沒?(抓『悄悄壞掉』)"]
        Z5["📌 規則本跟程式碼放在同一個倉庫、綁在一起版控<br/>—— 改 code 就被逼著想到規則本,這就是核心"]
        Z6["AI 只負責『指出哪裡可能壞了』,不自動改<br/>—— 改不改留給人逐筆看(怕 AI 誤判)"]
        Z1 --> Z2 --> Z3 --> Z4
    end

    subgraph A["① 一本『共同記憶 + 規則本』(有完整的生老病死)"]
        A1["寫下『不能亂改的鐵則』+ 每條配一個會跑的測試當證據"]
        A2["危險動作(上線/改正式資料)標記出來 + 先寫好『萬一錯了怎麼收回』"]
        A3["每條規則記『在什麼前提下才成立』<br/>—— 前提變了,就該回頭重驗"]
        A4["會過期:舊的半年自動歸檔、定期找乾淨 AI 體檢『有沒有失真』"]
        A5["跨專案共用的鐵則 → 升格到獨立倉庫<br/>別人只留一個『指針』指過去,不各自複製(複製必走樣)"]
    end

    subgraph B["② 檢查員定期巡查 (11 道;大多只提醒,少數硬擋)"]
        B1["該寫的鐵則寫了沒?綁的測試是真的、跑得動嗎?"]
        B2["有沒有斷掉的連結、沒人理的孤兒、寫一半沒接回去的?"]
        B3["驗證紀錄有沒有指回它驗的那份計劃?<br/>(計劃改了 → 提醒回頭看)"]
        B4["最重的鐵則只測了順風案例嗎?危險動作有沒有寫回退?<br/>規則本有沒有過期該重審?"]
    end

    subgraph C["③ 動手寫程式前,先派 AI 審一遍設計 (進階防護)"]
        C1["審查員專挑毛病"] --> C2["偷塞已知錯誤,測它有沒認真看"]
        C2 --> C3["另一個 AI 獨立打分"] --> C4["辯護律師反駁、逼拿代碼證據,免得冤枉"]
        C4 --> C5["換一家公司的 AI 再看(避開相似盲點)"] --> C6["連兩輪沒大問題才過關"]
    end

    subgraph D["④ 每天自動跑一遍 (沒人盯著也能動)"]
        D1["每天 AI 盤點:外面有什麼新做法/新風險,列出『哪裡可以更好』"]
        D2["挑一個來做(做過的永遠跳過;一次只處理一個,<br/>上一個沒人點頭前不開新的)"]
        D3["自動設計 → 自動走一遍③那套審查"]
        D4["審不過 → 降分放回、改天再試;試三次還不過 → 放棄、留給人"]
        D5["過了 → 附一份『可信度 + 還有哪些查不到的風險』報告<br/>停在『等人點頭』,絕不自動上線"]
        D6["⚠ 每份設計都要聲明『碰了哪些規則文件』<br/>—— 放行時一起更新,防規則本跟實作脫節"]
        D1 --> D2 --> D3 --> D4
        D3 --> D5 --> D6
    end

    EDIT["✏️ 平時:改一段 code"] ==> Z
    Z -.觸發.-> B
    Z ==> A
    START["💭 要做個新東西 / 大改"] ==> C
    A <-->|檢查員讀規則本| B
    C ==>|審過| CI["🎯 真的把測試跑一遍,綠了才能上線"]
    CI ==> A
    D -. 用的是同一套③ .-> C

    LEDGER["🧾 所有攔截/繞過/腐化/審計,全部留痕,事後可稽核<br/>(不是法律,是『摩擦』+ 看得見)"]
    Z3 -.-> LEDGER
    B -.-> LEDGER

    HUMAN["👤 最後一定是『人』拍板放行 — AI 全程只是多幾層保險"]
    D6 ==> HUMAN
    CI ==> HUMAN

    NOTE["💡 核心信念:AI 審查再多層也不可能 100% 對。<br/>真正靠得住的是『測試跑綠』+『人最後確認』;AI 只是降低出錯機率的摩擦,不是萬靈丹。<br/>而這整套的地基,是『改 code 就被逼著更新規則本』—— 這才是 lumos 的名字(架構圖即合約)由來。"]

    class Z1,Z2,Z3,Z4,Z5,Z6 base
    class A1,A2,A3,A4,A5 book
    class B1,B2,B3,B4 check
    class C1,C2,C3,C4,C5,C6 ai
    class D1,D2,D3,D4,D5,D6 loop
    class HUMAN human
    class NOTE,CI,LEDGER anchor
```

## 怎麼讀這張圖

lumos 有**三條線**,由近到遠:

### ⓪ 最日常的地基:平時改 code,規則本被逼著跟上
這是 lumos 的本體,也是它叫「架構圖即合約」的原因。規則本(知識架構圖)跟程式碼**放在同一個倉庫、綁在一起版控**——你改了 code 忘了更新規則本,當下軟提醒;真要存檔(commit)會被擋;硬要繞過會留下記錄;存檔後還會自動掃「以前驗證過的結論,被這次改動弄失效了沒」。重點:**讓「不更新規則本」變成比「更新」還麻煩的事。** 而 AI 在這裡只負責「指出哪裡可能壞了」,不自動改——改不改留給人,因為 AI 會誤判。

### ① 那本「共同記憶 + 規則本」有完整的生老病死
不只是「寫下鐵則」:每條鐵則配一個**真的會跑的測試**當證據(嘴上說重要不算);危險動作先寫好「萬一錯了怎麼收回」;每條規則記「在什麼前提下成立」(前提變了要重驗);舊的半年自動歸檔、定期找乾淨 AI 體檢有沒有失真;跨專案共用的鐵則升格到獨立倉庫、別人**只留指針不複製**(複製必走樣)。

### ② 檢查員定期巡查(共 11 道,大多只提醒、少數硬擋)
不跑測試,只看「形式上齊不齊」:鐵則寫了沒、綁的測試真的跑得動嗎、有沒有斷鏈/孤兒/寫一半沒接回去的、驗證紀錄有沒有指回它驗的計劃、最重的鐵則是不是只測了順風案例、危險動作有沒有寫回退、規則本有沒有過期。

### ③ 進實作前,派 AI 審一遍設計(進階防護)
真正動手寫程式之前,先派 AI 審設計草稿。但 AI 會偷懶、會冤枉人,所以疊好幾道:**偷塞已知錯誤**測它有沒認真看、找另一個 AI **獨立打分**、派**辯護律師**反駁逼它拿代碼證據(免得冤枉好設計)、再**換一家公司的 AI** 看(避開相似盲點)、**連兩輪**沒大問題才過關。

### ④ 整套③可以每天沒人盯著、自己跑
每天 AI 盤點外界 + 列改進清單,挑一個來做(做過的永遠跳過、一次只處理一個)、自動設計、自動走③、審不過就降分改天再試(三次還不過就放棄留給人),過了就附「可信度 + 還有哪些查不到的風險」報告,**停在等人點頭、絕不自動上線**。每份設計還要聲明碰了哪些規則文件,放行時一起更新——防規則本跟實作脫節。

### 橫切兩條底線
1. **🧾 全部留痕**:所有攔截、繞過、腐化、審計都記錄下來,事後可稽核——不是法律約束,是「摩擦 + 看得見」。
2. **🎯 真錨點**:不管 AI 審了幾層,真正算數的只有兩件——**把測試真的跑綠**、**最後由人拍板**。而「測試真的跑綠」成立的前提=批改程式沒被動過——`lumos anchor verify`(2026-07-02)把這個前提從盲信變成可機械核對的宣稱(runner/hooks sha256 baseline,pre-push 與自主 loop 入口把關)。

## 一句話收束

**AI 審查再多層,也不可能 100% 對。真正靠得住的是「測試跑綠」+「人最後確認」;所有 AI 把關都只是降低出錯機率的摩擦,不是萬靈丹。** 這也是為什麼 ③④ 那些聰明的 AI 審查,在圖上永遠是「進階層」,而 ⓪ 那條「改 code 就被逼著更新規則本」的笨地基,才是 lumos 的根。
