# ⚠ 本目錄已降唯讀歷史（2026-07-21）

**新設計/spec 一律寫架構圖計劃節點**（`docs/lumos-toolchain-knowledge/Projects/<主題>_計劃.md`），不再寫本目錄——「架構圖即唯一真相」涵蓋計劃（CLAUDE.md）。

本目錄 30 份既有設計文件保留供考古（部分被架構圖節點以 `core_refs`/正文路徑引用），**不再新增、不再折入審計修正**。design-loop 的工作對象、gate `--spec`、golden 凍結自 2026-07-21 起一律以計劃節點為準。

背景：外審發現 spec 雙真相來源（本目錄 vs 架構圖計劃節點）構成靜默漂移風險——見架構圖 `Projects/全盤外審2026-07_調研` finding 1。
